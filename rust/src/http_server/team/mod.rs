//! Team Server HTTP layer — OSS stub (ADR-023).
//!
//! The full implementation (RBAC, connectors, request pipeline, token auth)
//! lives in `lean-ctx-enterprise/team-server`. This stub keeps the module
//! declaration so `http_server/mod.rs` compiles without feature-gating.
