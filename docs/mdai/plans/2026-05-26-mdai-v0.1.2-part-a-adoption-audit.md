# mdai-Skill v0.1.2 Part-A — Adoption-Audit gegen markdownai v1.0.0

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or
> `superpowers:executing-plans`. Steps use checkbox (`- [ ]`) syntax for tracking.
>
> **Subagent-Modell:** Default `sonnet`. Task 2 (Findings-v2 Re-Validation) darf bei Bedarf `opus` für
> komplexes Engine-Source-Reasoning (§2/§3/§5 Macro-Edge-Cases).

**Goal:** Phase 0 des v0.1.2-Refits ausführen — datengetriebene Entscheidung zwischen Variante A
(MDAI_LIBRARY_ROOT eliminieren via `source_root` absolut) und Variante B (Status quo) durch zwei volle
`mdai-brainstorm`-E2E-Runs, plus systematische Re-Validation aller 12 v0.1.1-Findings gegen die v1.0.0-Engine.
Endet mit hartem User-Decision-Gate über Audit-Konsolidierung.

**Architecture:** 3 Tasks, strikt sequentiell. T0.1 erzeugt einen Wegwerf-Branch `audit/no-mdai-root` für die
Variante-A-Isolation, fährt 4 Smokes pro Variante (list_phases / resolve_phase / full brainstorm / call_macro × 3),
reverts security.json + löscht Branch. T0.2 dispatcht zwei parallele Subagents (je 6 Findings) und persistiert
12 Tupel in `ctx_session`. T0.3 konsolidiert beide Outputs in einen Audit-Doc und blockt bis der User die
zwei Decisions (Variante + Findings-Action-Items für v0.1.3) gibt.

**Tech Stack:** markdownai v1.0.0 (CLI + MCP), `ctx_*` (lean-ctx-MCP), JetBrains-MCP. Verifikation ausschließlich
via `mcp__markdownai__*` + `ctx_search` + Engine-Source-Reads. KEINE permanenten Code-Edits an Library-Files.

**Hard Dependency:**

- v0.1.1-Plan-Part-2 complete (commit `8262d5b5` + post-phase commits `475544a1` `b2415931` auf `feat-mdai`).
- Findings-v1-Doc committed (`6a128e0b`).
- Spec committed (`efcc4b1a`: `docs/mdai/specs/2026-05-26-mdai-v0.1.2-refit-v1.0-audit-design.mdai.md`).
- markdownai-Submodule auf v1.0.0+ (commit `3bdd5a7` oder neuer auf `feat-mdai` des Submodule).
- Working tree clean auf `feat-mdai` vor Pre-Flight.

**Plan-Part-Kette:**

- **Part-A (this):** Phase 0 — Adoption-Audit. User-Approval-Gate am Ende.
- Part-B: `docs/mdai/plans/2026-05-26-mdai-v0.1.2-part-b-findings-v2-and-dedup.md` — Phase 1 (Findings-v2-Doc) + Phase
  2 (Dedup-Investigation). Startet erst nach Part-A-Approval.
- Part-C: `docs/mdai/plans/2026-05-26-mdai-v0.1.2-part-c-v011-closure.md` — Phase 3 (v0.1.1-Abschluss) +
  Final-Verification.

**Spec:** `docs/mdai/specs/2026-05-26-mdai-v0.1.2-refit-v1.0-audit-design.mdai.md`

**Carry-Forwards für Part-B (von dieser Plan-Datei produziert):**

- `docs/mdai/audits/2026-05-26-mdai-library-root-audit.md` (committed in T1 Step 16).
- `docs/mdai/audits/2026-05-26-mdai-v1.0-adoption-audit.md` (committed in T3 Step 4, mit Frontmatter-Decisions).
- 12 ctx_session-Tupel `[t0.2-§1]..[t0.2-§12]` (persistiert in T2; Part-B Task 4 konsumiert sie).
- 8 ctx_session-Tupel `[t0.1-A-smoke1..4]` + `[t0.1-B-smoke1..4]` (persistiert in T1; Part-C Task 7 konsumiert
  die B-Tupel).

---

## File-Map (Touch-Inventar Part-A)

### Neu erstellte Dateien

| Pfad | Task | Zweck |
| `~/.markdownai/security.json.pre-audit`                    | T0 | Backup vor T1 Variante-A-Edit (Cleanup-State für
Part-C T6) |
| `docs/mdai/audits/2026-05-26-mdai-library-root-audit.md`   | T1 | Vergleichstabelle Variante A vs. B + Empfehlung |
| `docs/mdai/audits/2026-05-26-mdai-v1.0-adoption-audit.md`  | T3 | Konsolidierte Audit-Doc + persistierte
User-Decisions |

### Modifizierte Dateien (temporär, im Wegwerf-Branch — reverted)

| Pfad | Task | Aktion |
| `~/.markdownai/security.json`                 | T1 | Variante-A-Config setzen, dann revert auf pre-audit-State |
| `mdai/**/*.md`                                | T1 | Sed-Substitution im Wegwerf-Branch, Branch-Delete reverts |

### Wegwerf-Artefakte (NICHT committen)

- Branch `audit/no-mdai-root` (erstellt in T1 Step 2, gelöscht in T1 Step 10).
- `/tmp/t02-repro/` (T2-Repros, gelöscht in T2 Step 4).
- `/tmp/t01-commit-msg.txt`, `/tmp/t03-commit-msg.txt` (heredoc-Commit-Files, gelöscht nach Commit).

---

## Pre-Flight

### Task 0: Branch-Snapshot + Backup setzen

**Files:** Backup-Operation (kein In-Tree-File-Touch).

- [ ] **Step 1: Working tree + Branch verifizieren**

`mcp__lean-ctx__ctx_shell(command="git status", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: Branch = `feat-mdai`. Untracked-Files OK (z.B. der alte Plan v1, andere docs). KEINE unstaged
Modifikationen an `mdai/**/*.md`, `docs/mdai/specs/2026-05-26-...`, `docs/mdai/audits/`, oder
`docs/mdai/findings/`.

- [ ] **Step 2: Git-Log Sanity (Spec + Findings-v1 erreichbar)**

`mcp__lean-ctx__ctx_shell(command="git log --oneline -5", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet (in irgendeiner Reihenfolge in den letzten 10 Commits): `efcc4b1a docs(mdai/specs): v0.1.2 refit …`,
`6a128e0b docs(mdai/findings): v0.1.1 engine-constraints …`, `b2415931`, `475544a1`, `8262d5b5`.

- [ ] **Step 3: markdownai v1.0.0 verifizieren**

`mcp__lean-ctx__ctx_shell(command="cat markdownai/packages/core/package.json", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: `"version": "1.0.0"` (oder höher).

`mcp__lean-ctx__ctx_shell(command="which mai", cwd="/home/tholo/Scripts/lean-ctx")`
`mcp__lean-ctx__ctx_shell(command="mai --version", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: V1 = absoluter Pfad (z.B. `/home/tholo/.npm-global/bin/mai`); V2 = `1.0.0`.

- [ ] **Step 4: MDAI_LIBRARY_ROOT env-Check**

`mcp__lean-ctx__ctx_shell(command="env | grep MDAI_LIBRARY_ROOT", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: `MDAI_LIBRARY_ROOT=/home/tholo/Scripts/lean-ctx/mdai`. Falls leer: `source ~/.zshrc` oder neuer
Terminal, dann Pre-Flight wiederholen.

- [ ] **Step 5: `~/.markdownai/security.json` Backup erzeugen**

`mcp__lean-ctx__ctx_shell(command="cp ~/.markdownai/security.json ~/.markdownai/security.json.pre-audit", cwd="/home/tholo/Scripts/lean-ctx")`
`mcp__lean-ctx__ctx_shell(command="ls -l ~/.markdownai/security.json ~/.markdownai/security.json.pre-audit", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: beide Files existieren, identische Größe.

- [ ] **Step 6: Pre-Variante-A-Inventar (Library-Treffer-Count)**

`mcp__lean-ctx__ctx_search(pattern="MDAI_LIBRARY_ROOT", path="mdai/", max_results=200)`

Treffer-Count notieren (Hypothese aus v0.1.1: ≥ 5 in body.mdai.md, write-spec.md, spec-reviewer.md u.a.).

Persistieren: `mcp__lean-ctx__ctx_session(action="finding", val="[t0.1-pre-sed] count=N files=[…]")`.

Kein Commit nötig — Pre-Flight ist nicht in-tree.

---

## Phase 0: Adoption-Audit

### Task 1: T0.1 — MDAI_LIBRARY_ROOT-Audit (E2E pro Variante)

**Files:**

- Modify (temporär, im Wegwerf-Branch): `~/.markdownai/security.json`, `mdai/**/*.md`
- Create: `docs/mdai/audits/2026-05-26-mdai-library-root-audit.md`
- Revert: `~/.markdownai/security.json`

**Ziel:** Zwei vollständige `mdai-brainstorm`-E2E-Runs (je 4 Smokes), Vergleichstabelle als Audit-Doc.

- [ ] **Step 1: Wegwerf-Branch erzeugen**

`mcp__lean-ctx__ctx_shell(command="git checkout -b audit/no-mdai-root", cwd="/home/tholo/Scripts/lean-ctx")`
`mcp__lean-ctx__ctx_shell(command="git status", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: Branch = `audit/no-mdai-root`, working tree unverändert (clean wenn vor Pre-Flight schon clean).

- [ ] **Step 2: Sed-Substitution `${MDAI_LIBRARY_ROOT}/` → `mdai/` in der Library**

Zwei find+sed-Operationen über alle `mdai/**/*.md`:

`mcp__lean-ctx__ctx_shell(command="find mdai -name '*.md' -type f -exec sed -i 's|@include \\${MDAI_LIBRARY_ROOT}/|@include mdai/|g' {} +", cwd="/home/tholo/Scripts/lean-ctx")`
`mcp__lean-ctx__ctx_shell(command="find mdai -name '*.md' -type f -exec sed -i 's|@import \\${MDAI_LIBRARY_ROOT}/|@import mdai/|g' {} +", cwd="/home/tholo/Scripts/lean-ctx")`

- [ ] **Step 3: Post-Sed-Verifikation (0 Restvorkommen)**

`mcp__lean-ctx__ctx_search(pattern="@include \\$\\{MDAI_LIBRARY_ROOT\\}/", path="mdai/", max_results=10)`
`mcp__lean-ctx__ctx_search(pattern="@import \\$\\{MDAI_LIBRARY_ROOT\\}/", path="mdai/", max_results=10)`

Erwartet: V1 = 0 matches, V2 = 0 matches.

Falls Restvorkommen: Run pausieren, manuell investigieren (z.B. Macro-Body mit Pfad in `@set`-String).
KEINE Smokes ohne 0/0.

- [ ] **Step 4: Variante-A security.json setzen**

`mcp__lean-ctx__ctx_read(path="/home/tholo/.markdownai/security.json", mode="full")`

Aus dem aktuellen Content das `filesystem`-Objekt finden und durch Folgendes ersetzen (via
`mcp__lean-ctx__ctx_edit` für Merge, oder `Write` falls `filesystem` komplett fehlt):

```json
{
  "filesystem": {
    "source_root": "/home/tholo/Scripts/lean-ctx",
    "data_root": "cwd",
    "allowed_source_paths": [
      "/home/tholo/Scripts/lean-ctx/mdai/**"
    ],
    "allowed_data_paths": [
      "/home/tholo/Scripts/lean-ctx/mdai/**",
      "/home/tholo/Scripts/lean-ctx/docs/mdai/**"
    ],
    "write_enabled": true
  }
}
```

Verifikation:
`mcp__lean-ctx__ctx_read(path="/home/tholo/.markdownai/security.json", mode="full", fresh=true)`

Erwartet: `source_root: "/home/tholo/Scripts/lean-ctx"` (absoluter Pfad, NICHT `"auto"`).

- [ ] **Step 5: Variante-A Smoke 1 — `list_phases`**

`mcp__markdownai__list_phases(file="mdai/skills/mdai-brainstorm/body.mdai.md", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: `found: true`, `phases`-Array enthält 5 Namen: `pre-context`, `dialog-rules`, `dialog-process`,
`write-outputs`, `handoff`.

`mcp__lean-ctx__ctx_session(action="finding", val="[t0.1-A-smoke1] status=PASS|FAIL phases=[…] errors=[…]")`

- [ ] **Step 6: Variante-A Smoke 2 — `resolve_phase pre-context`**

`mcp__markdownai__resolve_phase(file="mdai/skills/mdai-brainstorm/body.mdai.md", phase="pre-context", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: `warnings: []`, `content` enthält Substrings aus `lean-context.md`, `tool-quick-ref.md`,
`hard-rules.md` (z.B. „Context Engineering Layer", „ctx_read", „No worktrees"). KEINE ENOENT-warnings.

`mcp__lean-ctx__ctx_session(action="finding", val="[t0.1-A-smoke2] status=PASS|FAIL warnings_count=N content_keywords=[…]")`

- [ ] **Step 7: Variante-A Smoke 3 — voller brainstorm-Run**

In Claude-Code-Session manuell ausführen: `/superpowers:brainstorming dummy-feature-x als Testlauf für T0.1 Variante A`.

Akzeptanz: Brainstorm-Skill durchläuft alle Phasen ohne ENOENT auf Library-Files, spec-self-review läuft
fehlerfrei, Spec wird unter `docs/mdai/specs/2026-05-26-dummy-feature-x-design.mdai.md` (oder ähnlich) erzeugt.

**Nach dem Run:** Test-Spec(s) löschen:
`mcp__lean-ctx__ctx_shell(command="rm -f docs/mdai/specs/2026-05-26-dummy-feature-x*", cwd="/home/tholo/Scripts/lean-ctx")`

`mcp__lean-ctx__ctx_session(action="finding", val="[t0.1-A-smoke3] status=PASS|FAIL spec_created=true|false errors=[…]")`

- [ ] **Step 8: Variante-A Smoke 4 — `call_macro` × 3**

Drei sequentielle MCP-Calls:

`mcp__markdownai__call_macro(file="mdai/core/startup-check.md", macro="mdai_bootstrap", cwd="/home/tholo/Scripts/lean-ctx")`
`mcp__markdownai__call_macro(file="mdai/core/startup-check.md", macro="detect_mai_hook_version", cwd="/home/tholo/Scripts/lean-ctx")`
`mcp__markdownai__call_macro(file="mdai/core/lean-context-audit.md", macro="lean_context_audit", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet pro Call: `found: true`, `warnings: []`.

`mcp__lean-ctx__ctx_session(action="finding", val="[t0.1-A-smoke4] mdai_bootstrap=PASS|FAIL detect_mai_hook_version=PASS|FAIL lean_context_audit=PASS|FAIL")`

- [ ] **Step 9: Variante-A Cleanup — security.json revert + Branch löschen**

security.json revert:
`mcp__lean-ctx__ctx_shell(command="cp ~/.markdownai/security.json.pre-audit ~/.markdownai/security.json", cwd="/home/tholo/Scripts/lean-ctx")`
`mcp__lean-ctx__ctx_shell(command="diff ~/.markdownai/security.json ~/.markdownai/security.json.pre-audit", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: leerer Output.

Branch verlassen + löschen:
`mcp__lean-ctx__ctx_shell(command="git checkout feat-mdai", cwd="/home/tholo/Scripts/lean-ctx")`
`mcp__lean-ctx__ctx_shell(command="git branch -D audit/no-mdai-root", cwd="/home/tholo/Scripts/lean-ctx")`

Library-Files Verifikation:
`mcp__lean-ctx__ctx_search(pattern="MDAI_LIBRARY_ROOT", path="mdai/", max_results=10)`

Erwartet: Treffer-Count identisch zu Pre-Flight Step 6.

- [ ] **Step 10: Variante-B Smoke 1 — `list_phases` mit env-arg**

`mcp__markdownai__list_phases(file="mdai/skills/mdai-brainstorm/body.mdai.md", cwd="/home/tholo/Scripts/lean-ctx", env={"MDAI_LIBRARY_ROOT": "/home/tholo/Scripts/lean-ctx/mdai"})`

Erwartet: identisch zu Step 5.

`mcp__lean-ctx__ctx_session(action="finding", val="[t0.1-B-smoke1] status=PASS|FAIL phases=[…]")`

- [ ] **Step 11: Variante-B Smoke 2 — `resolve_phase` mit env-arg**

`mcp__markdownai__resolve_phase(file="mdai/skills/mdai-brainstorm/body.mdai.md", phase="pre-context", cwd="/home/tholo/Scripts/lean-ctx", env={"MDAI_LIBRARY_ROOT": "/home/tholo/Scripts/lean-ctx/mdai"})`

Erwartet: identisch zu Step 6.

`mcp__lean-ctx__ctx_session(action="finding", val="[t0.1-B-smoke2] status=PASS|FAIL warnings_count=N")`

- [ ] **Step 12: Variante-B Smoke 3 — voller brainstorm-Run (Status-quo-Library)**

In Claude-Code-Session: `/superpowers:brainstorming dummy-feature-y als Testlauf für T0.1 Variante B`.

Hinweis: Wenn der Brainstorm-Skill den `env`-Arg an MCP-Calls nicht automatisch propagiert, ist das selbst
ein dokumentierter Befund für Variante B (Finding §7 „env-Vererbung-Stolperfalle") — in der Audit-Tabelle als
`env_propagation=manual` markieren.

**Nach dem Run:** Test-Spec löschen:
`mcp__lean-ctx__ctx_shell(command="rm -f docs/mdai/specs/2026-05-26-dummy-feature-y*", cwd="/home/tholo/Scripts/lean-ctx")`

`mcp__lean-ctx__ctx_session(action="finding", val="[t0.1-B-smoke3] status=PASS|FAIL env_propagation=auto|manual errors=[…]")`

- [ ] **Step 13: Variante-B Smoke 4 — `call_macro` × 3 mit env-arg**

`mcp__markdownai__call_macro(file="mdai/core/startup-check.md", macro="mdai_bootstrap", cwd="/home/tholo/Scripts/lean-ctx", env={"MDAI_LIBRARY_ROOT": "/home/tholo/Scripts/lean-ctx/mdai"})`
`mcp__markdownai__call_macro(file="mdai/core/startup-check.md", macro="detect_mai_hook_version", cwd="/home/tholo/Scripts/lean-ctx", env={"MDAI_LIBRARY_ROOT": "/home/tholo/Scripts/lean-ctx/mdai"})`
`mcp__markdownai__call_macro(file="mdai/core/lean-context-audit.md", macro="lean_context_audit", cwd="/home/tholo/Scripts/lean-ctx", env={"MDAI_LIBRARY_ROOT": "/home/tholo/Scripts/lean-ctx/mdai"})`

Erwartet pro Call: `found: true`, `warnings: []`.

`mcp__lean-ctx__ctx_session(action="finding", val="[t0.1-B-smoke4] mdai_bootstrap=PASS|FAIL detect_mai_hook_version=PASS|FAIL lean_context_audit=PASS|FAIL")`

- [ ] **Step 14: Audit-Doc schreiben**

`Write` auf `/home/tholo/Scripts/lean-ctx/docs/mdai/audits/2026-05-26-mdai-library-root-audit.md`:

```markdown
---
title: MDAI_LIBRARY_ROOT Audit — Variante A (eliminieren) vs. Variante B (Status quo)
date: 2026-05-26
status: ready-for-review
authors: claude
related_specs:
  - docs/mdai/specs/2026-05-26-mdai-v0.1.2-refit-v1.0-audit-design.mdai.md
related_plans:
  - docs/mdai/plans/2026-05-26-mdai-v0.1.2-part-a-adoption-audit.md
sourced_from:
  - ctx_session findings [t0.1-A-smoke1..4] [t0.1-B-smoke1..4]
---

# MDAI_LIBRARY_ROOT Audit — Variante A vs. Variante B

## Methodik

Zwei identische `mdai-brainstorm`-E2E-Runs, je 4 Smokes:
- Smoke 1: `mcp__markdownai__list_phases` auf `body.mdai.md`
- Smoke 2: `mcp__markdownai__resolve_phase` mit Phase `pre-context`
- Smoke 3: voller Brainstorm-Lauf mit Test-Topic, Spec-Erzeugung + spec-self-review
- Smoke 4: `mcp__markdownai__call_macro` für 3 Bootstrap-Macros (mdai_bootstrap,
  detect_mai_hook_version, lean_context_audit)

Variante A: `source_root: "/home/tholo/Scripts/lean-ctx"` (absolut) + relative `@include mdai/...` via
Sed-Substitution im Wegwerf-Branch.

Variante B: Status quo — `${MDAI_LIBRARY_ROOT}`-Globs + env-arg `{"MDAI_LIBRARY_ROOT": "..."}` in MCP-Calls.

## Vergleichstabelle

| Kriterium                            | Variante A (kein env)      | Variante B (Status quo)      |
| Setup-Cost (one-time)                | security.json edit         | env-var + zshrc-Export       |
| Setup-Cost (neuer Dev / CI)          | nur security.json          | env-var + shell-source       |
| Portabilität Library → andere Repos  | NEIN                       | JA                           |
| Subagent-env-Vererbung (Finding §7)  | nicht betroffen            | weiterhin Stolperfalle       |
| Smoke 1 (list_phases)                | <PASS/FAIL>                | <PASS/FAIL>                  |
| Smoke 2 (resolve_phase)              | <PASS/FAIL, warnings=N>    | <PASS/FAIL, warnings=N>      |
| Smoke 3 (full brainstorm)            | <PASS/FAIL, errors=[…]>    | <PASS/FAIL, errors=[…]>      |
| Smoke 4 (call_macro × 3)             | <mdai_bs/detect/lean_ctx>  | <mdai_bs/detect/lean_ctx>    |
| Render-Output identisch zu Baseline? | <yes/no + diff-summary>    | <baseline>                   |

## Empfehlung des Audits

<1–2 Sätze, datengetrieben aus den 4×2 Smokes. Lass die User-Decision offen — der Audit gibt eine Empfehlung,
der User entscheidet in T0.3.>

## Wegwerf-Branch-Cleanup-Status

- Branch `audit/no-mdai-root`: gelöscht (T1 Step 9).
- `~/.markdownai/security.json`: revertiert auf pre-audit-State (T1 Step 9).
- Library-Files: unverändert nach Branch-Delete (T1 Step 9 verify, Treffer-Count = Pre-Flight Step 6 Count).
```

Werte aus ctx_session-Tupeln einsetzen.

- [ ] **Step 15: Reformat + Commit**

`mcp__jetbrains__reformat_file(path="docs/mdai/audits/2026-05-26-mdai-library-root-audit.md", projectPath="/home/tholo/Scripts/lean-ctx")`
`mcp__lean-ctx__ctx_shell(command="git add docs/mdai/audits/2026-05-26-mdai-library-root-audit.md", cwd="/home/tholo/Scripts/lean-ctx")`

Commit-message via heredoc-File (ctx_shell blockt `$(…)`):

`Write` auf `/tmp/t01-commit-msg.txt`:

```
docs(mdai/audits): MDAI_LIBRARY_ROOT audit — Variante A vs. B (T0.1)

Two full mdai-brainstorm E2E runs (4 smokes each) compared:
- Variant A: source_root absolute + relative @include mdai/...
- Variant B: status quo with ${MDAI_LIBRARY_ROOT} env-var

Library unchanged after audit (throwaway branch deleted, security.json reverted).
ctx_session tuples [t0.1-A-smoke1..4] and [t0.1-B-smoke1..4] persisted for T0.3
(consolidation) and Part-C Task 7 (smoke-status update for §8.1/§8.2/§8.3).
```

`mcp__lean-ctx__ctx_shell(command="git commit -F /tmp/t01-commit-msg.txt", cwd="/home/tholo/Scripts/lean-ctx")`
`mcp__lean-ctx__ctx_shell(command="rm /tmp/t01-commit-msg.txt", cwd="/home/tholo/Scripts/lean-ctx")`

---

### Task 2: T0.2 — Findings-v2 Re-Validation (12 Findings systematisch)

**Files:** keine permanenten Modifikationen — nur Source-Reads + Minimal-Repros + ctx_session-Tupel.

- Read (temporär): Engine-Source-Files unter `markdownai/packages/{parser,engine}/src/`
- Create (Wegwerf): `/tmp/t02-repro/*.md` — gelöscht in Step 4

**Ziel:** Pro Finding aus `docs/mdai/findings/2026-05-25-mdai-v0.1.1-engine-constraints.md` ein Status-Tupel
sammeln. Part-B Task 4 konsumiert die 12 Tupel und schreibt den Findings-v2-Doc.

**Subagent-Modell:** zwei parallele `Agent`-Dispatches via `subagent_type: general-purpose`, model `sonnet`.
Bei Fehlschlag auf §2/§3/§5 (Macro-Edge-Cases): Re-Dispatch dieser einzelnen Findings mit `model: opus`.

- [ ] **Step 1: Subagent #1 dispatchen (Findings §1–§6, parallel zu Step 2)**

`Agent` Tool-Call:

- `subagent_type`: `general-purpose`
- `model`: `sonnet`
- `description`: `Re-validate findings 1-6 vs v1.0`
- `prompt`:

```
Re-validate findings §1–§6 from
docs/mdai/findings/2026-05-25-mdai-v0.1.1-engine-constraints.md against
markdownai v1.0.0 (source in markdownai/packages/).

For EACH finding, produce a tuple via:
mcp__lean-ctx__ctx_session(
  action="finding",
  val="[t0.2-§N] status=<X> evidence=<Y> recommendation=<Z> migration=<M>"
)

Where:
- status: still-valid | obsolete-in-v1.0 | workaround-deprecated | still-valid-with-new-mechanism
- evidence: 1-line ref (engine-source-path:line OR smoke-result key OR test-result)
- recommendation: 1–2 sentences for v0.1.3 adoption plan
- migration: yes | no | optional

Per-finding methods:

§1 Closer-Syntax (@end / @endforeach / @endswitch / @endif):
   Read markdownai/packages/parser/src/directives/ via
   mcp__lean-ctx__ctx_read(path=..., mode="signatures") to confirm closer tokens still required.

§2 @set+@foreach+{{var}} in @define broken at call_macro:
   Minimal repro — write /tmp/t02-repro/s2.md:
   ---
   ---
   @markdownai v1.0
   @define test_macro
     @set items = ["a","b"]
     @foreach item in items
       Item: {{ item }}
     @endforeach
   @end
   Then call mcp__markdownai__call_macro(file="/tmp/t02-repro/s2.md", macro="test_macro", cwd="/tmp/t02-repro").
   Status:
   - obsolete-in-v1.0 if output contains both "Item: a" and "Item: b"
   - still-valid if output is empty or contains "{{ item }}" literal

§3 {{var}} in file.containsLine inside @define not interpolated:
   Minimal repro /tmp/t02-repro/s3.md:
   ---
   ---
   @markdownai v1.0
   @define test_macro
     @set path = "/tmp/t02-repro/s3.md"
     @if file.containsLine "{{ path }}" "test_macro"
       FOUND
     @endif
   @end
   Call via call_macro. Status PASS if output contains "FOUND".

§4 file.containsLine on paths outside project CWD blocked:
   Read current ~/.markdownai/security.json. Add allowed_data_paths: ["/home/**"] (temp edit via ctx_edit).
   Write /tmp/t02-repro/s4.md with @if file.containsLine "~/.markdownai/security.json" "filesystem" then YES.
   Run resolve_phase or call_macro. Status:
   - workaround-deprecated if YES appears (config-only workaround in v1.0)
   - still-valid if still blocked
   REVERT ~/.markdownai/security.json via cp from ~/.markdownai/security.json.pre-audit before next step.

§5 @set var = "{{@date}}" + @if file.exists "{{var}}" at @define top-level (Parser-Bug):
   Minimal repro /tmp/t02-repro/s5.md:
   ---
   ---
   @markdownai v1.0
   @define test_macro
     @set today = "{{ @date format='YYYY-MM-DD' }}"
     @if file.exists "{{ today }}-foo.txt"
       EXISTS
     @endif
   @end
   Call via call_macro. Status PASS (obsolete-in-v1.0) if no ParseError. Status FAIL (still-valid) if
   ParseError or false negative on `file.exists`.

§6 @render-template from/to/force Syntax:
   Engine-Source-Verify: mcp__lean-ctx__ctx_read(path="markdownai/packages/parser/src/directives/render-template.ts", mode="signatures").
   Confirm: block-form @render-template ... @end with from=, to=, optional force. NO output= argument.
   Status = still-valid (it's documentation, not a bug).

For each finding, after running the test:
1. Persist the tuple via ctx_session.
2. Print "[t0.2-§N] persisted" to stdout so the parent can count.

Final action: list all 6 persisted ctx_session keys.

Cleanup at end: mcp__lean-ctx__ctx_shell(command="rm -rf /tmp/t02-repro/", cwd="/home/tholo/Scripts/lean-ctx")
ONLY after all 6 tuples are persisted (otherwise leave files for debugging).
```

Erwartet: Subagent gibt zurück: 6 persisted tuples, /tmp/t02-repro/ gecleaned.

- [ ] **Step 2: Subagent #2 dispatchen (Findings §7–§12, parallel zu Step 1)**

`Agent` Tool-Call:

- `subagent_type`: `general-purpose`
- `model`: `sonnet`
- `description`: `Re-validate findings 7-12 vs v1.0`
- `prompt`:

```
Re-validate findings §7–§12 from
docs/mdai/findings/2026-05-25-mdai-v0.1.1-engine-constraints.md against
markdownai v1.0.0.

Same tuple format as Subagent #1 (see ctx_session action="finding" val="[t0.2-§N] status=… evidence=… recommendation=… migration=…").

Per-finding methods:

§7 ${MDAI_LIBRARY_ROOT} not expanded at standalone resolve_phase:
   ALREADY COVERED by T0.1. Read ctx_session keys [t0.1-A-smoke2] and [t0.1-B-smoke2].
   - If Variant B Smoke 2 passed only with explicit env-arg AND Variant A Smoke 2 also passed:
     status = still-valid (workaround = always pass env={...}).
   - If Variant A's source_root config eliminated the issue: still-valid-with-new-mechanism.
   evidence: ref [t0.1-A-smoke2] and [t0.1-B-smoke2].

§8 Frontmatter leaks as text in mode:include standalone read_file:
   mcp__lean-ctx__ctx_search(pattern="frontmatter|mode.*include", path="markdownai/packages/engine/src/")
   to find the include-directive implementation. Read the relevant file via ctx_read mode="signatures".
   Confirm whether v1.0 strips frontmatter for mode:include or still leaks. If unclear from source:
   minimal repro /tmp/t02-repro/s8.md with --- frontmatter --- then mcp__markdownai__read_file.
   Status:
   - obsolete-in-v1.0 if v1.0 strips frontmatter
   - still-valid if v1.0 still leaks

§9 filesystem.write_enabled: true required for @mkdir / @render-template:
   v1.0 has write_root + allowed_write_paths (see markdownai/README.md security section).
   mcp__lean-ctx__ctx_read(path="markdownai/packages/engine/src/security/config.ts", mode="signatures").
   Check whether write_enabled is still honored, ignored, or replaced by write_root.
   Status:
   - workaround-deprecated if v1.0 uses write_root instead (write_enabled gone or ignored)
   - still-valid-with-new-mechanism if v1.0 keeps write_enabled AND adds write_root

§10 npm config prefix + PATH export (User-Env side-effect):
   No re-test needed. v1.0 doesn't change this (User-PATH issue).
   status = still-valid. evidence = "user-env side-effect, no engine code path".
   recommendation = "doc-only update in mdai/core/hard-rules.md".
   migration = no.

§11 Modus A (Prosa-Pointer call_macro) vs Modus B (@call):
   Design decision — re-affirm. mcp__lean-ctx__ctx_read on
   mdai/skills/mdai-brainstorm/body.mdai.md and mdai/skills/mdai-brainstorm/spec-self-review.md
   (mode="signatures" or "lines:1-30") to confirm the current mode-pattern.
   status = still-valid (convention, not engine constraint).
   migration = no.

§12 Bootstrap-Macros resolved via global scope, no explicit @import startup-check.md:
   mcp__lean-ctx__ctx_search(pattern="global|scope|@import", path="markdownai/packages/engine/src/")
   for macro-scope resolution. Read the relevant file.
   Confirm whether source_root config populates a global macro scope at session-start.
   status = still-valid (by design) or still-valid-with-new-mechanism (if config-driven).
   evidence: engine-source ref.

For each finding, persist via ctx_session, print "[t0.2-§N] persisted".

Final action: list all 6 persisted keys.

Cleanup: rm -rf /tmp/t02-repro/ via ctx_shell after all 6 tuples done.
```

Erwartet: 6 persisted tuples, /tmp/t02-repro/ gecleaned.

- [ ] **Step 3: Completeness-Check — 12 Tupel persistiert**

In der Parent-Session prüfen ob alle 12 Tupel existieren. Via `ctx_session` recall pro Key:

`mcp__lean-ctx__ctx_session(action="recall", val="[t0.2-§1]")`
`mcp__lean-ctx__ctx_session(action="recall", val="[t0.2-§2]")`
... (für alle 12)

Falls < 12 Tupel: identifiziere fehlende Finding-Nummern, dispatche einen dritten Subagent für nur diese
Findings (gleiche Methodik aus Step 1 oder Step 2 Prompt). Bei §2/§3/§5 darf `model: opus` verwendet werden.

- [ ] **Step 4: /tmp/t02-repro/ Cleanup-Verifikation**

`mcp__lean-ctx__ctx_shell(command="ls /tmp/t02-repro/ 2>/dev/null || echo 'gone'", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: `gone` (oder leerer Output mit exit code != 0).

Falls Reste:
`mcp__lean-ctx__ctx_shell(command="rm -rf /tmp/t02-repro/", cwd="/home/tholo/Scripts/lean-ctx")`

Kein Commit aus T0.2 — Tupel sind ctx_session-only, Part-B T4 konsumiert sie für Findings-v2-Doc.

---

### Task 3: T0.3 — Audit-Konsolidierung + User-Decision-Gate

**Files:**

- Create: `docs/mdai/audits/2026-05-26-mdai-v1.0-adoption-audit.md`

**Ziel:** Konsolidierter Audit-Doc, der T0.1 + T0.2 zusammenfasst. Endet mit User-Decision-Gate.

- [ ] **Step 1: Konsolidierungs-Doc schreiben (Zustand `pending-user-decision`)**

`Write` auf `/home/tholo/Scripts/lean-ctx/docs/mdai/audits/2026-05-26-mdai-v1.0-adoption-audit.md`:

```markdown
---
title: markdownai v1.0.0 Adoption-Audit — konsolidiert (T0.3)
date: 2026-05-26
status: pending-user-decision
authors: claude
related_specs:
  - docs/mdai/specs/2026-05-26-mdai-v0.1.2-refit-v1.0-audit-design.mdai.md
related_audits:
  - docs/mdai/audits/2026-05-26-mdai-library-root-audit.md
related_plans:
  - docs/mdai/plans/2026-05-26-mdai-v0.1.2-part-a-adoption-audit.md
sourced_from:
  - ctx_session [t0.1-A-smoke1..4] [t0.1-B-smoke1..4] [t0.2-§1..§12]
library_root_decision: pending
v0.1.3_action_items: pending
---

# markdownai v1.0.0 Adoption-Audit — konsolidiert

## Executive Summary

- **MDAI_LIBRARY_ROOT:** <1 Satz aus T0.1-Empfehlung, mit Pointer zu Sektion 1>
- **Findings-Update-Highlights:** <2-3 Findings, die in v1.0 obsolet/deprecated sind, mit Pointer zu Sektion 2>
- **Action-Items für v0.1.3:** <Anzahl Migrations-Items, mit Pointer zu Sektion 3>

## Sektion 1: MDAI_LIBRARY_ROOT-Vergleich

<Inline-Kopie der Vergleichstabelle aus docs/mdai/audits/2026-05-26-mdai-library-root-audit.md
ODER nur Pointer mit kurzer Zusammenfassung. Entscheidung: Pointer + 3-Zeilen-Summary, weil der
Library-Root-Audit-Doc bereits committet ist und die ganze Tabelle dort steht.>

**Empfehlung des Audits:** <1-2 Sätze, datengetrieben aus den 4×2 Smokes>.

## Sektion 2: Findings-12er-Tabelle

| §  | Topic                                       | v1.0-Status                   | Migration in v0.1.3? |
| 1  | Closer-Syntax                               | <status aus [t0.2-§1]>        | <yes/no/optional>    |
| 2  | @set+@foreach+{{var}} in @define            | <status aus [t0.2-§2]>        | <yes/no/optional>    |
| 3  | {{var}} in file.containsLine inside @define | <status aus [t0.2-§3]>        | <yes/no/optional>    |
| 4  | file.containsLine Cross-CWD                 | <status aus [t0.2-§4]>        | <yes/no/optional>    |
| 5  | @set var="{{@date}}" Parser-Bug             | <status aus [t0.2-§5]>        | <yes/no/optional>    |
| 6  | @render-template from/to/force              | <status aus [t0.2-§6]>        | <yes/no/optional>    |
| 7  | ${MDAI_LIBRARY_ROOT} nicht expandiert       | <status aus [t0.2-§7]>        | <yes/no/optional>    |
| 8  | Frontmatter leakt in mode:include           | <status aus [t0.2-§8]>        | <yes/no/optional>    |
| 9  | write_enabled Pflicht                       | <status aus [t0.2-§9]>        | <yes/no/optional>    |
| 10 | npm-prefix PATH                             | still-valid                   | no                   |
| 11 | Modus A vs B (Prosa vs @call)               | still-valid                   | no                   |
| 12 | Bootstrap-Macros via global scope           | <status aus [t0.2-§12]>       | <yes/no/optional>    |

Werte aus ctx_session-Tupeln einsetzen. Pro Zeile: 1-Satz-Detail wenn Status != still-valid.

## Sektion 3: Action-Items für v0.1.3-Plan (sortiert nach Aufwand)

<Bullet-Liste, sortiert nach Aufwand (klein → groß). Pro Item: §N + 1 Satz was zu tun ist + erwartete Files
die in v0.1.3 modifiziert werden müssen.>

Format-Beispiel:
- [Klein] §10 (PATH): nur Doku-Update in mdai/core/hard-rules.md, keine Code-Edits.
- [Mittel] §4 (file.containsLine Cross-CWD): allowed_data_paths in ~/.markdownai/security.json
  erweitern, Library-Files unverändert.
- [Groß] §1 + Variante-A-Migration (falls A gewählt): security.json source_root + Sed-Migration aller
  mdai/**/*.md + zshrc-Export entfernen.
```

Werte aus ctx_session-Tupeln einsetzen. Lass `library_root_decision` und `v0.1.3_action_items` auf `pending`
— wird in Step 3 nach User-Antwort gesetzt.

- [ ] **Step 2: Audit-Doc dem User präsentieren — Decision-Gate**

Präsentations-Text an User:

> **„Phase 0 Adoption-Audit abgeschlossen. Konsolidiert in
> `docs/mdai/audits/2026-05-26-mdai-v1.0-adoption-audit.md` (Status: `pending-user-decision`).**
>
> Pro Entscheidung, die ich brauche bevor commit + Part-B-Start:
>
> 1. **MDAI_LIBRARY_ROOT-Strategie für v0.1.3** — Variante A (eliminieren via `source_root` absolut +
     > relative `@include mdai/...`) oder Variante B (Status quo mit env-var)?
>
> 2. **Findings für v0.1.3-Migrations-Plan** — welche der 12 Findings landen als Migrations-Action im
     > v0.1.3-Adoption-Plan? (Default: alle mit `migration: yes` aus Sektion 2.)
>
> WARTEN auf User-Antwort. Bitte beide Decisions in einer Antwort geben."

WARTEN. Kein Commit bevor Antwort vorliegt.

- [ ] **Step 3: Decisions ins Frontmatter persistieren**

Pro User-Antwort `mcp__lean-ctx__ctx_edit` auf den Audit-Doc:

- `library_root_decision: pending` → `library_root_decision: A` (oder `B`).
- `v0.1.3_action_items: pending` → `v0.1.3_action_items: [§N, §M, ...]` (Liste der vom User gewählten
  Finding-Nummern, kommagetrennt).
- `status: pending-user-decision` → `status: decided`.

Falls User auch eine Begründung mitgegeben hat: in den Body unter „Executive Summary" einen Absatz
„User-Decision-Rationale (2026-05-26):" einfügen (1–3 Sätze).

Verifikation:
`mcp__lean-ctx__ctx_search(pattern="library_root_decision: [AB]$", path="docs/mdai/audits/2026-05-26-mdai-v1.0-adoption-audit.md", max_results=2)`

Erwartet: ≥ 1 match.

- [ ] **Step 4: Reformat + Commit**

`mcp__jetbrains__reformat_file(path="docs/mdai/audits/2026-05-26-mdai-v1.0-adoption-audit.md", projectPath="/home/tholo/Scripts/lean-ctx")`
`mcp__lean-ctx__ctx_shell(command="git add docs/mdai/audits/2026-05-26-mdai-v1.0-adoption-audit.md", cwd="/home/tholo/Scripts/lean-ctx")`

`Write` auf `/tmp/t03-commit-msg.txt`:

```
docs(mdai/audits): v1.0.0 adoption-audit consolidated (T0.3) — decisions persisted

User decisions (frontmatter):
- library_root_decision: <A|B>
- v0.1.3_action_items: [§N, §M, …]

Consolidates T0.1 (library-root) + T0.2 (12 findings re-validation against
markdownai v1.0.0) outputs. Part-B (Findings-v2-Doc + Dedup-Investigation)
starts after this commit.
```

`mcp__lean-ctx__ctx_shell(command="git commit -F /tmp/t03-commit-msg.txt", cwd="/home/tholo/Scripts/lean-ctx")`
`mcp__lean-ctx__ctx_shell(command="rm /tmp/t03-commit-msg.txt", cwd="/home/tholo/Scripts/lean-ctx")`

---

## Part-A End-Gate (User-Approval vor Part-B-Start)

### Task 4: Status-Summary an User + Part-B-Handoff

- [ ] **Step 1: Part-A Status-Summary**

Präsentations-Text:

> **„Part-A (Phase 0 — Adoption-Audit) komplett:**
>
> - **T0.1 — Library-Root-Audit:** commit `<sha aus git log>` —
    > `docs/mdai/audits/2026-05-26-mdai-library-root-audit.md` mit Variante A vs. B Vergleichstabelle.
> - **T0.2 — Findings-Re-Validation:** 12 ctx_session-Tupel persistiert (`[t0.2-§1]..[t0.2-§12]`),
    > /tmp/t02-repro/ gecleaned.
> - **T0.3 — Audit-Konsolidierung:** commit `<sha>` —
    > `docs/mdai/audits/2026-05-26-mdai-v1.0-adoption-audit.md` mit persistierten Decisions:
    >
- `library_root_decision: <A|B>`
>   - `v0.1.3_action_items: [§N, …]`
>
> **Carry-Forwards für Part-B:**
> - 12 ctx_session-Tupel `[t0.2-§1..§12]` werden in Part-B Task 4 (Findings-v2-Doc) konsumiert.
> - 8 ctx_session-Tupel `[t0.1-A-smoke*]` + `[t0.1-B-smoke*]` werden in Part-C Task 7 (§8.1/§8.2/§8.3
    > Smoke-Status-Update) konsumiert.
> - `library_root_decision` aus Audit-Doc Frontmatter steuert Part-B Task 5 Step 4/5 (Dedup-Prototype
    > `@include`-Prefix: `mdai/` für A, `${MDAI_LIBRARY_ROOT}/` für B).
>
> **Working tree clean?** `git status` auf `feat-mdai` muss sauber sein, Wegwerf-Branch
> `audit/no-mdai-root` gelöscht, `~/.markdownai/security.json` revertiert.
>
> **Approval-Frage:** soll ich mit Part-B starten?"

- [ ] **Step 2: User-Approval-Gate**

WARTEN auf explizite User-Approval. Bei Approval: Wechsel zu Plan-File
`docs/mdai/plans/2026-05-26-mdai-v0.1.2-part-b-findings-v2-and-dedup.md`.

Bei Änderungs-Request: zurück zu betroffener Task in Part-A, anpassen, erneut User-Decision-Gate.

---

## Part-A Final-Verification

### Task 5: Part-A End-Gate

- [ ] **Step 1: T0.1 Output exists**

`mcp__lean-ctx__ctx_search(pattern="Variante A.*Variante B|Variante B.*Variante A", path="docs/mdai/audits/2026-05-26-mdai-library-root-audit.md", max_results=5)`

Erwartet: ≥ 1 match.

- [ ] **Step 2: T0.2 Tupel-Completeness (12 Keys)**

Recall pro Key `[t0.2-§1]` bis `[t0.2-§12]` via `mcp__lean-ctx__ctx_session(action="recall", val="…")`.

Erwartet: 12 vorhanden, jeder mit Format `status=… evidence=… recommendation=… migration=…`.

- [ ] **Step 3: T0.3 Decisions persistiert**

`mcp__lean-ctx__ctx_search(pattern="library_root_decision: [AB]$", path="docs/mdai/audits/2026-05-26-mdai-v1.0-adoption-audit.md", max_results=2)`

Erwartet: ≥ 1 match. Wert `A` oder `B`, NICHT `pending`.

`mcp__lean-ctx__ctx_search(pattern="v0\\.1\\.3_action_items: \\[", path="docs/mdai/audits/2026-05-26-mdai-v1.0-adoption-audit.md", max_results=2)`

Erwartet: ≥ 1 match. Wert ist Liste, NICHT `pending`.

- [ ] **Step 4: Working-Tree Cleanup**

`mcp__lean-ctx__ctx_shell(command="git branch --list audit/no-mdai-root", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: leerer Output.

`mcp__lean-ctx__ctx_shell(command="diff ~/.markdownai/security.json ~/.markdownai/security.json.pre-audit", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: leerer Output (Dateien identisch).

`mcp__lean-ctx__ctx_search(pattern="MDAI_LIBRARY_ROOT", path="mdai/", max_results=10)`

Erwartet: Treffer-Count identisch zu Pre-Flight Step 6 (Library unverändert).

- [ ] **Step 5: Git-Log Sanity**

`mcp__lean-ctx__ctx_shell(command="git log --oneline efcc4b1a..HEAD", cwd="/home/tholo/Scripts/lean-ctx")`

Erwartet: 2 neue Commits seit Spec:

1. `docs(mdai/audits): MDAI_LIBRARY_ROOT audit — Variante A vs. B (T0.1)` (von T1 Step 15)
2. `docs(mdai/audits): v1.0.0 adoption-audit consolidated (T0.3) — decisions persisted` (von T3 Step 4)

Falls < 2: identifiziere fehlende Task, abschließen.

---

## Notes

- **Bash:** keine `&&`-Ketten. Jeden Befehl einzeln (CLAUDE.md).
- **ctx_shell `$(…)`-Block:** Commit-Messages immer via heredoc-File-Pattern: `Write` auf `/tmp/tNN-commit-msg.txt`
  → `git commit -F /tmp/...` → `rm /tmp/...`.
- **Pre-commit:** `mcp__jetbrains__reformat_file` vor jedem `git add` (CLAUDE.md).
- **git add:** immer Dateien einzeln nennen, NIE `-A` / `.`.
- **No worktrees:** direkt auf `feat-mdai`. Ausnahme: T1 verwendet Wegwerf-Branch `audit/no-mdai-root`
  (kein worktree, normaler git-branch), wird am Ende gelöscht.
- **Sprache:** Plan-Body Deutsch (Umlaute ä/ö/ü/ß), Code/Snippets/commit-messages Englisch.
- **Subagent-Modell:** `sonnet` default. T2 darf `opus` für §2/§3/§5 wenn Sonnet scheitert.
- **Tool-Allowlist pro Subagent:** Serena + lean-ctx (`ctx_*`) + JetBrains-MCP + `mcp__markdownai__*`.
  Bash via `ctx_shell` für `git`, `cp`, `rm`, `mv`, `sed`, `find`, `diff`, `ls`.
- **Scope:** KEINE permanenten Code-Edits an Library-Files in Part-A. Sed-Edits in T1 sind isoliert im
  Wegwerf-Branch und werden durch Branch-Delete reverted.
- **Deferred-tool reflex:** bei `InputValidationError` / „not loaded" → IMMER zuerst
  `ToolSearch(query="select:<tool>")`, NIE direkt Bash-Workaround (CLAUDE.md Hard Rule).
