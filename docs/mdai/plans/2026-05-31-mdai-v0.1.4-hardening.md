# mdai-Library v0.1.4 — Hardening — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Drei v0.1.3-Folgearbeiten abschließen — Engine-Fix-Verifikation + Dependency-Doku (TG1), `@set`-Pipe-Fix in `body.mdai.md` (TG2), Directive-Indentation-Normalisierung (TG3) — reines Hardening, keine funktionale Macro-Änderung außer dem §2-B-Fix.

**Architecture:** Markdownai-Macro-Library (`.md`/`.mdai.md`-Quellen unter `mdai/`). „Tests" sind hier `npx mai validate` (Statik) + MCP-Smokes (`call_macro` / `resolve_phase` / CLI `render`) als Laufzeit-Wahrheit. Alle Smokes/Gates laufen gegen den **aktuellen `feat-mdai`-Dist** (`origin/main@aac0825` + lokale Fixes `f16b4c2`+`ede9793`), **nicht** gegen 1.3.0. Single-Sourcing: kanonische Doku in findings-v3 Anhang A, überall sonst nur Einzeiler-Pointer.

**Tech Stack:** markdownai 1.3.0+ Engine (CLI `mai`, MCP-Server `markdownai`), lean-ctx MCP, git.

**Spec:** `docs/mdai/specs/2026-05-29-mdai-v0.1.4-hardening-design.mdai.md`

---

## Vorbereitung (einmalig, vor allen Tasks)

- [ ] **Setup-Step: Dist bauen + Baseline bestätigen**

Die Laufzeit-Wahrheit ist der frisch gebaute `feat-mdai`-Dist. Sicherstellen, dass der laufende Dist die zwei lokalen Fixes enthält.

Run (aus Repo-Root `/home/tholo/Scripts/lean-ctx`):
```bash
git -C markdownai log --oneline -3
npm --prefix markdownai run build --workspaces
```
Expected: Log-Tip ist `419581a Merge … origin/main into feat-mdai` (oder neuer); Build endet ohne Fehler.

---

## File Structure

Welche Dateien dieser Plan anfasst und wofür:

| Datei | Verantwortung | Task |
|---|---|---|
| `docs/mdai/findings/2026-05-29-mdai-engine-constraints-v3-vs-1.3.0.md` | **Kanonische** Engine-Fix-Doku — Anhang A bekommt zwei Repro-Smoke-Snippets | TG1 |
| `mdai/core/mcp-markdownai.md` | Engine-Notes — **ein** Einzeiler-Pointer auf Anhang A | TG1 |
| `mdai/changelog.md` | neuer `v0.1.4`-Abschnitt mit Pointer; §2-B-Limitation als „resolved" markieren | TG1 + TG2 |
| `mdai/skills/mdai-brainstorm/body.mdai.md` | §2-B `@set`-Pipe-Fix (write-outputs, ~L185); De-Indent `@phase-end` (pre-context, L53) | TG2 + TG3 |
| `mdai/skills/mdai-brainstorm/write-spec.md` | De-Indent der genesteten `@`-Directives auf Spalte 0 | TG3 |

`mdai/core/file-utils.md` ist **ausgenommen** (Maintainer-gepflegt) — nicht anfassen.

---

## Task 1 (TG1): Engine-Fix-Verifikation & Dependency-Doku

**Files:**
- Verify only: `markdownai/` (git), laufender Dist
- Modify: `docs/mdai/findings/2026-05-29-mdai-engine-constraints-v3-vs-1.3.0.md` (Anhang A, nach Zeile 178)
- Modify: `mdai/core/mcp-markdownai.md` (Engine-Notes-Abschnitt)
- Modify: `mdai/changelog.md` (neuer v0.1.4-Kopf)

- [ ] **Step 1: Verifizieren, dass beide Fixes auf `feat-mdai` und NICHT in `origin/main` sind**

Run:
```bash
git -C markdownai branch --contains f16b4c2
git -C markdownai branch --contains ede9793
git -C markdownai merge-base --is-ancestor f16b4c2 origin/main; echo "f16b4c2 in origin/main? exit=$?"
git -C markdownai merge-base --is-ancestor ede9793 origin/main; echo "ede9793 in origin/main? exit=$?"
```
Expected: beide `branch --contains` zeigen `* feat-mdai`; beide `merge-base`-Checks geben `exit=1` (NICHT in origin/main). → Dependency-Annahme der Spec bestätigt.

- [ ] **Step 2: Smoke A (`ede9793` — `@foreach` Objekt-Dot-Access)**

Schreibe die Datei `/tmp/mdai-smoke-a.md` (via Write-Tool) mit exakt diesem Inhalt:
```
@markdownai v1.0
@set xs = {{ [{"name":"a"}] }} /
@foreach x in {{ xs }}
{{ x.name }}
@foreach-end
```
Run (aus Repo-Root):
```bash
npx --prefix markdownai mai render /tmp/mdai-smoke-a.md
```
Expected: Ausgabe enthält die Zeile `a` (NICHT leer). Bei leerer Ausgabe ist der Dist veraltet → Setup-Step (Build) wiederholen.

- [ ] **Step 3: Smoke B (`f16b4c2` — Named-Arg in `@if`-Condition)**

Schreibe `/tmp/mdai-smoke-b.md` (via Write-Tool) mit exakt diesem Inhalt:
```
@markdownai v1.0
@define chk(p)
@if file.exists({{ p }})
EXISTS
@else
MISSING
@if-end
@define-end
@call chk(p="mdai/changelog.md") /
```
Run (aus Repo-Root — `mdai/changelog.md` existiert dort):
```bash
npx --prefix markdownai mai render /tmp/mdai-smoke-b.md
```
Expected: Ausgabe enthält `EXISTS` (NICHT `MISSING`). `MISSING` würde bedeuten, der Named-Arg `{{ p }}` wird in der `@if`-Condition nicht aufgelöst → Fix fehlt im Dist.

- [ ] **Step 4: Repro-Smokes in findings-v3 Anhang A ergänzen (Single Source)**

Anhang A endet aktuell auf Zeile 178 (Punkt 2 zu `ede9793`), gefolgt von der `---`-Trennlinie auf Zeile 180. Füge **vor** der `---`-Trennlinie (also nach Zeile 178) folgenden Block ein.

Tool: `mcp__lean-ctx__ctx_edit`
old_string (eindeutig, Ende von Punkt 2 + Trennlinie):
```
erhalten + an `ctx.data` binden (Dot-Access `{{ x.field }}`). +1 vitest-File (3 Tests). Ermöglicht §2-Objektlisten.

---
```
new_string:
```
erhalten + an `ctx.data` binden (Dot-Access `{{ x.field }}`). +1 vitest-File (3 Tests). Ermöglicht §2-Objektlisten.

### Repro-Smokes (Dist-Verifikation)

Aus Repo-Root, gegen den laufenden `feat-mdai`-Dist (nach `npm --prefix markdownai run build --workspaces`):

- **Smoke A (`ede9793`):** Datei mit `@set xs = {{ [{"name":"a"}] }} /` + `@foreach x in {{ xs }}` / `{{ x.name }}` / `@foreach-end`, dann `npx --prefix markdownai mai render <datei>` → rendert `a` (nicht leer).
- **Smoke B (`f16b4c2`):** `@define chk(p)` mit `@if file.exists({{ p }})` (`EXISTS`/`MISSING`), `@call chk(p="mdai/changelog.md") /`, dann `npx --prefix markdownai mai render <datei>` → rendert `EXISTS` (nicht `MISSING`).

Beide grün ⇒ Dist enthält f16b4c2+ede9793.

---
```

- [ ] **Step 5: Einzeiler-Pointer in `core/mcp-markdownai.md` ergänzen**

Die Engine-Notes nennen aktuell die Auflösungs-Eigenheiten, aber nicht die Dist-Abhängigkeit. Ergänze einen Einzeiler-Pointer am Ende der Überschrift-Zeile des Notes-Abschnitts.

Tool: `mcp__lean-ctx__ctx_edit`
old_string:
```
## Engine resolution notes (markdownai 1.3.0)

Verified against the live 1.3.0 engine. They matter for every agent/session authoring or running mdai packs.
```
new_string:
```
## Engine resolution notes (markdownai 1.3.0)

Verified against the live 1.3.0 engine. They matter for every agent/session authoring or running mdai packs.

> **Dist-Abhängigkeit:** Library benötigt den Dist aus Branch `feat-mdai` (= `origin/main@aac0825` + 2 lokale Fixes `f16b4c2`+`ede9793`). Nach Pull `npm run build --workspaces`; verifizieren via findings-v3 Anhang A (Repro-Smokes). Fixes sind bewusst nicht gepusht.
```

- [ ] **Step 6: v0.1.4-Changelog-Abschnitt anlegen (mit Pointer)**

Füge oben (vor dem `## v0.1.3`-Abschnitt, nach der Titelzeile) einen neuen v0.1.4-Abschnitt ein.

Tool: `mcp__lean-ctx__ctx_edit`
old_string:
```
# mdai-macro-library — Changelog

## v0.1.3 — 2026-05-29 — markdownai-v2 adoption & library fix (engine 1.3.0)
```
new_string:
```
# mdai-macro-library — Changelog

## v0.1.4 — 2026-05-31 — hardening (v0.1.3 follow-ups)

- **TG1 — Engine-Fix-Verifikation & Dependency-Doku:** zwei lokale Engine-Fixes (`f16b4c2`, `ede9793`) leben weiterhin nur auf `feat-mdai` (nicht in `origin/main`). Repro-Smokes ergänzt in findings-v3 Anhang A; Dist-Pointer in `core/mcp-markdownai.md`. Dist = `feat-mdai` (`origin/main@aac0825` + die 2 Fixes); nach Pull `npm run build --workspaces`, verifizieren via Anhang A.
- **TG2 — `@set`-Pipe-Fix (`body.mdai.md`):** `@set render_target_resolved = render_target | default("none") /` entfernt (`@set` kann keine Pipe-Source sein); Use-Site auf inline-Interpolation umgestellt.
- **TG3 — Directive-Indentation:** alle echten `@`-Directives in `write-spec.md` und `body.mdai.md` auf Spalte 0 normalisiert (Verhalten unverändert).

## v0.1.3 — 2026-05-29 — markdownai-v2 adoption & library fix (engine 1.3.0)
```

- [ ] **Step 7: validate der geänderten Library-Datei**

Run (aus Repo-Root):
```bash
npx --prefix markdownai mai validate mdai/core/mcp-markdownai.md
```
Expected: kein Parse-Fehler. (Hinweis: validate-Grenze bei `${MDAI_LIBRARY_ROOT}`-Includes ist hier nicht relevant — `mcp-markdownai.md` hat keine.)

- [ ] **Step 8: reformat + commit**

Tool: `mcp__jetbrains__reformat_file` auf jede geänderte Datei (Projekt-Regel vor `git add`):
- `mdai/core/mcp-markdownai.md`
- `mdai/changelog.md`

(`docs/…/findings`-Datei liegt außerhalb von `mdai/` — falls reformat fehlschlägt/irrelevant, überspringen.)

Run (jeden Befehl einzeln — keine `&&`-Ketten):
```bash
git add docs/mdai/findings/2026-05-29-mdai-engine-constraints-v3-vs-1.3.0.md
git add mdai/core/mcp-markdownai.md
git add mdai/changelog.md
git commit -m "$(cat <<'EOF'
docs(mdai): TG1 v0.1.4 — engine-fix repro-smokes + dist-dependency pointers

- findings-v3 Anhang A: add Smoke A (ede9793) + Smoke B (f16b4c2) repro commands
- core/mcp-markdownai.md: one-liner dist-dependency pointer to Anhang A
- changelog: new v0.1.4 section (single-sourced pointer, no content dup)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2 (TG2): `body.mdai.md` `@set`-Pipe-Fix (§2-B)

**Files:**
- Modify: `mdai/skills/mdai-brainstorm/body.mdai.md` (write-outputs-Phase, ~Z. 183–192)
- Modify: `mdai/changelog.md` (v0.1.3 Known-limitation-Zeile aktualisieren)

Aktueller Block (write-outputs-Phase):
```
Optional inline-render (only when explicitly requested):

@set render_target_resolved = render_target | default("none") /

mcp__markdownai__call_macro(
file="mdai/skills/mdai-brainstorm/write-spec.md",
macro="render_spec",
args={ "slug": "{{ slug }}", "target": "{{ render_target_resolved }}" },
cwd="<repo>"
)
```

- [ ] **Step 1: Smoke-Gate — greift der `default`-Filter in der Interpolation?**

Schreibe `/tmp/mdai-default-gate.md` (via Write-Tool) mit exakt diesem Inhalt (`render_target` bleibt unset):
```
@markdownai v1.0
target={{ render_target | default('none') }}
```
Run (aus Repo-Root):
```bash
npx --prefix markdownai mai render /tmp/mdai-default-gate.md
```
Expected — Entscheidung:
- Ausgabe `target=none` ⇒ **Filter greift** → in Step 2 die Inline-Variante A nehmen.
- Ausgabe `target=` (leer) oder Fehler ⇒ **Filter greift NICHT** → Step 2 Variante B (siehe dort). Entscheidung im Commit-Body notieren.

- [ ] **Step 2: Fix anwenden — `@set`-Zeile entfernen, Use-Site umstellen**

Tool: `mcp__lean-ctx__ctx_edit`

**Variante A (Filter greift — bevorzugt):**
old_string:
```
@set render_target_resolved = render_target | default("none") /

mcp__markdownai__call_macro(
file="mdai/skills/mdai-brainstorm/write-spec.md",
macro="render_spec",
args={ "slug": "{{ slug }}", "target": "{{ render_target_resolved }}" },
cwd="<repo>"
)
```
new_string:
```
mcp__markdownai__call_macro(
file="mdai/skills/mdai-brainstorm/write-spec.md",
macro="render_spec",
args={ "slug": "{{ slug }}", "target": "{{ render_target | default('none') }}" },
cwd="<repo>"
)
```

**Variante B (Filter greift NICHT — Fallback):** identisches old_string wie A, aber new_string nutzt schlichtes Inline (leer ⇒ `render_spec`-`@switch` trifft `@case "none"` nicht; daher `@case "none"` Default sicherstellen — `render_spec` matcht bei leerem `target` keinen Case. Deshalb bei Variante B explizit `none` als Literal-Default über die Aufrufer-Konvention dokumentieren):
```
mcp__markdownai__call_macro(
file="mdai/skills/mdai-brainstorm/write-spec.md",
macro="render_spec",
args={ "slug": "{{ slug }}", "target": "{{ render_target }}" },
cwd="<repo>"
)
```
Plus Prosa-Zeile direkt davor ergänzen: `Wird kein render_target gesetzt, "none" übergeben (kein Inline-Render).` — damit der Aufrufer den fehlenden Filter-Default kompensiert.

- [ ] **Step 3: Verifizieren — write-outputs-Phase rendert warning-frei**

Run via MCP-Tool `mcp__markdownai__resolve_phase` (cwd = Repo-Root):
```
mcp__markdownai__resolve_phase(
  file="mdai/skills/mdai-brainstorm/body.mdai.md",
  phase="write-outputs",
  cwd="/home/tholo/Scripts/lean-ctx"
)
```
Expected: rendert ohne den Fehler `"@set" cannot be used as a pipe source`. Erlaubte agent-time `@call`/`@query`-Warnings (z. B. nicht aufgelöste `{{ slug }}`) sind ok.

- [ ] **Step 4: Changelog v0.1.3 Known-limitation-Zeile aktualisieren**

Die §2-B-Limitation ist jetzt behoben — Zeile als „resolved in v0.1.4" markieren statt „out of this scope".

Tool: `mcp__lean-ctx__ctx_edit`
old_string:
```
- `body.mdai.md:185` `@set render_target_resolved = render_target | default("none") /` — `@set` cannot be a pipe source
  in 1.3.0 (pre-existing; out of this scope, tracked in findings-v3).
```
new_string:
```
- `body.mdai.md` `@set … | default(…) /` pipe-source error — **resolved in v0.1.4** (TG2): `@set`-Zeile entfernt,
  Use-Site auf Inline-Interpolation umgestellt.
```

- [ ] **Step 5: reformat + commit**

Tool: `mcp__jetbrains__reformat_file` auf `mdai/skills/mdai-brainstorm/body.mdai.md` und `mdai/changelog.md`.

Run (einzeln):
```bash
git add mdai/skills/mdai-brainstorm/body.mdai.md
git add mdai/changelog.md
git commit -m "$(cat <<'EOF'
fix(mdai): TG2 v0.1.4 — remove @set pipe-source in body.mdai.md write-outputs

@set render_target_resolved = render_target | default(...) is invalid (@set
cannot be a pipe source). Inline the default into the render_spec call-site.
write-outputs phase now resolves without the pipe-source error.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3 (TG3): Directive-Indentation-Normalisierung

**Files:**
- Modify: `mdai/skills/mdai-brainstorm/write-spec.md` (genestete Directives)
- Modify: `mdai/skills/mdai-brainstorm/body.mdai.md` (`@phase-end` L53, pre-context)

**Regel:** De-Indent **nur echte `@`-Directives** auf Spalte 0. AUSGENOMMEN bleiben: `@render-template`-Body-Args (`slug=`/`date=`/`body=` key=value), Markdown-Listen (`- …`), Prosa, Inline-Code, und String-Continuation-Zeilen (z. B. die zweite Zeile eines mehrzeiligen `cmd="…"`).

- [ ] **Step 1: Live-Inventar erzeugen**

Tool: `mcp__lean-ctx__ctx_search`
```
mcp__lean-ctx__ctx_search(pattern="^\\s+@", path="mdai", max_results=60)
```
Expected (Stand jetzt): Treffer nur in `body.mdai.md` (`:53 @phase-end`) und `write-spec.md` (`@else`, `@mkdir`, `@render-template`, `@render-template-end`, `@if-end` ×3, `@case`, `@if`, `@query` ×… , `@switch-end`). Notiere die vollständige Trefferliste — **alle** echten Directives daraus de-indenten, außer den oben genannten Ausnahmen.

- [ ] **Step 2: `body.mdai.md` — `@phase-end` (L53) de-indenten**

Tool: `mcp__lean-ctx__ctx_edit`
old_string:
```
- Hard rules: see @include above
  @phase-end
```
new_string:
```
- Hard rules: see @include above
@phase-end
```

- [ ] **Step 3: `write-spec.md` — `write_spec`-Block de-indenten (L20–28)**

Tool: `mcp__lean-ctx__ctx_edit`
old_string:
```
- Not overwriting to prevent silent data loss.
  @else
  @mkdir docs/mdai/specs /
  @render-template from="mdai/skills/mdai-brainstorm/templates/spec-template.md" to="docs/mdai/specs/{{ spec_date }}-{{ slug }}-design.mdai.md" force
slug={{ slug }}
date={{ spec_date }}
body={{ body }}
  @render-template-end
- wrote docs/mdai/specs/{{ spec_date }}-{{ slug }}-design.mdai.md
  @if-end
```
new_string:
```
- Not overwriting to prevent silent data loss.
@else
@mkdir docs/mdai/specs /
@render-template from="mdai/skills/mdai-brainstorm/templates/spec-template.md" to="docs/mdai/specs/{{ spec_date }}-{{ slug }}-design.mdai.md" force
slug={{ slug }}
date={{ spec_date }}
body={{ body }}
@render-template-end
- wrote docs/mdai/specs/{{ spec_date }}-{{ slug }}-design.mdai.md
@if-end
```
(`slug=`/`date=`/`body=` bleiben Spalte 0 — das sind `@render-template`-Body-Args, keine Directives.)

- [ ] **Step 4: `write-spec.md` — `render_spec`-Block de-indenten (L49–59)**

Tool: `mcp__lean-ctx__ctx_edit`
old_string:
```
@query mcp markdownai read_file file="{{ spec_path }}" /
@else

- err: Cannot render — spec file does not exist at {{ spec_path }}
- Call write_spec(slug, body) first.
  @if-end
  @case "file"
  @if file.exists({{ spec_path }})
  @mkdir docs/mdai/specs/rendered /
  @query mcp lean-ctx ctx_shell cmd="cd markdownai && npx mai render \"../{{ spec_path }}\" > /
  \"../docs/mdai/specs/rendered/{{ spec_date }}-{{ slug }}.rendered.md\""
  @else
- err: Cannot render — spec file does not exist at {{ spec_path }}
- Call write_spec(slug, body) first.
  @if-end
  @switch-end
```
new_string:
```
@query mcp markdownai read_file file="{{ spec_path }}" /
@else

- err: Cannot render — spec file does not exist at {{ spec_path }}
- Call write_spec(slug, body) first.
@if-end
@case "file"
@if file.exists({{ spec_path }})
@mkdir docs/mdai/specs/rendered /
@query mcp lean-ctx ctx_shell cmd="cd markdownai && npx mai render \"../{{ spec_path }}\" > \"../docs/mdai/specs/rendered/{{ spec_date }}-{{ slug }}.rendered.md\""
@else
- err: Cannot render — spec file does not exist at {{ spec_path }}
- Call write_spec(slug, body) first.
@if-end
@switch-end
```
**Achtung:** Das `cmd="…"` der `@query`-Zeile war vom IDE-Watcher auf zwei Zeilen umgebrochen (das ` /` am alten Zeilenende ist ein Wrap-Artefakt, kein Self-Close). Hier zu **einer** logischen Zeile zusammengeführt. Falls `validate` (Step 5) danach einen Fehler an dieser Zeile meldet, war der Umbruch semantisch relevant — dann nur de-indenten und die zweite Zeile als Continuation belassen.

- [ ] **Step 5: validate beider Dateien**

Run (aus Repo-Root, einzeln):
```bash
npx --prefix markdownai mai validate mdai/skills/mdai-brainstorm/write-spec.md
npx --prefix markdownai mai validate mdai/skills/mdai-brainstorm/body.mdai.md
```
Expected: kein Parse-/Indentation-Fehler. (Erlaubt: `@include: file not found` für `${MDAI_LIBRARY_ROOT}/…`-Includes in `body.mdai.md` — bekannte validate-Grenze, findings-v3 §7, kein Defekt.)

- [ ] **Step 6: MCP-Smoke der betroffenen Macros (Verhalten unverändert)**

`write_spec` mit einem Wegwerf-Slug rendern (schreibt KEINE echte Spec, da Slug einzigartig — danach Datei löschen):
```
mcp__markdownai__call_macro(
  file="mdai/skills/mdai-brainstorm/write-spec.md",
  macro="write_spec",
  args={ "slug": "_tg3-smoke", "body": "smoke" },
  cwd="/home/tholo/Scripts/lean-ctx"
)
```
Expected: rendert die `@render-template`-Logik ohne Parse-Fehler (Output nennt den Ziel-Pfad). Anschließend etwaige erzeugte Datei entfernen:
```bash
rm -f docs/mdai/specs/*_tg3-smoke-design.mdai.md
```

- [ ] **Step 7: Regression — keine eingerückten Directives mehr**

Tool: `mcp__lean-ctx__ctx_search`
```
mcp__lean-ctx__ctx_search(pattern="^\\s+@", path="mdai", max_results=60)
```
Expected: **0** Treffer für echte `@`-Directives (ausgenommen bleiben nur Nicht-Directive-Zeilen wie `@render-template`-Body-Args, sofern eine zufällig mit `@` beginnt — hier nicht der Fall). `core/file-utils.md`-Treffer (falls vorhanden) ignorieren (ausgenommen, Maintainer-gepflegt).

- [ ] **Step 8: reformat + commit**

Tool: `mcp__jetbrains__reformat_file` auf `mdai/skills/mdai-brainstorm/write-spec.md` und `mdai/skills/mdai-brainstorm/body.mdai.md`.

> **Watcher-Hinweis:** Der IDE-Markdown-Watcher kann committete `.md` autonom re-indentieren/-wrappen (Spec Non-Goal / Risiko). Nach `reformat_file` und vor `git add` erneut `ctx_search "^\s+@" mdai` laufen lassen; falls der Watcher wieder eingerückt hat, vor dem Commit erneut de-indenten.

Run (einzeln):
```bash
git add mdai/skills/mdai-brainstorm/write-spec.md
git add mdai/skills/mdai-brainstorm/body.mdai.md
git commit -m "$(cat <<'EOF'
style(mdai): TG3 v0.1.4 — normalize @-directive indentation to column 0

write-spec.md (nested @else/@mkdir/@render-template/@if-end/@case/@switch-end)
+ body.mdai.md (@phase-end L53) de-indented. Behavior unchanged; verified via
mai validate + write_spec call_macro smoke. render-template body-args + prose
left as-is.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## End-Gate (nach allen drei Tasks)

- [ ] **Step 1: Alle 5 Phasen von `body.mdai.md` rendern — keine neuen Fehler**

Run via MCP `mcp__markdownai__resolve_phase` (cwd = Repo-Root) für jede Phase einzeln: `pre-context`, `dialog-rules`, `dialog-process`, `write-outputs`, `handoff`.
```
mcp__markdownai__resolve_phase(file="mdai/skills/mdai-brainstorm/body.mdai.md", phase="<phase>", cwd="/home/tholo/Scripts/lean-ctx")
```
Expected: jede Phase rendert ohne neuen Fehler. Erlaubt: agent-time `@call`/`@query`-Warnings (nicht aufgelöste Laufzeit-Vars) und `@include: file not found` für `${MDAI_LIBRARY_ROOT}/…` unter validate. NICHT erlaubt: `@set … pipe source`-Fehler (TG2) oder Indentation-Parse-Fehler (TG3).

- [ ] **Step 2: Finale Regression**

Run:
```bash
npx --prefix markdownai mai validate mdai/skills/mdai-brainstorm/write-spec.md
```
Tool: `mcp__lean-ctx__ctx_search(pattern="^\\s+@", path="mdai", max_results=60)`
Expected: validate fehlerfrei; ctx_search = 0 echte Directive-Treffer (außer ausgenommene Nicht-Directive-Zeilen / `file-utils.md`).

---

## Self-Review (vom Plan-Autor durchgeführt)

- **Spec-Coverage:** TG1 → Task 1 (Verify Steps 1–3, Single-Source-Doku Steps 4–6). TG2 → Task 2 (Smoke-Gate Step 1, Fix Step 2, warning-frei Step 3). TG3 → Task 3 (Inventar Step 1, De-Indent Steps 2–4, validate+Smoke Steps 5–6, Regression Step 7). Querschnitt Leanness/DRY → Single-Source via Pointer (Task 1 Steps 4–6, kein separates Smoke-File). Verifikations-Strategie + Engine-Baseline → Setup-Step + End-Gate. Non-Goals (kein Push/PR, keine Engine-Code-Änderung, file-utils ausgenommen) → eingehalten.
- **Zusätzlich gefunden:** Inventar deckte `body.mdai.md:53 @phase-end` auf (Spec-Inventar nannte nur write-spec.md) — in TG3 Step 2 abgedeckt.
- **Placeholder-Scan:** keine TBD/TODO; alle Edits mit konkreten old/new-Strings, alle Smokes mit konkretem Datei-Inhalt + erwarteter Ausgabe.
- **Konsistenz:** Macro-/Arg-Namen (`render_spec`, `write_spec`, `render_target`, `render_target_resolved`) durchgängig; Baseline-Commit (`aac0825`) konsistent zu Spec + Changelog-Text.
