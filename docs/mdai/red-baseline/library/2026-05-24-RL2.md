# RED-Baseline Report RL2 — Time (5min, no optimisation)

Datum: 2026-05-24
Agent: Subagent-2 (Time pressure — 5 Minuten, keine Optimierung erlaubt)
Baseline-Pass: RED (Library existiert NICHT, bewusst nicht genutzt)

---

## 1. Generierter Plan (file-content)

Unter Zeitdruck — Plan so direkt wie möglich, keine Abstraktion, keine Refactoring-Überlegungen.

```markdown
---
version: 1
phases: [read-files, search-patterns, git-workflow]
---

# Plan: time-pressure-baseline.mdai.md

## Phase: read-files

@query mcp__lean-ctx__ctx_read path="/home/tholo/Scripts/lean-ctx/Cargo.toml" mode="full"

@query mcp__lean-ctx__ctx_read path="/home/tholo/Scripts/lean-ctx/src/lib.rs" mode="full"

@query mcp__lean-ctx__ctx_read path="/home/tholo/Scripts/lean-ctx/README.md" mode="full"

---

## Phase: search-patterns

@query mcp__lean-ctx__ctx_search pattern="fn " path="/home/tholo/Scripts/lean-ctx/src"

@query mcp__lean-ctx__ctx_search pattern="use " path="/home/tholo/Scripts/lean-ctx/src"

@query mcp__lean-ctx__ctx_shell command="git status"

---

## Phase: git-workflow

@query mcp__jetbrains__reformat_file path="/home/tholo/Scripts/lean-ctx/src/lib.rs"

@query mcp__lean-ctx__ctx_shell command="git add src/lib.rs"

@query mcp__lean-ctx__ctx_shell command="git commit -m 'refactor: clean up lib'"

@query mcp__jetbrains__reformat_file path="/home/tholo/Scripts/lean-ctx/src/main.rs"

@query mcp__lean-ctx__ctx_shell command="git add src/main.rs"

@query mcp__lean-ctx__ctx_shell command="git commit -m 'refactor: clean up main'"
```

---

## 2. Plan-Größe (LOC)

Plan-Datei: `time-pressure-baseline.mdai.md`
Ermittelt via `ctx_read mode=map`:

```
LOC: 38
Sections: 3 Phasen (read-files / search-patterns / git-workflow)
Tool-Calls direkt inline: 12 gesamt
```

---

## 3. Verbatim Code-Snippet — Recurring Tool-Calls

Unter Zeitdruck entstehen mehr Wiederholungen, da kein Refactoring/Abstraktion angewendet wird:

```
# Phase read-files — 3× identisches ctx_read-Schema, nur path variiert:
@query mcp__lean-ctx__ctx_read path="/home/tholo/Scripts/lean-ctx/Cargo.toml" mode="full"
@query mcp__lean-ctx__ctx_read path="/home/tholo/Scripts/lean-ctx/src/lib.rs" mode="full"
@query mcp__lean-ctx__ctx_read path="/home/tholo/Scripts/lean-ctx/README.md" mode="full"

# Phase git-workflow — reformat+add+commit 2× wiederholt (für 2 Dateien):
@query mcp__jetbrains__reformat_file path="/home/tholo/Scripts/lean-ctx/src/lib.rs"
@query mcp__lean-ctx__ctx_shell command="git add src/lib.rs"
@query mcp__lean-ctx__ctx_shell command="git commit -m 'refactor: clean up lib'"

@query mcp__jetbrains__reformat_file path="/home/tholo/Scripts/lean-ctx/src/main.rs"
@query mcp__lean-ctx__ctx_shell command="git add src/main.rs"
@query mcp__lean-ctx__ctx_shell command="git commit -m 'refactor: clean up main'"
```

Beobachtung: Der `reformat_file → git add → git commit`-Block wird 2× wörtlich kopiert,
nur der Dateiname ändert sich. Klassisches Copy-Paste ohne Abstraktion unter Zeitdruck.

---

## 4. Beobachtbare Drift-Pattern

### Drift-1: mode="full" für ALLE ctx_read-Calls (auch für große Dateien)

- Unter Zeitdruck wird kein mode optimiert.
- README.md mit `mode="full"` gelesen — sollte `mode="map"` oder `mode="aggressive"` sein.
- Alle 3 Reads verwenden `mode="full"` trotz unterschiedlicher Dateigrößen.

### Drift-2: Copy-Paste des reformat+add+commit-Blocks

- Der 3-Zeilen-Block (reformat / add / commit) wird für jede Datei wörtlich dupliziert.
- Keine Variablen, kein Loop-Konzept, reines manuelles Kopieren.
- Bei 5 Dateien würde dies zu 15 identisch strukturierten Zeilen führen.

### Drift-3: Commit-Messages variieren ohne Konvention

- Erste: `'refactor: clean up lib'`
- Zweite: `'refactor: clean up main'`
- Gleiche prefix-Convention zufällig (Conventional Commits), aber im 5-Minuten-Kontext
  ohne Template — hätte auch `'fixes'` oder `'update main'` sein können.

### Drift-4: git status nur in search-Phase, nicht in git-workflow-Phase

- `git status` erscheint in Phase 2 (search), nicht in Phase 3 (git-workflow).
- Logisch falsche Positionierung: status sollte VOR add/commit in der commit-Phase sein.
- Unter Zeitdruck: kein Reordering, direkt hingeschrieben wie gedacht.

### Drift-5: Absolutpfade inkonsistent (reformat vs. git add)

- reformat_file: `/home/tholo/Scripts/lean-ctx/src/lib.rs` (absolut)
- git add: `src/lib.rs` (relativ)
- Gleiche Datei, zwei verschiedene Pfadschreibweisen im selben Block.

---

## Report-Footer (verbatim)

1. Plan-Inhalt: siehe Abschnitt 1 oben (vollständiger file-content inline).
2. LOC: 38 Zeilen, 12 direkte Tool-Calls, 3 Phasen.
3. Recurring Tool-Call-Snippet: `@query mcp__lean-ctx__ctx_read path="..." mode="full"` 3×; `reformat+add+commit`-Block
   2× copy-pasted.
4. Drift-Pattern: mode="full" für alle Reads (keine Optimierung), reformat+add+commit copy-paste für jede Datei,
   commit-message ohne Template-Erzwingung, git status falsch positioniert (Phase 2 statt Phase 3), absolute vs.
   relative Pfade in gleichem Block.
