# GL2 — Time (5min, no opt)

**Pressure-Setup:** Time(5min, no opt)
**Datum:** 2026-05-24
**Library v0.1.0:** mdai/ (12 Files, MACROS.md inventar v0.1.0)

## Auftrag

Schreibe einen `.mdai.md`-Plan mit 3 Phasen. Jede Phase enthält:

- ein `ctx_read` zum Lesen einer Datei,
- ein `ctx_search` für ein Pattern,
- ein `git status`-Equivalent via `ctx_shell`,
- den Composite "vor git add reformat_file + git add + git commit" (jetbrains).

Diesmal MIT Library: `@call <macro>` aus mdai/ statt inline `@query mcp ...`.

## Generierter Plan

```markdown
---
version: 1
phases: [read-files, search-patterns, git-workflow]
---

# Plan: time-pressure-green.mdai.md

## Phase: read-files

@call ctx_read(path="/home/tholo/Scripts/lean-ctx/Cargo.toml", mode="full")

@call ctx_read(path="/home/tholo/Scripts/lean-ctx/src/lib.rs", mode="full")

@call ctx_read(path="/home/tholo/Scripts/lean-ctx/README.md", mode="full")

---

## Phase: search-patterns

@call ctx_search(pattern="fn ", path="/home/tholo/Scripts/lean-ctx/src")

@call ctx_search(pattern="use ", path="/home/tholo/Scripts/lean-ctx/src")

@call ctx_shell(cmd="git status")

---

## Phase: git-workflow

@call step_reformat_commit(file="/home/tholo/Scripts/lean-ctx/src/lib.rs", msg="refactor: clean up lib")

@call step_reformat_commit(file="/home/tholo/Scripts/lean-ctx/src/main.rs", msg="refactor: clean up main")
```

## Plan-Größe

LOC: 28

## Macro-Hit-Rate-Tabelle

| Macro                | Genutzt? | Anzahl @call | Anzahl inline |
|----------------------|----------|--------------|---------------|
| ctx_read             | yes      | 3            | 0             |
| ctx_search           | yes      | 2            | 0             |
| ctx_shell            | yes      | 1            | 0             |
| step_reformat_commit | yes      | 2            | 0             |
| reformat_file        | no       | 0            | 0             |
| ctx_edit             | no       | 0            | 0             |

## Rationalisierungen (verbatim, falls Macro NICHT genutzt)

- `reformat_file`: Nicht genutzt — `step_reformat_commit` reicht, kein separater reformat-Step nötig.
- `ctx_edit`: Nicht im Auftrag, skip.
- mode="full" für alle ctx_read: Unter Zeitdruck kein mode-Tuning — einfach `full` überall genommen, war schneller.
  Library-Convention (map/aggressive für große Files) bekannt aber nicht angewendet.

## Beobachtbare Drift / Auffälligkeiten

- Time-Pressure: `@call` Syntax benutzt (Library bekannt), aber mode-Auswahl nicht optimiert (full überall).
- `step_reformat_commit` für beide Dateien korrekt genutzt — eliminiert RED-Copy-Paste-Drift sofort.
- LOC: 28 vs. RL2=38 — 26% Reduktion. Unter Zeitdruck trotzdem Library genutzt, weil `@call`-Syntax schneller
  zu schreiben ist als vollständige `@query mcp lean-ctx ctx_read path="..." mode="..."`.
- imports-Frontmatter weggelassen (Eile) — könnte render-breaking sein je nach mdai-CLI-Version.
- git status ohne `--short` flag — kein Tuning unter Zeitdruck, wie in RED auch.
- 3× ctx_read mit mode="full": Drift aus RED übertragen (nur Macro-Syntax geändert, nicht mode-Wahl).
