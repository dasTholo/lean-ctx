# GL1 — Cold (kein Druck)

**Pressure-Setup:** Cold
**Datum:** 2026-05-24
**Library v0.1.0:** mdai/ (12 Files, MACROS.md inventar v0.1.0)

## Auftrag

Schreibe einen `.mdai.md`-Plan mit 3 Phasen. Jede Phase enthält:
- ein `ctx_read` zum Lesen einer Datei,
- ein `ctx_search` für ein Pattern,
- ein `git status`-Equivalent via `ctx_shell`,
- den Composite "vor git add reformat_file + git add + git commit" (jetbrains).

Diesmal MIT Library: `@call <macro>` aus mdai/ statt inline `@query mcp ...`.

## Generierter Plan

```markdown
---
version: 1
phases: [inspect, search, commit]
mdai_bootstrap: true
imports:
  - mdai/core/ctx-tools.md
  - mdai/tooling/jetbrains.md
---

# Plan: inspect-search-commit-green.mdai.md

## pre-context

@call mdai_bootstrap()

## Phase: inspect

Lese die Hauptkonfigurationsdatei des Projekts.

@call ctx_read(path="/home/tholo/Scripts/lean-ctx/Cargo.toml", mode="map")

Lese die Changelog-Datei für aktuellen Stand.

@call ctx_read(path="/home/tholo/Scripts/lean-ctx/CHANGELOG.md", mode="aggressive")

---

## Phase: search

Suche nach allen TODO-Markierungen im Codebase.

@call ctx_search(pattern="TODO", path="/home/tholo/Scripts/lean-ctx/src")

Suche nach allen pub fn Definitionen in Rust-Quellen.

@call ctx_search(pattern="pub fn", path="/home/tholo/Scripts/lean-ctx/src")

Prüfe Git-Status.

@call ctx_shell(cmd="git status --short")

---

## Phase: commit

Reformat, stage und commit src/main.rs.

@call step_reformat_commit(file="/home/tholo/Scripts/lean-ctx/src/main.rs", msg="fix(main): update entry point logic")
```

## Plan-Größe

LOC: 35

## Macro-Hit-Rate-Tabelle

| Macro | Genutzt? | Anzahl @call | Anzahl inline |
|---|---|---|---|
| ctx_read | yes | 2 | 0 |
| ctx_search | yes | 2 | 0 |
| ctx_shell | yes | 1 | 0 |
| step_reformat_commit | yes | 1 | 0 |
| reformat_file | no | 0 | 0 |
| ctx_edit | no | 0 | 0 |

## Rationalisierungen (verbatim, falls Macro NICHT genutzt)

- `reformat_file`: Nicht direkt genutzt — `step_reformat_commit` kapselt reformat+add+commit als atomic unit;
  separates `reformat_file` wäre redundant.
- `ctx_edit`: Kein Edit-Auftrag in diesem Plan — nicht applicable.

## Beobachtbare Drift / Auffälligkeiten

- Cold-Pressure: Ausreichend Zeit, um Library-Conventions zu lesen → alle eligible Calls via `@call`.
- mode-Wahl (map/aggressive) explizit aus Library-Convention abgeleitet — kein mode-Wildwuchs.
- `step_reformat_commit` eliminiert den dreiteiligen reformat+add+commit-Block aus RED komplett.
- Kein Namespace-Mix: lean-ctx + jetbrains sind via Library-Macros transparent koordiniert.
- LOC: 35 vs. RL1=42 — 17% Reduktion, obwohl Cold-Plan inhaltlich äquivalent.
- Frontmatter `imports:` Section explizit angegeben — Library-Convention klar erkennbar.
