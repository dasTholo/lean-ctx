requirements-traceability.md [192L]
# Token-Control-Platform — Requirements & Evidence Matrix
> **INTERNAL — Betriebsgeheimnis.** · Stand 2026-07-28 (aktualisiert nach E4 A2A Transport Hardening)
> Diese Matrix verhindert „fertig durch Behauptung". `Status` beschreibt den
... [lean-ctx: omitted 2 lines]
## Statusregeln
- **Built:** Runtimepfad und relevante Tests wurden im aktuellen Code gefunden.
... [lean-ctx: omitted 1 lines]
- **Planned:** kein produktiver, verifizierter Zielpfad vorhanden.
... [lean-ctx: omitted 1 lines]
## A. Plattform und Contracts
| ID | Requirement | Status | Aktuelle Evidenz | Zielnachweis | Wave |
... [lean-ctx: omitted 1 lines]
| PL-01 | Customer-owned Data/Control/Evidence Plane | Partial | Lokale Engine, Proxy und Ledger bleiben ohne Billing-Edge nutzbar. Die Hosted-`PaidCache`-Lizenz toleriert einen bereits gecachten Pro-Entscheid ausschließlich bis exakt eine Stunde nach Lookup-Fehler und nur bei Completion-Time innerhalb der Grenze; ein äußerer Zwei-Sekunden-Deadline umfasst Semaphore-, Blocking-Queue-, Request- und Join-Zeit. Ein realer Sechs-Zustände-Binary-Prozess-Gate plus MCP und Local-Free 5/5 belegen die lokale Grenze. Air-gap, HA, Full-Cloud und SSO bleiben unbelegt | Cloud-Ausfall-/Offline-E2E | W4–W9 |
| PL-02 | 14 unabhängige OCLA Capabilities | Partial | OSS-`core/ocla` enthält alle 14 versionierten, object-safe Traits mit payloadfreien Request-/Result-Types und Trait-Count-/Object-Safety-Gates. Der optionale `OclaBus` definiert zehn payloadfreie Lifecycle-Events und ist global default-disabled; UsageSink, SavingsLedger, der PathJail-/Registry-gebundene CompressionProvider, der explizite OutcomeTracker, der trusted-signal-only IntentClassifier und `BuiltinAgentGateway` sind echte Adapter. Der Gateway akzeptiert ausschließlich einen schema-versionierten, kanonisch BLAKE3-identifizierten, vollständig korrelierten, owner-gebundenen, distinct-agent und `capsule:`-gebundenen Relay-Envelope mit positivem Budget, emittiert opt-in `AgentChainEvent` und journalisiert die Admission als Ledger-only `AgentRelayAdmitted`; Budget bleibt ausdrücklich nur eine Autorisierungsgrenze, nicht Delivery-/Savings-Evidenz. Der Gateway führt weder Materialisierung noch Netzwerkzustellung aus. Der vollständige Proxy-Usage-Meter emittiert zusätzlich `RequestCompleted`. Ledger/Outcome prüfen vollständige Lineage und bounded Evidence, der Provider verlangt registrierte Recovery-Refs, explizite Quality-Policy und Tokenparität; der Intent-Adapter akzeptiert genau einen kanonischen Kandidaten mit opaque autorisiertem Signal-Ref. Produktive Signal-Emitter und übrige Adoption folgen | Trait count + object-safety tests + bus/built-in/proxy emitter tests | W1/W2 |
| PL-03 | Rust- und Wire-Projektion bleiben konsistent | Partial | `CanonicalTokenEnvelopeV1` und der payloadfreie A2A-`AgentEnvelopeV1` besitzen strikte JSON-Wire-Projektionen. Der eigenständige `lean-ctx-client` 0.2.0 decodiert beide ohne Engine-Abhängigkeit gegen paketlokale, byteidentische Draft-2020-12-Schemas und prüft unbekannte/duplizierte/nichtkanonische Felder, Lineage, Accounting, Relay-ID und exakte `u64`-Maxima fail-closed. Wire-Validität und Gateway-Admission sind getrennt: Self-Relay bleibt wire-valid, wird durch die explizite Gateway-Policy abgewiesen. Schema-/Rust-/Package-Drift, Rust 1.74 und Stable sind lokal grün; veröffentlichte externe Interoperabilität fehlt | external SDK compatibility suite | W1/W6 |
... [lean-ctx: omitted 1 lines]
| PL-05 | Kanonisches providerneutrales Token Envelope | Partial | payloadfreies `CanonicalTokenEnvelopeV1` normalisiert Lineage, Surface, Provider/Model, Richtung, Token-Lifecycle, Route/Policy und Idempotency; produktiver Proxy, MCP-Read und `ctx_shell` emittieren bei vollständiger Lineage korrelierte Envelopes in den lokalen ContextReceipt-Journalpfad. Shell bindet den Envelope zusätzlich an einen kanonischen Shadow-Plan und verweigert eine Receipt-Projektion bei unvollständiger Lineage oder expandierender Lieferung; A2A und weitere Provider-Adapter fehlen | provider golden roundtrip corpus | W1 |
... [lean-ctx: omitted 3 lines]
## B. Interception und Protokolle
| ID | Requirement | Status | Aktuelle Evidenz | Zielnachweis | Wave |
... [lean-ctx: omitted 5 lines]
| IN-05 | Agent-to-Agent Interception | Partial | `core/a2a/`, Handoff-Primitives, schema-versionierter kanonischer `AgentEnvelope`, validierender lokaler `BuiltinAgentGateway` und deterministischer Capsule-A2A-Manifestadapter. Admission ist mit einer BLAKE3-Relay-ID idempotency-aware korrelierbar und lokal ledger-journalisiert. `AgentRelayTransportV1` definiert die object-safe OSS-Boundary für künftige Sidecar-/Remote-/Customer-Transporte. Der separate lokale `LocalSignedCapsuleTransport` implementiert sie: er schreibt eine autorisierte, Ed25519-verifizierbare Capsule in eine In-Process-Queue, misst serialisierte Bytes exakt und Tokens ausschließlich als `local_tokenizer_proxy`; er persistiert einen vollständig korrelierten Agent-Receipt vor dem Ledger-only-`AgentRelayDelivered` und rollt den Queue-Eintrag bei Receipt-Fehler zurück. Fehlgeschlagene gepinnte Key-Prüfung konsumiert nicht. **E4 (2026-07-28):** `remote_transport.rs` implementiert HTTP-basiertes `AgentRelayTransportV1` mit Retry/Timeout/Auth-Header; `health.rs` prüft Transport-Readiness (Ready/Degraded/Unavailable); `relay.rs` trackt Multi-Hop Relay Chains mit Cycle-Detection und Max-Hop-Limit; `budget_cascade.rs` verteilt Token-Budgets Parent→Child mit Lineage; `telemetry.rs` zählt Delivery Success/Failure/Latency. Noch offen: Trust-Distribution/Rotation, E2E mit echtem Remote-Agent, Backpressure unter Last | AgentGateway chain E2E mit authentisierter Remote-Zustellung | W7 |
| IN-06 | OpenAI Chat und Responses | Partial | provider modules vorhanden; `codex/w0-token-control-foundation` schützt erwartete Codex-/SSE-Streams vor dem allgemeinen Read-Idle-Timeout mit Transport-Regressionstest | stream/tool/error golden traces | W4 |
| IN-07 | Anthropic Messages | Partial | `proxy/anthropic.rs` | bidirectional translation E2E | W4 |
... [lean-ctx: omitted 3 lines]
| IN-11 | Streaming und Tool Calls semantisch erhalten | Partial | Der synthetische W0-Golden-Corpus nutzt die produktive `StreamXlat`-Route: SSE-Ausgabe bleibt über alle Transport-Splitpositionen und Byte-Chunks identisch; parallele/interleavte Tool-Indizes bewahren ID/Name/Block-Lineage, malformed/unknown Fortsetzungen und Multiple Choices liefern genau ein terminales Anthropic-Error-Event. OpenAI-`arguments` und Anthropic-`partial_json` werden indexgebunden und bounded reassembliert. Native Providerstreams außerhalb der Shape-Translation und Live-Provider-E2E fehlen | cross-provider golden traces | W4 |
| IN-12 | Nicht-interceptbarer Traffic ehrlich ausgewiesen | Partial | `client-path-coverage-matrix-v1` gleicht 23 produktive Constraint-Clients und 35 produktiv konsumierte MCP-Setup-Agenten sowie `custom-agent` als externes Fixture code-/docsgebunden ab. Drei nicht-interceptierbare Grenzen sind explizit; `runtime_path_complete` und `live_adapter_complete` bleiben hart `false`. Maintained Deployment-/Live-Traffic-Coverage fehlt | maintained coverage matrix | W0/W10 |
## C. Kompression und Qualität
| ID | Requirement | Status | Aktuelle Evidenz | Zielnachweis | Wave |
... [lean-ctx: omitted 1 lines]
| CO-01 | Struktur-aware Input Compression | Partial | compressor, AST/read modes sowie `FidelityClassV1`/`assess_fidelity`: bytegenaues `exact`, lokal gemessenes `structural`, kein lokal behauptetes `semantic`; versionierter Code-/JSON-/Docs-Golden-Corpus deckt Accept/Reject ab. Der reale `ctx_read`-Density-Guard nutzt den Contract für Accept/Recovery. Der Proxy-Prose-Squeeze nutzt zusätzlich dedup-aware vollständige Unique-Line-Abdeckung; Truncation/Summary bleibt unbelegt | versioned quality benchmark + weitere Hotpaths | W3 |
| CO-02 | Shell/Tool-Output Compression | Partial | shell patterns/tool output sowie versionierter Tool-Output-/Unicode-Log-Corpus; lossy Candidate wird an der strukturellen Grenze abgewiesen. Generic Tool-Terse akzeptiert nur bestehendes Quality-/Savings-Gate plus unabhängigen strukturellen Fidelity-Pass, sonst Original-Fallback | adversarial golden corpus + Shell/Provider/Response coverage | W3 |
... [lean-ctx: omitted 2 lines]
| CO-05 | Tokenizer-/Model-Family Calibration | Partial | `TokenizerFamily::calibration` und `count_tokens_with_calibration` kennzeichnen die lokale Text-BPE-Zählung als `exact_text_encoding` oder die nicht-provider-billed Approximationen als `proxy_tokenizer` bzw. `corrected_proxy_tokenizer`; ungesicherte Modellnamen ändern keine Familie | versioned per-family provider-count calibration corpus/report | W3 |
... [lean-ctx: omitted 1 lines]
| CO-07 | Response Optimization | Partial | verbosity/output modules; Output-Holdout publiziert einen statistisch gemessenen A/B-Token-Effekt nur als `measured_unverified` mit `outcome_evidence_required` und `commercial_savings_eligible=false`. Vollständig korrelierte Proxy-Receipts tragen zusätzlich `ExperimentAssignmentV1`; `ctx_receipt parity` fordert je Arm 30 vollständige explizite Acceptance-/Quality-Outcomes und weist Treatment-Regression aus, ohne kommerziell freizugeben. `/api/roi.output_quality` macht dieselbe lokale Replay-Evidenz sichtbar, ohne ROI/Metering zu verändern | task-quality + semantic gate + lineage-verknüpfter Holdout + policy approval | W3/W7 |
... [lean-ctx: omitted 1 lines]
| CO-09 | Kein Savings-Gewinn ohne Quality Gate | Partial | Lokaler Fidelity-Contract, `ctx_read`, `ctx_response`, Holdout und Receipt-Parität sperren unbelegte Savings weiterhin. `SettlementEvidenceManifestV2` verlangt für Eligibility vollständige Methodenreferenzen sowie Baseline-, Price-, Contract-, Quality-, Attribution-, PeriodCompletion- und CustomerApproval-Evidenz aus einem externen Trust Store; `Disputed` und `Superseded` sind explizite Evidenzzustände. Sämtliche Authority-Flags bleiben im OSS-Artefakt hart `false`. Reale Customer-Approval-, Vertrags- und Ledger-Enforcement-Evidenz fehlt | combined outcome test + policy/ledger enforcement | W2/W3/W7/W9 |
... [lean-ctx: omitted 1 lines]
## D. Control, Routing und Agents
| ID | Requirement | Status | Aktuelle Evidenz | Zielnachweis | Wave |
... [lean-ctx: omitted 1 lines]
| CT-01 | Human/Service/Agent/Workload Identity | Partial | gateway identity/agent IDs; `SignedContextCapsuleV1` bindet einen Capsule-Owner per Ed25519 an einen explizit gepinnten Sender-Key und verweigert Owner-/Key-/Signatur-Drift beim A2A-Import | end-to-end attribution suite + versionierte Trust-/Rotation-Policy | W5 |
| CT-02 | Policy-Hierarchie und deterministische Priorität | Partial | org/local policy primitives | conflict/override property tests | W5 |
... [lean-ctx: omitted 2 lines]
| CT-05 | Sensitivity, Egress und Residency | Partial | PII/sensitivity/security modules | cross-adapter enforcement test | W5 |
... [lean-ctx: omitted 3 lines]
| CT-09 | Agent fan-out und chain budget | Partial | agent budget/A2A primitives; `AgentLeaseRegistryV1` liefert bounded, idempotente Pfad-/Symbol-Exklusivität als Grundlage für konfliktfreien Fan-out | bounded chain + concurrent lease/work graph test | W7 |
| CT-10 | ACT nur nach Approval, mit rollback | Partial | Der lokale OCLA-`BuiltinConfigTuner` v2 erlaubt ausschließlich vier bounded Scalar-Keys, erzeugt content-addressierte Proposal-/Rollback-Refs, verlangt genau eine zeitgültige operator-owned Approval-Receipt und führt Apply/Rollback global serialisiert, drift-/identity-gebunden und bytegenau aus. Durable Zustände, idempotentes Reapply, exakter Rollback sowie Symlink-/Swap-/Concurrent-Negativfälle sind geprüft. V1 sowie MCP/HTTP/Wire/SDK bleiben nicht aufrufbar; der unsigned Approval Store authentifiziert keinen Akteur und begründet keine externe ACT-Autorität | audit + rollback integration | W7 |
## E. Evidence und FinOps
| ID | Requirement | Status | Aktuelle Evidenz | Zielnachweis | Wave |
... [lean-ctx: omitted 3 lines]
| EV-03 | Measurement Method und Evidence Class | Partial | Die bestehenden payloadfreien Method-/Class-Artefakte bleiben erhalten. `SettlementEvidenceManifestV2` trennt gemessene, reconciled und deklarierte Methodenreferenzen und bindet sie mit sieben Rollen an einen out-of-band geladenen Trust Store. ISO-Zeit, Integer-Beträge, Canonicalization, Preflight-Bounds vor Hashing sowie `nofollow`-/atomare I/O werden fail-closed geprüft. Die eingefrorene zehnfeldrige Usage-v1-Quelle und ihre `is_billable`-Semantik bleiben unverändert kompatibel; Signaturen belegen nur Integrität/Herkunft, nicht Kausalität, Vertragsgültigkeit oder Settlement-Autorität | schema + backward compat test | W2/W9 |
... [lean-ctx: omitted 2 lines]
| EV-06 | Exklusive Attribution, kein Double Count | Partial | Event-/Receipt- und trusted Task-Attempt-Reconciliation bleiben fail-closed. Settlement Evidence v2 fordert zusätzlich eine exklusive Attribution über Basis-, Korrektur- und Supersession-Lineage; 5'040 Rollenpermutationen ergeben dasselbe kanonische Ergebnis, während doppelte/überlappende Attribution, fehlende Trust-Entscheide und unvollständige Methoden sperren. Die experimentelle Unified-Ledger-Projektion (`docs/reference/unified-ledger-evidence-projection-v2.md`, `unified_ledger_evidence_v2_gate`) bindet jede positive Ledger-Beobachtung an genau eine reconciled Source- und Attribution-Gruppe und weist Permutations-/Doppelverwendungsfälle zurück. Produktive cross-source Attribution, Billing und Settlement fehlen | reconciliation/property tests + produktiver Attribution-Replay | W2/W7/W9 |
| EV-07 | Customer Approval/Dispute/Settlement | Partial | `SettlementEvidenceManifestV2` modelliert eine CustomerApproval-Rolle, die Evidenzzustände `Disputed` und `Superseded`, Korrektur-/Supersession-Lineage sowie out-of-band Trust-Entscheide; es definiert keine Dispute- oder Settlement-Rolle. Der lokale Verifier hält Approval-/Contract-/Pricing-/Invoice-/Settlement-Authority ausdrücklich `false`. Reale Freigaben, Rechtswirksamkeit, Dispute-Workflow und private Settlement-Ausführung bleiben außerhalb des OSS-Nachweises | idempotent settlement workflow mit realen Customer-Entscheiden | W9 |
... [lean-ctx: omitted 1 lines]
| EV-09 | Signatur wird nicht als Kausalität behauptet | Partial | Evidence-Attestations, Key-Rotation, `SignedContextCapsuleV1` und Settlement Evidence v2 belegen ausschließlich Herkunft/Integrität. Der v2-Verifier akzeptiert Rollen nur gegen einen getrennten out-of-band Trust Store und hält alle Authority-Flags `false`; keine Signatur behauptet Kausalität, Outcome-Qualität, Approval, Vertragsgültigkeit oder Settlement | doc/API assertion audit | W0/W9 |
| EV-10 | Rechnung vollständig auf Evidenz rückführbar | Partial | Settlement Evidence v2 bindet Usage-v1-Quellintegrität, Methoden, exklusive Attribution, Price/Baseline/Contract, CustomerApproval sowie die Zustände `Disputed`/`Superseded` und Korrektur-/Supersession-Lineage content-addressed und offline verifizierbar. Die Unified-Ledger-Projektion bindet zusätzlich den verifizierten Savings-Snapshot, Signed-Batch und Source-/Attribution-IDs deterministisch; sie bleibt ein untrusted Attribution-Adapter und erzeugt weder Rechnung noch Zahlungs-/Settlement-Entscheid. Invoice-Provenance-E2E und private Workflow-Evidenz fehlen | invoice provenance E2E | W9 |
## F. Enterprise, Security und Betrieb
| ID | Requirement | Status | Aktuelle Evidenz | Zielnachweis | Wave |
... [lean-ctx: omitted 2 lines]
| EN-02 | Definierte Fail-open/closed Matrix | Partial | Ein realer Sechs-Zustände-Binary-Prozess-Gate plus MCP unterscheidet lokale Free-Fähigkeit, gültige Pro-Lizenz, Lookup-Ausfall innerhalb/außerhalb der einstündigen Completion-Time-Grenze, Revocation und konkurrierende Lookups. Fehler öffnen nur den bezahlten Cache innerhalb der engen Grace; lokale Free-Pfade bleiben unabhängig. Eine systemweite Policy-Class-/Cloud-/HA-Matrix fehlt | systemweite policy-class failure suite | W5/W8/W9 |
... [lean-ctx: omitted 2 lines]
| EN-05 | Zero-downtime Upgrade/Rollback | Partial | Der produktive atomare Installationspfad, `delivery-evidence-v1`, fünf digestgebundene Lifecycle-Stages und ein offline verifizierter Rollback-Harness belegen lokale Komponenten- und Contract-Konsistenz. Der Contract hält `zero_downtime_verified` und `external_operational_acceptance` ausdrücklich `false`; Multi-Replica-/Load-Balancer-Drain, Inflight-Stream-Parität, Produktionsupgrade, realer Rollback und SLO-Messung fehlen | HA upgrade drill | W8/W9 |
... [lean-ctx: omitted 1 lines]
| EN-07 | Threat Model/SBOM/Provenance/Vuln Process | Partial | Security-/History-Gates sowie getrennte `delivery-evidence-v1`- und `support-lifecycle-v1`-Verträge binden Schema, Verifier, Harness und Release-Tag exakt; operative Security-/Release-Attainment-Flags bleiben false. Produktionssignatur, SBOM-/Provenance-Emission und Vulnerability-Response-Nachweis fehlen | signed release security pack | W8/W9 |
... [lean-ctx: omitted 2 lines]
| EN-10 | Retention/Delete/Residency/Legal Hold | Partial | config/policies verteilt; jeder lokale `EventKind` projiziert compiler-exhaustiv eine stabile Privacy-Klasse (`operational|customer|security|evidence metadata`) und Retention-Klasse (`operational|evidence|security_audit`). `EvidenceRetentionPolicyV1` definiert OSS-seitig getrennte Fenster von 1–36'500 Tagen, opaque Policy-Ref und Legal Hold; die Entscheidung nutzt injizierte Zeit, inklusive Grenztag, und behält Future-Timestamps fail-safe. Event- und Receipt-Retention verarbeiten jeweils aktive und rotierte Journale unter Prozess-/Interprozess-Lock und entfernen nur valide, eindeutig klassifizierte bzw. versioniert persistenzzeitbelegte abgelaufene Records; korrupte und nicht klassifizierbare Legacy-Zeilen bleiben gezählt erhalten, Legal Hold verhindert Expiration. `ActivePolicy::evidence_retention_policy` projiziert eine konfigurierte OSS-`audit_retention_days`-Frist validiert in diesen Vertrag, ohne Legal Hold oder Lizenzstatus zu erfinden; Pack-Validation lehnt 0 und Werte über 36'500 Tagen bereits fail-closed ab. `apply_local_evidence_retention` führt beide Sweeps explizit mit derselben injizierten Zeit aus, liefert getrennte Summaries und weist einen möglichen Teilfehler nach Event-Erfolg offen aus. Automatisches Scheduling, Customer-/Legal-Hold-State-Resolution, Delete und Residency-Enforcement fehlen | data lifecycle acceptance | W8 |
| EN-11 | Metrics/Logs/Traces/SIEM | Partial | tracing/metrics modules; Support-Lifecycle v1 definiert getrennte Evidence-Refs und lokale Harnesses, setzt LTS-, SLA-, On-call-, Incident-, OS- und Operability-Attainment jedoch ausschließlich auf false | operability dashboard/runbook + reale Support-Attainment-Evidence | W8/W9 |
... [lean-ctx: omitted 1 lines]
## G. Open Source, Commercial und Adoption
| ID | Requirement | Status | Aktuelle Evidenz | Zielnachweis | Wave |
... [lean-ctx: omitted 1 lines]
| BC-01 | OSS Data Plane vollständig lokal nutzbar | Built/Partial | Local-Free-Contract und CI laufen normal sowie `public-minimal-no-ort` 5/5. Lizenzlookup begrenzt nur den bezahlten Cache; 64 tatsächliche Worker, 50'000 Cache-/Metadaten-Einträge, äußerer Zwei-Sekunden-Deadline und per-user Epoch-/Latest-Authorität schützen Ausfall, Revocation und Race-Fälle. Die bestehende 14-Tage-Client-Grace bleibt unverändert. Vollständiger Air-gap-/Full-Feature-Nachweis fehlt | full offline scenario | W8/W9 |
| BC-02 | OCLA Spec/Wire/Verifier offen | Partial | Öffentliche OCLA-Wire-Schemas, der ausschließlich 2.0.0 unterstützende Contract-Pack und Apache-2.0-`lean-ctx-client` 0.2.0 enthalten einen bounded Offline-Decoder und Verifier-CLI; das paketierte Crate trägt seine Draft-2020-12-Schemas und Fixtures selbst. Contract-Pack-/Delivery-Digest und die deterministische Ed25519-Testsignatur auf Basis des RFC-8032-Testseeds sind neu gebunden. Ein veröffentlichter Release, Remote-Compatibility-Nachweis und Production-Key-Evidence fehlen | public artifact/release | W6 |
... [lean-ctx: omitted 1 lines]
| BC-04 | Setup/Integration als standardisiertes Paket | Partial | `delivery-evidence-v1` und `support-lifecycle-v1` trennen Delivery- von Support-Evidenz und binden Stage, Harness, Release-Tag, Schema und Verifier exakt; fünf hermetische Lifecycle-Harnesses und required CI belegen die Paketstruktur. Wiederholte reale Kundeninstallation fehlt | repeatable delivery checklist | W9/W10 |
... [lean-ctx: omitted 1 lines]
| BC-06 | Value Share capped, quality-bound, approved | Partial | Settlement Evidence v2 bindet integerbasierte Baseline-/Pricing-/Contract-/Quality-/Approval-Refs und exklusive Attribution, setzt aber sämtliche Approval-, Contract-, Pricing-, Invoice- und Settlement-Authority-Flags hart `false`. Capping-Formel, signiertes Customer Schedule und echte Freigabe fehlen | signed customer schedule + E2E | W9 |
| BC-07 | Support/LTS/SLA operativ lieferbar | Partial | `support-lifecycle-v1` besitzt exakte Schema-/Verifier-Parität, release-tag-/harnessgebundene Stages und fünf hermetische Lifecycle-Harnesses; drei Wave-8-Dokumente sind Frozen. Alle operativen Claims bleiben false-only: kein LTS-, SLA-, On-call-, OS-Zertifizierungs- oder Incident-Attainment | support matrix/on-call/LTS policy mit realer Betriebs-Evidenz | W8/W9 |
... [lean-ctx: omitted 3 lines]
## H. Ultimate Token Efficiency
| ID | Requirement | Status | Aktuelle Evidenz | Zielnachweis | Wave |
... [lean-ctx: omitted 1 lines]
| TE-01 | ETPAO als primäre Nettoeffizienz-Metrik | Partial | `reconcile_accepted_outcome_etpao` liefert eine deterministische Request→Accepted-Outcome-Bilanz mit vollständiger Korrelation, Receipt-Ref und allen Kostenkomponenten. Ergänzend reconciled `reconcile_task_attempt_quality` Initialversuch, Retry und Regeneration nur bei vollständiger Task-/Attempt-/Kind-/Sequence-Lineage; fehlende Duplicate-Messung bleibt `None`, gemessene Null bleibt explizit Null, Sequence-Lücken und untypisierte Legacy-Evidenz sperren. Ein payloadfreier Fünf-Client-Corpus und `w0-quality-baseline-v2` laufen durch den produktiven Reconciler; Duplicate 90‰, Freshness 500‰ und First-Pass 0‰ sind ausschließlich hermetische Contractwerte, alle Runtime-Observed-Values bleiben `null`. **E2 (2026-07-27):** `savings_ledger/etpao.rs` berechnet RuntimeEtpao aus echten Ledger-Events (Baseline vs. Delivered, per-Tool Breakdown); `telemetry.rs` exportiert `ObservedEfficiency` mit realen Cache-/Request-Stats; `ctx_gain.rs` zeigt ETPAO-Section mit Live-Daten im Dashboard; 5 E2E-Tests validieren die Pipeline. `/api/roi` hält Runtime-Adapter-, Commercial-Savings- und Settlement-Eligibility auf `false`; produktive Nicht-Proxy-/Duplicate-Evidenz und echte Clientadapter-Parität fehlen | reproduzierbare request→attempt→accepted-outcome Bilanz mit Runtime-Evidenz | W0/W2 |
| TE-02 | Runtime-belegte Coverage Class je Clientpfad | Partial | `core::client_coverage` liefert einen versionierten, payloadfreien und auf 1'000 Receipts/Events begrenzten Runtime-Probe. `FullInline` verlangt genau einen validierten Proxy-Receipt, vollständige vierdimensionale Lineage, genau ein `GatewayUsage` sowie exakte Provider-/Model-/Token-Reconciliation gegen Input-/Output-Envelopes und `EffectiveTokenCostV1`; MCP/Shell/Agent ergibt `ContextControlled`, eindeutige reine Usage-/Outcome-Evidenz `ObserveOnly`, keine Evidenz `Unknown`, Incomplete/Ambiguous/Over-bound `Uncontrolled`. Clientnamen werden nicht heuristisch verwendet; Live-IDE-Parität und Commercial Eligibility bleiben hart `false`. Die maintained Matrix bindet zusätzlich 23 Constraint-Clients und 35 MCP-Setup-Agenten an codekonsumierte Registries, exakte Rust-AST-Source-Refs und den Docs-SSOT; Missing→`Unknown` sowie Incomplete/Ambiguous→`Uncontrolled` laufen required unter `CI Green`. `runtime_path_complete` und `live_adapter_complete` bleiben `false`; echte Adapter-Probes und Deployment-/Live-Traffic-Coverage fehlen | E2E Coverage Probe + maintained matrix | W0/W4 |
... [lean-ctx: omitted 6 lines]
| TE-09 | ContextCapsuleV1 für deltafähige Handoffs | Partial | `core/context_capsule.rs` definiert einen versionierten, payloadfreien Manifest-Contract mit kanonischer BLAKE3-Identität, Delta-/Parent-Referenzen, Content-/Evidence-/Recovery-Refs, Sensitivity/Allowlist, Budget, Ownership/Attribution und bounded Chain-Hops. Eine validierte Capsule kann ausschließlich für einen erlaubten Ziel-Agenten als bestehendes `AgentEnvelope` projiziert sowie über deterministische A2A-Manifestadapter exportiert/importiert werden. `SignedContextCapsuleV1` bindet Owner und Manifest per Ed25519; der signed Import verlangt genau ein Artefakt, übereinstimmende Empfänger-/Capsule-/Signer-Metadaten und einen extern gepinnten Sender-Key. Ein lokaler Referenztransport liefert die signierte Capsule nach erfolgreicher Pin-Prüfung genau einmal aus einer In-Process-Queue und macht Bytes/Proxy-Tokens sowie Agent-Receipt replaybar. Materialisierung, Remote-Zustellung, Trust-Distribution/Rotation und Multi-Hop-Delta-Resolution fehlen | multi-hop capsule/delta compatibility E2E | W1/W6/W7 |
| TE-10 | Path-/Symbol-Leases gegen Agentenkollisionen | Partial | `core/agent_lease.rs` definiert einen payloadfreien, versionierten Lease-Request/-Grant-Contract und bounded lokalen Registry-Kern. Akquisition ist pro Request-Ref idempotent, verweigert fremde Owner, verlangt exakte Owner-/Lease-Ref beim Release und räumt Ablauf deterministisch über einen injizierten Clock-Wert. `ctx_agent lease_acquire`/`lease_release` bindet den Kern als opt-in lokale Agentenoberfläche: raw Pfad-/Symbol-Input wird nur zu opaque `pathref:`/`symbolref:` gehasht, und Tool-Output enthält keinen Pfad. Ein paralleles Acht-Agenten-Gate belegt genau einen Grant und sieben Holder-Resultate am gemeinsamen Prozess-Registry-Rand. AgentGateway-/Mutation-Adoption, Persistenz, Policy und concurrent mutation E2E fehlen | concurrent mutation conflict suite | W7 |
... [lean-ctx: omitted 1 lines]
| TE-12 | Multi-Agent Nettoeffizienz-Benchmark | Partial | `MultiAgentEfficiencyBenchmarkCorpusV1` und `reconcile_multi_agent_efficiency_benchmark` vergleichen 30–256 paarweise identische, samples-übergreifend eindeutige Single-Agent-Control- und Multi-Agent-Treatment-Workloads. Beide Arme benötigen vollständig korrelierte Receipts, Accepted Outcomes und alle zehn `EffectiveTokenCostV1`-Komponenten. Treatment-Handoffs werden nur mit gepinnten Ed25519-signierten `ContextCapsuleV1`, lokal-prozessgebundenem V2-Selbstkonsistenzvertrag, Receipt-Eintrag sowie kanonisch neu berechneten A2A-Bytes/Proxy-Tokens bilanziert; unsignierte oder manipulierte Delivery-Zähler, fehlende/mehrdeutige Evidence, Reference Drift, Quality-/Outcome-Regression und Overflow sperren sämtliche Armmetriken. Der lokale 30-Paar-Gate belegt deterministische ETPAO-, Duplicate-Context-, konfigurierte Fan-out- und Outcome-Parität ausschließlich hermetisch im selben Prozess. Er belegt keinen Transport-Commit oder tatsächliche Zustellung; Live-Traffic, Provider-Billing und Commercial-Savings-Eligibility bleiben hart `false`, reale Multi-Agent-Runtime, repräsentative Kundenworkloads und Produktionskonfidenz fehlen | single-vs-multi baseline mit Outcome-Parität | W3/W10 |
## I. Repository, Supply Chain und Delivery Boundary
| ID | Requirement | Status | Aktuelle Evidenz | Zielnachweis | Wave |
... [lean-ctx: omitted 1 lines]
| RG-01 | GitHub ist kanonischer OSS Source; GitLab `lean-ctx` nur read-only Mirror | Partial | `repository-governance-v1` weist GitHub `yvgude/lean-ctx` als einzige OSS-/Engineering-SSOT und GitLab als `historical_reference` aus; die alte GitLab-SSOT-Kollision in `docs/IMPLEMENTATION_PROTOCOL.md` ist korrigiert. Ein repo-weiter bounded Active-Prose-Gate prüft Entity-/Prädikat-/Negationsbindung, HEAD↔Index↔Worktree, pinned Refs und CODEOWNERS required unter `CI Green`. Mirror-Automation, Branch Protection und unabhängiger Trust Anchor bleiben `externally_unverified` | automated mirror + drift gate + owner decision | W0 |
... [lean-ctx: omitted 3 lines]
| RG-05 | Commercial konsumiert nur versionierte öffentliche Contracts/SDKs | Partial | Der required Public-Clean-Room-Gate scannt 1'996 getrackte OSS-Buildinputs contentfrei und bounded gegen private Contract-Imports, Pfade, Git-/Registry-/Action-Quellen sowie Cargo-`patch`/`replace`; Public-Minimal-No-ORT und Local-Free 5/5 sind grün. `lean-ctx-client` 0.2.0 beweist zusätzlich eine separate, enginefreie öffentliche OCLA-Consumer-Grenze auf Stable, Rust 1.74 und aus dem extrahierten Paket. Ein realer Commercial-Consumer und dessen Compatibility fehlen | dependency-direction + realer Commercial-Compatibility-Gate | W1/W6/W9 |
| RG-06 | Production Images sind immutable, signiert, SBOM-/Provenance-gebunden | Partial | `delivery-manifest-v1` verlangt unveränderlichen OCI-SHA256-Digest und content-addressierte SBOM-, SLSA-Provenance-, Signatur- und Vulnerability-Evidence. `delivery-trust.py` signiert den kanonischen Promotion-Payload deterministisch mit Ed25519 und verifiziert ihn offline gegen einen content-identifizierten Trust Root; strict Main-Subgroup-, Small-Order/Identity-, Base64-, Symlink-/PathJail- und Cross-Artifact-Prüfungen laufen im required CI-Gate. `leanctx.release-key-rotation/v1` bindet distinkte Old/New Root-Datei- und Key-Content-IDs an genau drei erlaubte Pre-Activation-/Overlap-/Post-Revocation-Zustände; Receipt-Key-Rollenselektion bleibt vor der bestehenden strikten Signaturprüfung fail-closed und ist mit 10 Negativ-/Positivfällen required. Produktions-Key-Ceremony, KMS/HSM-Operation, Wall-clock-Aktivierung, Secret-Vernichtung, tatsächlich ausgeführte Rotation und echte release-pipeline-erzeugte Evidence fehlen | verify signature/SBOM/provenance by digest | W0/W8 |
... [lean-ctx: omitted 1 lines]
| RG-08 | Secrets leben nur in Vault/Secret Store und besitzen Rotation Evidence | Partial | `security/history-policy-v1.json` versioniert konservative Secret-/IP-Regeln; der required CI-Gate prüft den öffentlichen HEAD und den Delta-Bereich seit einer digestgebundenen Baseline. Evidence enthält nur Rule/Pfad/Objekt-Ref/Hash-ID und keine Secret-Werte; der integrierte Lauf meldet 0 neue Findings. Produktive Rotation Evidence sowie Repo-/Image-/Log-Zero-Secret-Abnahme fehlen | rotation drill + zero-secret repo/image/log scan | W0/W8 |
| RG-09 | Öffentliche History ist auf Secrets/IP geprüft und Entscheidung dokumentiert | Partial | Ein kanonischer Baseline-Report bindet den Audit-Commit, Policy-Fingerprint, Scanner-Versionen und Scanner-Quell-Digests. Er dokumentiert 5'045 Commits, 218'932 Objekte, 27 historische Findings sowie 32 getrennte geerbte Current-Tree-Finding-IDs; der required bounded Current-/Delta-Gate steht bei 0 neuen Findings. Der explizite Vollaudit ist auf 20'000 Commits, 300'000 Objekte, 100 Findings und 120 Sekunden begrenzt, wird aber nicht pro Push ausgeführt. Rotation-/Rewrite-Entscheid und erneuter Vollaudit bleiben offen; kein Clean-History-Claim | full-history reports + rotation/rewrite decision | W0 |
... [lean-ctx: omitted 3 lines]
## J. Holistic Context Intelligence
| ID | Requirement | Status | Aktuelle Evidenz | Zielnachweis | Wave |
... [lean-ctx: omitted 1 lines]
| HC-01 | Einheitlicher ContextObjectV1-/ContentRef-Vertrag für alle Context-Quellen | Partial | `core/context_object.rs` definiert einen payloadfreien, store-neutralen `ContextObjectV1`-Metadatacontract mit BLAKE3-`ContentRef`, stabiler Object-ID, Provenance, Freshness/Invalidation, Governance, Ownership/Lifecycle sowie token- und recovery-beschriebenen Views. `context_workspace::project_workspace_file` projiziert den ersten realen Store PathJail-geschützt, ohne Pfad/Payload im Contract, mit stabiler Source-ID und frischer ContentRef. Erfolgreiche MCP-Reads registrieren diesen Store asynchron in einer prozesslokalen opaque Recovery-Registry, die der globale CompressionProvider erneut PathJail-prüft auflöst. Serde-/Payload-Leak-/Referenz-/View-/Store-/Provider-Invarianten sind getestet; weitere Stores und cross-store Identity-Fixtures fehlen | serde/schema roundtrip + cross-store identity fixtures | W1 |
... [lean-ctx: omitted 1 lines]
| HC-03 | ContextReceiptV1 verbindet Plan, Lieferung, Outcome und Quality | Partial | OSS-`ContextReceiptV1` ist lokal journalisiert und replaybar; Proxy und MCP-Read emittieren bei vollständiger Lineage Receipts einschließlich kanonischer, payloadfreier Token-Envelopes. `attach_plan` akzeptiert nur einen validierten, kanonisch content-gebundenen `ContextPlanV1` und persistiert lediglich die Plan-ID. Der lokale A2A-Referenztransport hängt pro zustellbarer Capsule genau eine validierte Delivery-Evidenz an einen Agent-Receipt: nur kanonische Relay-/Capsule-Refs, nur Agent-Surface, ohne Relay-Duplikate; der Receipt wird vor dem zugehörigen Ledger-Event geschrieben und ist replaybar. `SignedContextReceiptBundleV1` kann 1–1'000 solcher bereits validierten Receipts kanonisch und Ed25519-signiert exportieren; der untrusted Decoder begrenzt Input und kanonischen Export auf 8 MiB. Offline-Verify prüft Bundle-/Receipt-Invarianten, eindeutige IDs und Ursprung/Unverändertheit ohne lokalen Journalzugriff, behauptet aber keine Kausalität, Billing oder Savings. `ctx_outcome` nimmt explizite, payloadfreie Acceptance-/Quality-Evidenz nur mit Request-/Session-/Agent-/Content-Korrelation auf und der Builtin-Tracker schreibt sie in den lokalen Ledger. `reconcile_outcome_events` löst einen Outcome-Event nur gegen genau einen gleich korrelierten validen Receipt auf und liefert dessen BLAKE3-Integritäts-Ref; Missing/Ambiguous bleiben explizit. Das lokale read-only `ctx_receipt` projiziert diese Evidence ohne Payload-, Pfad- oder Promptleak und ist E2E-getestet; der Replay deckt die bewusst eine retained Journalgeneration vor dem aktiven Journal ab. Persistierte Link-Projektion und produktive Plan-/Shell-/Remote-A2A-Abbildung fehlen | request→plan→receipt→accepted-outcome replay | W2/W4 |
... [lean-ctx: omitted 7 lines]
| HC-11 | Multi-Agent-Fusion nutzt Capsules, Evidence-Deltas, Leases und Work Graph statt Prompt-Anhängen | Partial | A2A/Handoff/Context OS besitzen noch keinen kanonischen Gesamtpfad. `ContextCapsuleV1` liefert nun aber den payloadfreien, content-gebundenen Handoff-Manifestvertrag mit Allowlist, Sensitivity, Budget, Ownership/Attribution, Evidence-/Recovery-Handles und Delta-/Parent-Lineage; die OCLA-Envelope-Projektion sowie der deterministische A2A-Manifestexport/-import verweigern nicht erlaubte Ziel-Agenten und abweichende Metadaten. Der signed A2A-Pfad verlangt zusätzlich einen gepinnten Ed25519-Sender-Key und weist Owner-/Key-/Signaturdrift ab, ersetzt aber keine Trust-Distribution. `AgentLeaseRegistryV1` stellt nun eine bounded, idempotente Pfad-/Symbol-Exklusivität bereit, ist aber noch nicht an Gateway, Tool-Claims, Mutation oder Work Graph gebunden. Materialisierung, Evidence-Delta-Transport und Work-Graph-Fusion fehlen | multi-hop fusion/ownership/fan-out benchmark | W6/W7 |
| HC-12 | Holistic Context Quality misst ETPAO, Recall, Freshness, Duplicate Ratio und First-Pass Success gemeinsam | Partial | `EffectiveTokenCostV1`, Accepted-Outcome-ETPAO und der gemeinsame bounded Retained+Active+Live-Replay bleiben die Runtime-Basis. `TaskAttemptRefV1` typisiert Attempt-Kind und Sequence; die trusted Reconciliation erzeugt Metriken nur bei vollständiger Task-/Attempt-/Kind-/Sequence-Lineage, eindeutigen Outcomes, lückenlosen Sequenzen, vollständigen Costs und expliziter Duplicate-Messung (`Option<u64>` unterscheidet fehlend von gemessen null). Eine exhaustive 5×12-Cache-Outcome-/Reason-Truth-Table klassifiziert Fresh/Stale oder bewusst nicht im Freshness-Denominator; jede andere Paarung sperrt. Der payloadfreie Fünf-Client-Corpus prüft Initial, Retry und Regeneration permutationsstabil. `w0-quality-baseline-v2` ruft den produktiven Reconciler auf und erkennt Fixture-Mutationen; seine Werte Duplicate 90‰, Freshness 500‰ und First-Pass 0‰ sind nur hermetische Contractwerte. Sämtliche `observed_value` bleiben `null`, insbesondere Runtime-Proxy-Metriken bis zu echter Duplicate-Evidenz. Recall, echte Adapter-/Outcome-Parität, Produktions-SLO und Commercial Eligibility fehlen | versioned cross-client quality dashboard + produktive Runtime-Evidence + CI gate | W0/W3/W10 |
| HC-13 | Live-Telemetrie trennt Payload, Control, Cache und Ledger ohne Doppelzählung | Partial | Session-Counter, `EventChannel`, Summary/Eventdetail und die bounded Event-ID-deduplizierte Retained+Active+Live-Sicht trennen Payload, Control, Cache und Ledger; Property-Tests sichern `original = sent + saved` und verhindern tokenhaltige Ledger-/Cache-Doppelzählung. Event→Receipt-Reconciliation verlinkt nur genau ein validiertes Receipt vollständiger Lineage; Outcome verlangt identisches Task/Attempt, Missing/Ambiguous/Incomplete bleiben separat. Die neue Task-Attempt-Qualitätsbilanz zählt Initial/Retry/Regeneration nur bei vollständiger Kind-/Sequence-Evidenz und behandelt fehlende Duplicate-Tokens nicht als Null. Fünf-Client-Multichannel- und Attempt-Corpora laufen required und permutationsstabil. **E3 (2026-07-27):** SessionCache, ContentCache und ResponseCache melden hits/misses über `telemetry.record_cache()` in die zentrale Metrik; vorher wurde `record_cache()` NIE aus Produktion aufgerufen, was eine ~0.7% Hit Rate vortäuschte. `ctx_gain.rs` zeigt Multi-Layer Cache Performance (Session/Content/Response/Overall). ResponseCache für deterministische Tool-Calls aktiviert. Cache Warming Modul (`cache/warming.rs`) reduziert Cold-Start Misses. E2E Pipeline-Tests (`cache/pipeline_tests.rs`) validieren die Wiring-Kette. Runtime-Proxy-Duplicate-/First-Pass-/Freshness-Werte bleiben `None`, bis reale vollständige Messungen vorliegen; Live-IDE-, Billing-, Savings- und Settlement-Parität fehlen | event→receipt→attempt reconciliation + Accounting-Properties über Payload/Control/Cache/Ledger | W0/W2 |
... [lean-ctx: omitted 3 lines]
| HC-17 | Jede Runtime-Entscheidung ist Request-, Session-, Agent- und ContentRef-korreliert | Partial | `EventCorrelationV1` ist JSONL-rückwärtskompatibel; Proxy etabliert Request-ID, optionale explizite Session-/Agent-ID und BLAKE3-ContentRef am Interception-Boundary; echte Provider-Nutzung wird als Ledger-Event ohne Savings-Doppelzählung emittiert. `ctx_shell` leitet bei vollständiger lokaler Lineage eine payloadfreie BLAKE3-ContentRef, einen Shell-Input-Envelope und einen kanonischen Shadow-Plan in das lokale Receipt-Journal ab; unvollständige Lineage oder eine expandierende tatsächliche Lieferung bleiben konservativ ohne Receipt. Der lokale A2A-Transport verwendet dieselbe vollständige Capsule-Korrelation für Agent-Receipt und `AgentRelayDelivered`; Remote-/cross-process Trace-Propagation fehlt | distributed trace/receipt lineage E2E über MCP, Shell, Proxy und A2A | W1/W2/W4 |
| HC-18 | IDE-Golden-Corpus belegt Outcome-Parität für Cursor, Claude Code, Copilot, JetBrains und Custom Agents | Partial | Versionierte OSS-Golden-Corpora prüfen für Cursor, Claude Code, Copilot, JetBrains und Custom Agent identisch Snapshot→Append→Unchanged→Rotation→Recovery sowie Task→Initial-Attempt/Retry/Regeneration über denselben Proxy-Header-Contract. Der bounded ETPAO-Corpus ist als eigener required Job unter `CI Green` verdrahtet und sperrt Missing/Incomplete/Ambiguous Evidence; ein zweiter strikt versionierter Multichannel-Corpus belegt für dieselben fünf Clients Payload/Control/Cache/Ledger-Bilanz, vollständige Event→Receipt-Lineage und bytegleiche Evidenz über alle 120 Clientreihenfolgen. Beide bleiben Fixture-/Contract-Evidenz. `client_coverage` ergänzt eine clientnamenunabhängige Runtime-Klassifikation aus realen Receipt-/Usage-Grenzen, setzt Live-IDE-Parität aber ausdrücklich `false`. Die maintained Coverage-Matrix gleicht zudem 23 Constraint-Clients und 35 MCP-Setup-Agenten gegen produktive Registries ab, ohne Clientnamenheuristik; Runtime-/Live-Vollständigkeit bleibt `false`. Echte Adapter- und Outcome-/Qualitäts-Parität fehlen | client matrix with quality/recovery/token budgets | W0/W3/W10 |
## Completion Summary
Aktueller Stand am integrierten Head `b4baee4cf` (2026-07-23): **114/114** Requirements
... [lean-ctx: omitted 1 lines]
Wave 10 hebt CT-10 und TE-12 von Planned auf Partial; EN-05 wird anhand des
... [lean-ctx: omitted 3 lines]
Der aktuelle Worktree belegt eine starke Engine-Basis mit vollstaendigem OSS OCLA Engineering Milestone (P0-P9, P11, W0-W10, 419+ Kernel-Tests), aber **nicht** die fertige
Token-Control-Platform. OCLA, kanonisches Envelope, Wire Contract, vollständige
... [lean-ctx: omitted 3 lines]
