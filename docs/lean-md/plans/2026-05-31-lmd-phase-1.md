# lmd Phase 1 — Engine-Skelett (Header/Parser/Bridge-Registry/Fragment-Resolver) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

> ⚠️ **SUPERSEDED → `docs/lean-md/plans/2026-06-01-lmd-phase-1.md`.** Nach dem Referenz-Audit (`docs/reference`) ersetzt: geteilter `EngineContext`-Cache + Read→Delta-ohne-`fresh` (Spec §4.2a), Tasks 8/11/12 korrigiert. Diese Fassung **nicht** mehr ausführen.

**Goal:** Das native lmd-Render-Skelett bauen, sodass `@lean-md`, `@include` und ein R-Router (`@read`) end-to-end durch rushdown 0.18 rendern.

**Architecture:** Header wird per Zeilen-Pre-Scan extrahiert (nicht durch rushdown), der Body läuft durch eine rushdown-Extension (ein Block- + ein Inline-Parser) die `@<name>`/`{{ name args }}` in `LmdDirective`/`LmdInline`-Knoten parsen. Die Bridge-Ausführung passiert zur Render-Zeit: der Renderer-Hook trägt `Rc<EngineContext>` (inkl. `BridgeRegistry`) über einen eigenen `LmdRendererOptions`-Typ (rushdowns `RendererOptions` ist ein leerer Marker-Trait) und dispatcht pro Knoten in die Registry. Parser bleiben zustandslos — exakt wie der Phase-0-Spike.

**Tech Stack:** Rust, rushdown 0.18 (extension API: `new_markdown_to_html` + `parser_extension`/`renderer_extension`), bestehende lean-ctx-Core (`tools::ctx_read::handle`, `core::cache::SessionCache`).

**Orientierung (Pflichtlektüre vor Start):**
- `rust/tests/lmd_rushdown_spike.rs` — die einzige verifizierte, kompilierende Vorlage für die rushdown-0.18-Extension-API (Node + Block-/Inline-Parser + Renderer-Hook + Wiring). Jede `*.rs` unten spiegelt deren Muster. Methodennamen, Trait-Bounds und Import-Block sind dort belegt.
- `rust/src/lmd/audit.rs` — das R/H/E-Audit; `@read`/`@include`/`@lean-md` sind die in Phase 1 zu bauenden Einträge (Backings: `src/tools/ctx_read.rs` bzw. Extension).
- `docs/lean-md/specs/2026-05-31-lmd-lean-ctx-native-design.mdai.md` §3.3 (built-in-first Fragment-Resolver), §4 (Engine-Primitive + Bridge-Trait), §6 (Phasen-Gate), §7 (Jail).
- `docs/lean-md/decisions/2026-05-31-phase-0-gate-outcome.md` §1 (exakte rushdown-API-Notizen).

---

## Ausführungs-Konventionen (Subagent-Dispatch) — verbindlich

Diese Regeln gelten für **jeden** für diesen Plan dispatchten Subagent und **müssen wörtlich in den Subagent-Prompt übernommen werden** (sie propagieren nicht automatisch über die Dispatch-Grenze — vgl. Spec §2.3):

1. **Tool-Disziplin in Agenten:** I/O ausschließlich über MCP-Tools — **lean-ctx** (`ctx_read`/`ctx_search`/`ctx_tree`/`ctx_shell`), **serena** (Symbol-Operationen auf `*.rs`: `jet_brains_find_symbol`, `replace_symbol_body`, `insert_before_symbol`/`insert_after_symbol`, `replace_content`), **jetbrains** (`reformat_file`, `get_file_problems`). Native `Read`/`Grep`/`cat`/`sed` sind verboten.
2. **Kein `ctx_shell raw=true`:** Shell-Ausgaben immer komprimiert lassen; `raw=true` nur als letzter Ausweg bei nachweislich fehlerhafter Kompression, dann mit Begründung. Default ist komprimiert.
3. **Rust-Edits über serena:** `*.rs` niemals mit nativem `Edit`/`ctx_edit` bearbeiten — nur serena-Symbol-Tools (Projekt-Hard-Rule).
4. **Modell-Routing:**
   - **Sonnet** für leichte/mechanische Tasks (reine Rust-Funktionen, Arg-Parsing, Registry-Glue, Tests): Tasks **1, 2, 3, 7, 8, 12**.
   - **Opus** für schwierige Tasks (rushdown-Node-/Parser-/Renderer-Wiring, Engine-Verdrahtung, Rekursion/Depth-Guard): Tasks **4, 5, 6, 9, 10, 11**.
5. **Vor jedem `git add`:** `mcp__jetbrains__reformat_file` auf jede geänderte Datei.
6. **Tests:** immer `cargo nextest run`, nie `cargo test`. Keine `&&`-Ketten in Bash — jeden Befehl einzeln.

---

## File Structure

Neue/zu ändernde Dateien (alle unter `rust/`):

- `src/lmd/mod.rs` — **modify**: Modulbaum registrieren.
- `src/lmd/args.rs` — **create**: `DirectiveArgs` (positional + `key=value`).
- `src/lmd/header.rs` — **create**: `LeanMdHeader` + `parse_header` Pre-Scan (`@lean-md`).
- `src/lmd/fragments.rs` — **create**: `FragmentRegistry` (built-in-first + Datei-Fallback im Jail).
- `src/lmd/node.rs` — **create**: AST-Knoten `LmdDirective` (Block) + `LmdInline` (Inline).
- `src/lmd/parser/mod.rs` — **create**: `lmd_parser_extension()` Wiring.
- `src/lmd/parser/block.rs` — **create**: `LmdBlockParser` + reiner Helper `parse_directive_line`.
- `src/lmd/parser/inline.rs` — **create**: `LmdInlineParser` + reiner Helper `parse_inline_body`.
- `src/lmd/bridges/mod.rs` — **create**: `DirectiveBridge`-Trait, `BridgeError`, `BridgeRegistry`, `default_registry()`.
- `src/lmd/bridges/read.rs` — **create**: `ReadBridge` (`@read` → `ctx_read::handle`).
- `src/lmd/bridges/include.rs` — **create**: `IncludeBridge` (`@include` → Fragment + rekursives Render).
- `src/lmd/render.rs` — **create**: `LmdRendererOptions`, Renderer-Hooks, `lmd_renderer_extension()`.
- `src/lmd/engine.rs` — **create**: `EngineContext`, `render`/`render_body` Wiring.
- `tests/lmd_phase1_gate.rs` — **create**: e2e-Gate-Integrationstest.

**Bewusst Phase 1 deferred (nicht hier bauen):** HTML-Escaping/Output-Format-Target (Phase 1 schreibt Bridge-Output roh via `write_safe_str`; markdown-vs-html-Zielformat ist eine spätere Entscheidung, blockt das Gate nicht), `@read`-Jail (Pfad wird durchgereicht; Jail für `@read` ist §7/Phase-7-Security), Macro-Engine/`@if`/`{{ expr }}`-Eval (Phase 3).

---

### Task 1: `DirectiveArgs` + Modulbaum (sonnet)

**Files:**
- Modify: `rust/src/lmd/mod.rs`
- Create: `rust/src/lmd/args.rs`

- [ ] **Step 1: Failing-Test schreiben** — neue Datei `rust/src/lmd/args.rs` nur mit Test-Modul:

```rust
//! Argument parsing for lmd directives: positional tokens + `key=value` pairs.

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn parses_positional_and_named() {
        let a = DirectiveArgs::parse("path/to/file.rs mode=signatures");
        assert_eq!(a.positional(0), Some("path/to/file.rs"));
        assert_eq!(a.get("mode"), Some("signatures"));
        assert_eq!(a.get("missing"), None);
        assert_eq!(a.positional(1), None);
    }

    #[test]
    fn empty_is_empty() {
        let a = DirectiveArgs::parse("   ");
        assert_eq!(a.positional(0), None);
        assert_eq!(a.get("x"), None);
    }
}
```

- [ ] **Step 2: Test scheitert** — `DirectiveArgs` existiert nicht.

Run: `cargo nextest run -p lean-ctx lmd::args`
Expected: FAIL (compile error: cannot find `DirectiveArgs`).

- [ ] **Step 3: Implementierung** — oben in `rust/src/lmd/args.rs` einfügen (vor dem Test-Modul):

```rust
/// Parsed arguments of a directive: whitespace-separated tokens, where a token
/// containing `=` becomes a `key=value` pair and everything else is positional.
/// Phase 1 has no quoting — paths/values must not contain spaces (noted limitation).
#[derive(Debug, Default, Clone)]
pub struct DirectiveArgs {
    positional: Vec<String>,
    named: Vec<(String, String)>,
}

impl DirectiveArgs {
    pub fn parse(raw: &str) -> Self {
        let mut positional = Vec::new();
        let mut named = Vec::new();
        for tok in raw.split_whitespace() {
            match tok.split_once('=') {
                Some((k, v)) if !k.is_empty() => named.push((k.to_string(), v.to_string())),
                _ => positional.push(tok.to_string()),
            }
        }
        Self { positional, named }
    }

    pub fn positional(&self, i: usize) -> Option<&str> {
        self.positional.get(i).map(String::as_str)
    }

    pub fn get(&self, key: &str) -> Option<&str> {
        self.named.iter().find(|(k, _)| k == key).map(|(_, v)| v.as_str())
    }
}
```

- [ ] **Step 4: Modulbaum registrieren** — `rust/src/lmd/mod.rs` so ändern, dass nach `pub mod audit;` folgt:

```rust
pub mod args;
pub mod bridges;
pub mod engine;
pub mod fragments;
pub mod header;
pub mod node;
pub mod parser;
pub mod render;
```

> Hinweis: Die noch nicht existierenden Module werden in den Folge-Tasks angelegt. Wenn der Crate jetzt noch nicht kompiliert, ist das erwartet — bis Task 11 ist der Baum vollständig. Falls du strikt grün halten willst: füge die `pub mod`-Zeilen erst hinzu, wenn die jeweilige Datei existiert. Empfehlung: **nur `pub mod args;` jetzt**, Rest pro Task ergänzen.

- [ ] **Step 5: Test grün** —

Run: `cargo nextest run -p lean-ctx lmd::args`
Expected: PASS (2 tests).

- [ ] **Step 6: Reformat + Commit**

Reformat: `mcp__jetbrains__reformat_file` auf `rust/src/lmd/args.rs` und `rust/src/lmd/mod.rs`.

```bash
git add rust/src/lmd/args.rs rust/src/lmd/mod.rs
git commit -m "feat(lmd): DirectiveArgs parser + module tree (phase 1)"
```

---

### Task 2: `@lean-md` Header Pre-Scan (sonnet)

**Files:**
- Create: `rust/src/lmd/header.rs`
- Modify: `rust/src/lmd/mod.rs` (nur falls in Task 1 nicht alle `pub mod` gesetzt — dann `pub mod header;` aktiv halten)

- [ ] **Step 1: Failing-Test** — `rust/src/lmd/header.rs` mit Test-Modul:

```rust
//! `@lean-md` header pre-scan (spec §4.1 header parser).
//! The header is consumed by a line-based scan BEFORE rushdown sees the body,
//! so config feeds `EngineContext` and never appears in rendered output.

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn parses_header_and_strips_it() {
        let input = "@lean-md 0.1\nconsumer: ai\nshell: deny\n\n# Body\n";
        let (h, body) = parse_header(input);
        assert_eq!(h.version.as_deref(), Some("0.1"));
        assert_eq!(h.consumer, Consumer::Ai);
        assert_eq!(h.shell, ShellMode::Deny);
        assert_eq!(body, "# Body\n");
    }

    #[test]
    fn no_header_returns_full_input_and_defaults() {
        let input = "# Just markdown\n";
        let (h, body) = parse_header(input);
        assert_eq!(h.version, None);
        assert_eq!(h.consumer, Consumer::Ai);
        assert_eq!(body, input);
    }

    #[test]
    fn consumer_human_and_shell_allow_parse() {
        let (h, _) = parse_header("@lean-md\nconsumer: human\nshell: allow\n\nx\n");
        assert_eq!(h.consumer, Consumer::Human);
        assert_eq!(h.shell, ShellMode::Allow);
    }
}
```

- [ ] **Step 2: Test scheitert**

Run: `cargo nextest run -p lean-ctx lmd::header`
Expected: FAIL (cannot find `parse_header` / `LeanMdHeader`).

- [ ] **Step 3: Implementierung** — oben in `rust/src/lmd/header.rs` einfügen:

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub enum Consumer {
    #[default]
    Ai,
    Human,
}

impl Consumer {
    fn parse(s: &str) -> Self {
        match s.trim() {
            "human" => Consumer::Human,
            _ => Consumer::Ai,
        }
    }
}

#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub enum ShellMode {
    #[default]
    Deny,
    Allow,
}

impl ShellMode {
    fn parse(s: &str) -> Self {
        match s.trim() {
            "allow" => ShellMode::Allow,
            _ => ShellMode::Deny,
        }
    }
}

/// Parsed `@lean-md` header config (Phase 1 minimal set).
#[derive(Debug, Clone, Default)]
pub struct LeanMdHeader {
    pub version: Option<String>,
    pub consumer: Consumer,
    pub shell: ShellMode,
}

/// Line-based pre-scan. If the first non-blank line starts with `@lean-md`,
/// consume that line plus following `key: value` lines up to the first blank
/// line; return the parsed header and the remaining body. Otherwise: default
/// header and the input unchanged.
pub fn parse_header(input: &str) -> (LeanMdHeader, &str) {
    let mut header = LeanMdHeader::default();
    let first = input.lines().next().unwrap_or("");
    if !first.trim_start().starts_with("@lean-md") {
        return (header, input);
    }

    let mut offset = 0usize;
    let mut body_start = input.len();
    for line in input.split_inclusive('\n') {
        let len = line.len();
        let trimmed = line.trim();
        offset += len;
        if trimmed.is_empty() {
            body_start = offset;
            break;
        }
        if let Some(rest) = trimmed.strip_prefix("@lean-md") {
            let v = rest.trim().trim_start_matches('v');
            if !v.is_empty() {
                header.version = Some(v.to_string());
            }
        } else if let Some((k, v)) = trimmed.split_once(':') {
            match k.trim() {
                "consumer" => header.consumer = Consumer::parse(v),
                "shell" => header.shell = ShellMode::parse(v),
                _ => {}
            }
        }
    }
    (header, &input[body_start..])
}
```

- [ ] **Step 4: `pub mod header;` aktiv** in `rust/src/lmd/mod.rs` (falls nicht schon).

- [ ] **Step 5: Test grün**

Run: `cargo nextest run -p lean-ctx lmd::header`
Expected: PASS (3 tests).

- [ ] **Step 6: Reformat + Commit**

Reformat: `rust/src/lmd/header.rs`.

```bash
git add rust/src/lmd/header.rs rust/src/lmd/mod.rs
git commit -m "feat(lmd): @lean-md header pre-scan (phase 1)"
```

---

### Task 3: `FragmentRegistry` (built-in-first) (sonnet)

**Files:**
- Create: `rust/src/lmd/fragments.rs`
- Modify: `rust/src/lmd/mod.rs` (`pub mod fragments;`)

**Hinweis:** `FragmentRegistry::resolve` gibt `Result<String, BridgeError>` zurück. `BridgeError` entsteht erst in Task 7. Um Task-Reihenfolge nicht zu brechen, definiert dieser Task einen **lokalen** Fehlertyp `ResolveError` und Task 9 (IncludeBridge) mappt ihn auf `BridgeError`. So bleibt jeder Task eigenständig kompilierbar.

- [ ] **Step 1: Failing-Test** — `rust/src/lmd/fragments.rs`:

```rust
//! Fragment resolution: built-in-first, then a jailed `*.lmd.md` file fallback
//! (spec §3.3). Built-ins carry the canonical, logic-stable fragments (e.g. the
//! tool-discipline hard-rules) with zero disk cost; files override/extend them.

#[cfg(test)]
mod tests {
    use super::*;
    use std::path::Path;

    #[test]
    fn builtin_resolves_before_file() {
        let reg = FragmentRegistry::with_builtins();
        let out = reg.resolve("hard-rules", Path::new(".")).unwrap();
        assert!(out.contains("lean-ctx"), "built-in hard-rules must mention lean-ctx");
    }

    #[test]
    fn unknown_fragment_errors() {
        let reg = FragmentRegistry::with_builtins();
        let err = reg.resolve("does-not-exist", Path::new(".")).unwrap_err();
        matches!(err, ResolveError::NotFound(_));
    }

    #[test]
    fn file_fallback_within_jail() {
        let dir = std::env::temp_dir().join("lmd_frag_test");
        std::fs::create_dir_all(&dir).unwrap();
        std::fs::write(dir.join("greeting.lmd.md"), "hello from file\n").unwrap();
        let reg = FragmentRegistry::with_builtins();
        let out = reg.resolve("greeting", &dir).unwrap();
        assert_eq!(out, "hello from file\n");
    }

    #[test]
    fn jail_blocks_escape() {
        let reg = FragmentRegistry::with_builtins();
        let err = reg.resolve("../etc/passwd", Path::new(".")).unwrap_err();
        matches!(err, ResolveError::Jail(_));
    }
}
```

- [ ] **Step 2: Test scheitert**

Run: `cargo nextest run -p lean-ctx lmd::fragments`
Expected: FAIL (cannot find `FragmentRegistry`).

- [ ] **Step 3: Implementierung** — oben in `rust/src/lmd/fragments.rs`:

```rust
use std::collections::HashMap;
use std::path::Path;

/// Built-in `hard-rules` fragment — the canonical tool-discipline block that
/// goes into every dispatch (spec §3.3/§3.5). Kept short on purpose.
const HARD_RULES: &str = "\
# Hard Rules (lmd built-in)
- I/O only via lean-ctx MCP tools (ctx_read/ctx_search/ctx_tree/ctx_shell), serena (symbols), jetbrains (reformat).
- Never use native Read/Grep/cat/sed; never `ctx_shell raw=true` unless compression is provably wrong.
- Edit *.rs only via serena symbol tools.
";

#[derive(Debug)]
pub enum ResolveError {
    NotFound(String),
    Jail(String),
    Io(String),
}

/// Built-in-first fragment registry. `resolve` checks the embedded built-ins,
/// then falls back to a jailed `<name>.lmd.md` file under `jail_root`.
pub struct FragmentRegistry {
    builtins: HashMap<&'static str, &'static str>,
}

impl FragmentRegistry {
    pub fn with_builtins() -> Self {
        let mut builtins = HashMap::new();
        builtins.insert("hard-rules", HARD_RULES);
        Self { builtins }
    }

    pub fn resolve(&self, name: &str, jail_root: &Path) -> Result<String, ResolveError> {
        if let Some(content) = self.builtins.get(name) {
            return Ok((*content).to_string());
        }
        let candidate = jail_root.join(format!("{name}.lmd.md"));
        let jail = jail_root
            .canonicalize()
            .map_err(|e| ResolveError::Jail(format!("jail root: {e}")))?;
        let resolved = candidate
            .canonicalize()
            .map_err(|_| ResolveError::NotFound(name.to_string()))?;
        if !resolved.starts_with(&jail) {
            return Err(ResolveError::Jail(format!("{name} escapes jail")));
        }
        std::fs::read_to_string(&resolved).map_err(|e| ResolveError::Io(e.to_string()))
    }
}
```

- [ ] **Step 4: Test grün**

Run: `cargo nextest run -p lean-ctx lmd::fragments`
Expected: PASS (4 tests).

- [ ] **Step 5: Reformat + Commit**

```bash
git add rust/src/lmd/fragments.rs rust/src/lmd/mod.rs
git commit -m "feat(lmd): built-in-first FragmentRegistry with jailed file fallback (phase 1)"
```

---

### Task 4: AST-Knoten `LmdDirective` + `LmdInline` (opus)

**Files:**
- Create: `rust/src/lmd/node.rs`
- Modify: `rust/src/lmd/mod.rs` (`pub mod node;`)

**Vorlage:** `rust/tests/lmd_rushdown_spike.rs` `UpperBlock` (Block) + `ShoutInline` (Inline). Wir speichern owned `String` (transformierter, nicht verbatim-Inhalt) wie `UpperBlock`.

- [ ] **Step 1: Failing-Test** — `rust/src/lmd/node.rs` mit Test-Modul:

```rust
//! lmd AST nodes. `LmdDirective` is a leaf BLOCK node (`@name args`);
//! `LmdInline` is an INLINE node (`{{ name args }}`). Both store the parsed
//! name + raw args as owned strings; the renderer dispatches them into the
//! bridge registry at render time. Mirrors the Phase-0 spike's UpperBlock/ShoutInline.

#[cfg(test)]
mod tests {
    use super::*;
    use rushdown::ast::{NodeKind, NodeType};

    #[test]
    fn directive_node_reports_leaf_block() {
        let n = LmdDirective::new("read".to_string(), "x.rs mode=full".to_string());
        assert_eq!(n.typ(), NodeType::LeafBlock);
        assert_eq!(n.kind_name(), "LmdDirective");
        assert_eq!(n.name, "read");
    }

    #[test]
    fn inline_node_reports_inline() {
        let n = LmdInline::new("include".to_string(), "hard-rules".to_string());
        assert_eq!(n.typ(), NodeType::Inline);
        assert_eq!(n.kind_name(), "LmdInline");
    }
}
```

- [ ] **Step 2: Test scheitert**

Run: `cargo nextest run -p lean-ctx lmd::node`
Expected: FAIL (cannot find `LmdDirective`).

- [ ] **Step 3: Implementierung** — oben in `rust/src/lmd/node.rs`:

```rust
use core::fmt::{self, Write};

use rushdown::ast::*;

/// Leaf-block node for an `@name args` directive line.
#[derive(Debug)]
pub struct LmdDirective {
    pub name: String,
    pub args: String,
}

impl LmdDirective {
    pub fn new(name: String, args: String) -> Self {
        Self { name, args }
    }
}

impl NodeKind for LmdDirective {
    fn typ(&self) -> NodeType {
        NodeType::LeafBlock
    }
    fn kind_name(&self) -> &'static str {
        "LmdDirective"
    }
}

impl PrettyPrint for LmdDirective {
    fn pretty_print(&self, w: &mut dyn Write, _source: &str, level: usize) -> fmt::Result {
        writeln!(w, "{}LmdDirective: @{} {}", pp_indent(level), self.name, self.args)
    }
}

impl From<LmdDirective> for KindData {
    fn from(e: LmdDirective) -> Self {
        KindData::Extension(Box::new(e))
    }
}

/// Inline node for a `{{ name args }}` directive.
#[derive(Debug)]
pub struct LmdInline {
    pub name: String,
    pub args: String,
}

impl LmdInline {
    pub fn new(name: String, args: String) -> Self {
        Self { name, args }
    }
}

impl NodeKind for LmdInline {
    fn typ(&self) -> NodeType {
        NodeType::Inline
    }
    fn kind_name(&self) -> &'static str {
        "LmdInline"
    }
}

impl PrettyPrint for LmdInline {
    fn pretty_print(&self, w: &mut dyn Write, _source: &str, level: usize) -> fmt::Result {
        writeln!(w, "{}LmdInline: {{{{ {} {} }}}}", pp_indent(level), self.name, self.args)
    }
}

impl From<LmdInline> for KindData {
    fn from(e: LmdInline) -> Self {
        KindData::Extension(Box::new(e))
    }
}
```

> Falls `pp_indent`, `NodeType`, `KindData`, `NodeKind` oder `PrettyPrint` nicht aufgelöst werden: prüfe die exakten Re-Export-Pfade gegen `rust/tests/lmd_rushdown_spike.rs` Zeilen 21–39 (`use rushdown::ast::*;`). Die Namen sind dort verifiziert.

- [ ] **Step 4: Test grün**

Run: `cargo nextest run -p lean-ctx lmd::node`
Expected: PASS (2 tests).

- [ ] **Step 5: Reformat + Commit**

```bash
git add rust/src/lmd/node.rs rust/src/lmd/mod.rs
git commit -m "feat(lmd): LmdDirective + LmdInline AST nodes (phase 1)"
```

---

### Task 5: Block-Parser + reiner `parse_directive_line` (opus)

**Files:**
- Create: `rust/src/lmd/parser/mod.rs`
- Create: `rust/src/lmd/parser/block.rs`
- Modify: `rust/src/lmd/mod.rs` (`pub mod parser;`)

**Vorlage:** Spike `UpperBlockParser` (`trigger() -> b"@"`, Methode `open` — NICHT `parse`, `can_interrupt_paragraph() -> true`, `State::NO_CHILDREN`, `reader.advance_to_eol()`, `From<P> for AnyBlockParser`).

- [ ] **Step 1: Failing-Test** — den reinen Helper testen wir direkt (das rushdown-Glue wird im Gate-Task e2e geprüft). `rust/src/lmd/parser/block.rs` Test-Modul:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn parses_name_and_args() {
        let r = parse_directive_line(b"@read path/to/x.rs mode=full\n").unwrap();
        assert_eq!(r.0, "read");
        assert_eq!(r.1, "path/to/x.rs mode=full");
    }

    #[test]
    fn parses_bare_directive() {
        let r = parse_directive_line(b"@include hard-rules\n").unwrap();
        assert_eq!(r.0, "include");
        assert_eq!(r.1, "hard-rules");
    }

    #[test]
    fn rejects_non_directive() {
        assert!(parse_directive_line(b"plain text\n").is_none());
        assert!(parse_directive_line(b"@\n").is_none());
        assert!(parse_directive_line(b"@1bad name\n").is_none());
    }
}
```

- [ ] **Step 2: Test scheitert**

Run: `cargo nextest run -p lean-ctx lmd::parser::block`
Expected: FAIL (cannot find `parse_directive_line`).

- [ ] **Step 3: Implementierung** — `rust/src/lmd/parser/block.rs`:

```rust
//! Block parser: claims `@<name> [args]` lines into an `LmdDirective` node.
//! Stateless (like the Phase-0 spike); bridge dispatch happens at render time.

use rushdown::ast::*;
use rushdown::parser;
use rushdown::parser::*;
use rushdown::text;

use super::super::node::LmdDirective;

/// Pure syntax recognizer: `@<ident> [args]\n` -> (name, args).
/// `ident` = ascii-alpha start, then `[a-z0-9-]`. Returns None for non-matches
/// so normal text/`@`-lines that aren't directives pass through untouched.
pub fn parse_directive_line(line: &[u8]) -> Option<(String, String)> {
    let rest = line.strip_prefix(b"@")?;
    let first = *rest.first()?;
    if !first.is_ascii_alphabetic() {
        return None;
    }
    let name_len = rest
        .iter()
        .position(|b| !(b.is_ascii_alphanumeric() || *b == b'-'))
        .unwrap_or(rest.len());
    if name_len == 0 {
        return None;
    }
    let name = String::from_utf8_lossy(&rest[..name_len]).to_string();
    let args_raw = &rest[name_len..];
    let args = String::from_utf8_lossy(args_raw)
        .trim_matches(|c: char| c.is_whitespace())
        .to_string();
    Some((name, args))
}

#[derive(Debug, Default)]
pub struct LmdBlockParser {}

impl LmdBlockParser {
    pub fn new() -> Self {
        Self::default()
    }
}

impl BlockParser for LmdBlockParser {
    fn trigger(&self) -> &[u8] {
        b"@"
    }

    fn open(
        &self,
        arena: &mut Arena,
        _parent_ref: NodeRef,
        reader: &mut text::BasicReader,
        _ctx: &mut parser::ctx,
    ) -> Option<(NodeRef, State)> {
        let (line, _seg) = reader.peek_line_bytes()?;
        let (name, args) = parse_directive_line(line)?;
        reader.advance_to_eol();
        Some((arena.new_node(LmdDirective::new(name, args)), State::NO_CHILDREN))
    }

    fn cont(
        &self,
        _arena: &mut Arena,
        _node_ref: NodeRef,
        _reader: &mut text::BasicReader,
        _ctx: &mut parser::ctx,
    ) -> Option<State> {
        None
    }

    fn can_interrupt_paragraph(&self) -> bool {
        true
    }
}

impl From<LmdBlockParser> for AnyBlockParser {
    fn from(p: LmdBlockParser) -> Self {
        AnyBlockParser::Extension(Box::new(p))
    }
}
```

- [ ] **Step 4: Parser-Wiring-Stub** — `rust/src/lmd/parser/mod.rs`:

```rust
//! lmd rushdown parser extension wiring (block + inline parsers).

pub mod block;
pub mod inline;

use rushdown::parser::*;

use block::LmdBlockParser;
use inline::LmdInlineParser;

/// Registers both lmd parsers. Block sits at heading priority (claim `@`-lines
/// before they become paragraphs); inline runs after emphasis (the spike's
/// `PRIORITY_EMPHASIS + 100`); `{` is not a CommonMark inline trigger.
pub fn lmd_parser_extension() -> impl ParserExtension {
    parser_extension(|p| {
        p.add_block_parser(LmdBlockParser::new, NoParserOptions, PRIORITY_ATX_HEADING);
        p.add_inline_parser(LmdInlineParser::new, NoParserOptions, PRIORITY_EMPHASIS + 100);
    })
}
```

> `inline` wird in Task 6 angelegt. Falls du strikt grün halten willst: kommentiere die `inline`-Zeilen hier aus, bis Task 6 fertig ist, oder ziehe Task 6 vor. Empfehlung: Task 5 und 6 als Paar fahren.

- [ ] **Step 5: Test grün** (Helper)

Run: `cargo nextest run -p lean-ctx lmd::parser::block`
Expected: PASS (3 tests). (Kompiliert erst vollständig, wenn `inline.rs` existiert — siehe Hinweis.)

- [ ] **Step 6: Reformat + Commit** (nach Task 6, gemeinsam — oder einzeln wenn auskommentiert)

```bash
git add rust/src/lmd/parser/mod.rs rust/src/lmd/parser/block.rs rust/src/lmd/mod.rs
git commit -m "feat(lmd): block directive parser (phase 1)"
```

---

### Task 6: Inline-Parser + reiner `parse_inline_body` (opus)

**Files:**
- Create: `rust/src/lmd/parser/inline.rs`

**Vorlage:** Spike `ShoutInline`/`ShoutInlineParser` — `trigger() -> b"{"`, Methode `parse(...) -> Option<NodeRef>`, **Dispatcher konsumiert das Trigger-Byte NICHT** → selbst `reader.advance(consumed)`. `From<P> for AnyInlineParser`.

- [ ] **Step 1: Failing-Test** — `rust/src/lmd/parser/inline.rs` Test-Modul:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn parses_inline_name_and_args() {
        let r = parse_inline_body("include hard-rules").unwrap();
        assert_eq!(r.0, "include");
        assert_eq!(r.1, "hard-rules");
    }

    #[test]
    fn parses_inline_name_only() {
        let r = parse_inline_body("date").unwrap();
        assert_eq!(r.0, "date");
        assert_eq!(r.1, "");
    }

    #[test]
    fn rejects_empty() {
        assert!(parse_inline_body("   ").is_none());
    }
}
```

- [ ] **Step 2: Test scheitert**

Run: `cargo nextest run -p lean-ctx lmd::parser::inline`
Expected: FAIL (cannot find `parse_inline_body`).

- [ ] **Step 3: Implementierung** — `rust/src/lmd/parser/inline.rs`:

```rust
//! Inline parser: claims `{{ name args }}` into an `LmdInline` node.
//! Mirrors the Phase-0 spike's ShoutInline (trigger `{`, manual advance of the
//! full match because the inline dispatcher does not pre-consume the trigger).

use rushdown::ast::*;
use rushdown::parser;
use rushdown::parser::*;
use rushdown::text;

use super::super::node::LmdInline;

/// Pure recognizer for the body between `{{ ` and ` }}`: first token is the
/// directive name, the remainder (trimmed) is the args. Returns None if empty.
pub fn parse_inline_body(body: &str) -> Option<(String, String)> {
    let body = body.trim();
    if body.is_empty() {
        return None;
    }
    match body.split_once(char::is_whitespace) {
        Some((name, args)) => Some((name.to_string(), args.trim().to_string())),
        None => Some((body.to_string(), String::new())),
    }
}

#[derive(Debug, Default)]
pub struct LmdInlineParser {}

impl LmdInlineParser {
    pub fn new() -> Self {
        Self::default()
    }
}

impl InlineParser for LmdInlineParser {
    fn trigger(&self) -> &[u8] {
        b"{"
    }

    fn parse(
        &self,
        arena: &mut Arena,
        _parent_ref: NodeRef,
        reader: &mut text::BlockReader,
        _ctx: &mut parser::ctx,
    ) -> Option<NodeRef> {
        let (line, _seg) = reader.peek_line_bytes()?;
        const OPEN: &[u8] = b"{{ ";
        const CLOSE: &[u8] = b" }}";
        if !line.starts_with(OPEN) {
            return None;
        }
        let body_start = OPEN.len();
        let mut i = body_start;
        let close_at = loop {
            if i + CLOSE.len() > line.len() {
                return None;
            }
            if &line[i..i + CLOSE.len()] == CLOSE {
                break i;
            }
            i += 1;
        };
        let body = String::from_utf8_lossy(&line[body_start..close_at]).to_string();
        let (name, args) = parse_inline_body(&body)?;
        let consumed = close_at + CLOSE.len();
        reader.advance(consumed);
        Some(arena.new_node(LmdInline::new(name, args)))
    }
}

impl From<LmdInlineParser> for AnyInlineParser {
    fn from(p: LmdInlineParser) -> Self {
        AnyInlineParser::Extension(Box::new(p))
    }
}
```

- [ ] **Step 4: Test grün**

Run: `cargo nextest run -p lean-ctx lmd::parser`
Expected: PASS (block + inline helper tests).

- [ ] **Step 5: Reformat + Commit**

```bash
git add rust/src/lmd/parser/inline.rs rust/src/lmd/parser/mod.rs rust/src/lmd/parser/block.rs rust/src/lmd/mod.rs
git commit -m "feat(lmd): inline directive parser + parser extension wiring (phase 1)"
```

---

### Task 7: `DirectiveBridge` Trait + `BridgeRegistry` (sonnet)

**Files:**
- Create: `rust/src/lmd/bridges/mod.rs`
- Modify: `rust/src/lmd/mod.rs` (`pub mod bridges;`)

**Spec §4.2.** Signatur weicht bewusst ab: `execute` bekommt `&Rc<EngineContext>` (statt `&EngineContext`), damit `@include` rekursiv `render_body` aufrufen kann. `EngineContext`/`default_registry` werden erst in Tasks 8–11 vollständig; dieser Task definiert Trait, Fehler und Registry-Hülle.

- [ ] **Step 1: Failing-Test** — `rust/src/lmd/bridges/mod.rs` Test-Modul:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn registry_registers_and_gets() {
        let mut reg = BridgeRegistry::new();
        reg.register(Box::new(read::ReadBridge));
        assert!(reg.get("read").is_some());
        assert!(reg.get("nope").is_none());
    }

    #[test]
    fn default_registry_has_read_and_include() {
        let reg = default_registry();
        assert!(reg.get("read").is_some());
        assert!(reg.get("include").is_some());
    }
}
```

- [ ] **Step 2: Test scheitert**

Run: `cargo nextest run -p lean-ctx lmd::bridges`
Expected: FAIL (missing `BridgeRegistry`, `read`, `default_registry`).

- [ ] **Step 3: Implementierung** — oben in `rust/src/lmd/bridges/mod.rs`:

```rust
//! Directive bridges: each Router directive is a thin bridge into an existing
//! lean-ctx core API (spec §4.2). `execute` takes `&Rc<EngineContext>` so a
//! bridge can re-enter the engine (e.g. `@include` renders its fragment).

pub mod include;
pub mod read;

use std::collections::HashMap;
use std::rc::Rc;

use super::args::DirectiveArgs;
use super::engine::EngineContext;

#[derive(Debug)]
pub enum BridgeError {
    MissingArg(&'static str),
    Resolve(String),
    Io(String),
    DepthExceeded,
}

pub trait DirectiveBridge {
    fn name(&self) -> &'static str;
    fn execute(&self, ctx: &Rc<EngineContext>, args: &DirectiveArgs) -> Result<String, BridgeError>;
}

/// Name-keyed registry of directive bridges.
#[derive(Default)]
pub struct BridgeRegistry {
    map: HashMap<&'static str, Box<dyn DirectiveBridge>>,
}

impl BridgeRegistry {
    pub fn new() -> Self {
        Self { map: HashMap::new() }
    }

    pub fn register(&mut self, bridge: Box<dyn DirectiveBridge>) {
        self.map.insert(bridge.name(), bridge);
    }

    pub fn get(&self, name: &str) -> Option<&dyn DirectiveBridge> {
        self.map.get(name).map(|b| b.as_ref())
    }
}

/// The Phase-1 default registry: `@read` (Router) + `@include` (Extension).
pub fn default_registry() -> BridgeRegistry {
    let mut reg = BridgeRegistry::new();
    reg.register(Box::new(read::ReadBridge));
    reg.register(Box::new(include::IncludeBridge));
    reg
}
```

> `read`/`include`/`engine` existieren nach Tasks 8/9/11. Bis dahin kompiliert dieser Task nur zusammen mit ihnen. Empfehlung: Tasks 7→11 als zusammenhängenden Block fahren und erst nach Task 11 das `cargo nextest run` für `lmd::bridges` ausführen. Commit dieses Tasks erst, wenn der Block kompiliert.

- [ ] **Step 4: (verschoben) Test grün nach Task 11.**

- [ ] **Step 5: Reformat + Commit** (gemeinsam mit Task 11)

---

### Task 8: `ReadBridge` (`@read`) (sonnet)

**Files:**
- Create: `rust/src/lmd/bridges/read.rs`

**Backing (audit.rs):** `@read` → `core::structured_read`/`ctx_read`. Konkret nutzbar: `crate::tools::ctx_read::handle(&mut SessionCache, path, mode, CrpMode)` (Muster verifiziert in `dashboard/routes/tools.rs:233`: `handle(&mut cache, path, "reference", CrpMode::Off)`).

- [ ] **Step 1: Failing-Test** — `rust/src/lmd/bridges/read.rs` Test-Modul:

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::lmd::args::DirectiveArgs;
    use crate::lmd::engine::EngineContext;
    use crate::lmd::header::LeanMdHeader;
    use std::path::PathBuf;
    use std::rc::Rc;

    #[test]
    fn reads_a_file_via_ctx_read() {
        let f = std::env::temp_dir().join("lmd_read_bridge.txt");
        std::fs::write(&f, "SENTINEL_LINE_42\n").unwrap();
        let ctx = Rc::new(EngineContext::new(LeanMdHeader::default(), PathBuf::from(".")));
        let args = DirectiveArgs::parse(f.to_str().unwrap());
        let out = ReadBridge.execute(&ctx, &args).unwrap();
        assert!(out.contains("SENTINEL_LINE_42"), "got: {out}");
    }

    #[test]
    fn missing_path_errors() {
        let ctx = Rc::new(EngineContext::new(LeanMdHeader::default(), PathBuf::from(".")));
        let err = ReadBridge.execute(&ctx, &DirectiveArgs::parse("")).unwrap_err();
        matches!(err, BridgeError::MissingArg(_));
    }
}
```

- [ ] **Step 2: Test scheitert**

Run: `cargo nextest run -p lean-ctx lmd::bridges::read`
Expected: FAIL (cannot find `ReadBridge`).

- [ ] **Step 3: Implementierung** — `rust/src/lmd/bridges/read.rs`:

```rust
//! `@read` Router bridge → the same core as the `ctx_read` MCP tool.

use std::rc::Rc;

use super::{BridgeError, DirectiveBridge};
use crate::lmd::args::DirectiveArgs;
use crate::lmd::engine::EngineContext;

/// `@read <path> [mode=<mode>]` — defaults to `auto`. Phase 1 passes the path
/// through unchanged (jailing `@read` is a §7/Phase-7 concern).
pub struct ReadBridge;

impl DirectiveBridge for ReadBridge {
    fn name(&self) -> &'static str {
        "read"
    }

    fn execute(&self, _ctx: &Rc<EngineContext>, args: &DirectiveArgs) -> Result<String, BridgeError> {
        let path = args
            .positional(0)
            .or_else(|| args.get("path"))
            .ok_or(BridgeError::MissingArg("path"))?;
        let mode = args.get("mode").unwrap_or("auto");
        let mut cache = crate::core::cache::SessionCache::new();
        Ok(crate::tools::ctx_read::handle(&mut cache, path, mode, crate::tools::CrpMode::Off))
    }
}
```

- [ ] **Step 4: (verschoben) Test grün nach Task 11** (braucht `EngineContext`).

- [ ] **Step 5: Reformat + Commit** (gemeinsam mit Task 11)

---

### Task 9: `IncludeBridge` (`@include`) (opus)

**Files:**
- Create: `rust/src/lmd/bridges/include.rs`

**Verhalten:** Fragment via `FragmentRegistry` auflösen (built-in-first), Inhalt **rekursiv** durch `engine::render_body` rendern (Content sichtbar, spec §4.1 `@include`), mit Depth-Guard (`EngineContext::enter`/`leave`, `max_chain_depth=16`, spec §7). `ResolveError` (Task 3) → `BridgeError`.

- [ ] **Step 1: Failing-Test** — `rust/src/lmd/bridges/include.rs` Test-Modul:

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::lmd::args::DirectiveArgs;
    use crate::lmd::engine::EngineContext;
    use crate::lmd::header::LeanMdHeader;
    use std::path::PathBuf;
    use std::rc::Rc;

    #[test]
    fn includes_builtin_fragment() {
        let ctx = Rc::new(EngineContext::new(LeanMdHeader::default(), PathBuf::from(".")));
        let out = IncludeBridge
            .execute(&ctx, &DirectiveArgs::parse("hard-rules"))
            .unwrap();
        assert!(out.contains("lean-ctx"), "included hard-rules must render; got: {out}");
    }

    #[test]
    fn missing_name_errors() {
        let ctx = Rc::new(EngineContext::new(LeanMdHeader::default(), PathBuf::from(".")));
        let err = IncludeBridge.execute(&ctx, &DirectiveArgs::parse("")).unwrap_err();
        matches!(err, BridgeError::MissingArg(_));
    }
}
```

- [ ] **Step 2: Test scheitert**

Run: `cargo nextest run -p lean-ctx lmd::bridges::include`
Expected: FAIL (cannot find `IncludeBridge`).

- [ ] **Step 3: Implementierung** — `rust/src/lmd/bridges/include.rs`:

```rust
//! `@include` extension bridge: resolve a fragment (built-in-first) and render
//! its content recursively (content visible), guarded by the engine depth chain.

use std::rc::Rc;

use super::{BridgeError, DirectiveBridge};
use crate::lmd::args::DirectiveArgs;
use crate::lmd::engine::{render_body, EngineContext};
use crate::lmd::fragments::ResolveError;

pub struct IncludeBridge;

impl From<ResolveError> for BridgeError {
    fn from(e: ResolveError) -> Self {
        match e {
            ResolveError::NotFound(n) => BridgeError::Resolve(format!("fragment not found: {n}")),
            ResolveError::Jail(m) => BridgeError::Resolve(format!("jail: {m}")),
            ResolveError::Io(m) => BridgeError::Io(m),
        }
    }
}

impl DirectiveBridge for IncludeBridge {
    fn name(&self) -> &'static str {
        "include"
    }

    fn execute(&self, ctx: &Rc<EngineContext>, args: &DirectiveArgs) -> Result<String, BridgeError> {
        let name = args.positional(0).ok_or(BridgeError::MissingArg("name"))?;
        let content = ctx.fragments.resolve(name, &ctx.jail_root)?;
        ctx.enter()?;
        let rendered = render_body(ctx, &content);
        ctx.leave();
        Ok(rendered)
    }
}
```

- [ ] **Step 4: (verschoben) Test grün nach Task 11.**

- [ ] **Step 5: Reformat + Commit** (gemeinsam mit Task 11)

---

### Task 10: Renderer-Hook + `LmdRendererOptions` (opus)

**Files:**
- Create: `rust/src/lmd/render.rs`
- Modify: `rust/src/lmd/mod.rs` (`pub mod render;`)

**Vorlage:** Spike `UpperBlockHtmlRenderer`/`ShoutInlineHtmlRenderer` — `with_options(html_opts, options)`, `RenderNode::render_node`, `as_extension_data!`, `NodeRenderer::register_node_renderer_fn`, `renderer_extension(|r| r.add_node_renderer(...))`. **Neu ggü. Spike:** `LmdRendererOptions` trägt `Rc<EngineContext>` in den Hook (rushdowns `RendererOptions` ist ein leerer Marker-Trait — verifiziert in `rushdown-0.18.0/src/renderer/mod.rs:191`).

- [ ] **Step 1: Implementierung (kein eigener Unit-Test — e2e in Task 11/12)** — `rust/src/lmd/render.rs`:

```rust
//! Render-time bridge dispatch. The renderer hook carries `Rc<EngineContext>`
//! (incl. the bridge registry) via a custom `LmdRendererOptions` (rushdown's
//! `RendererOptions` is an empty marker trait), then dispatches each lmd node
//! into the registry. Phase 1 writes bridge output raw (HTML-escaping/output
//! target deferred). Mirrors the Phase-0 spike renderer structs.

use core::any::TypeId;
use std::rc::Rc;

use rushdown::ast::*;
use rushdown::renderer;
use rushdown::renderer::html::{renderer_extension, Options, RendererExtension};
use rushdown::renderer::*;
use rushdown::renderer::html;
use rushdown::text::*;
use rushdown::{as_extension_data, Result};

use super::args::DirectiveArgs;
use super::engine::EngineContext;
use super::node::{LmdDirective, LmdInline};

/// Carries the engine context into the render hook.
pub struct LmdRendererOptions {
    pub ctx: Rc<EngineContext>,
}

impl RendererOptions for LmdRendererOptions {}

/// Look up `name` in the registry and run the bridge; on miss/error emit a
/// visible comment instead of failing the whole render.
fn dispatch(ctx: &Rc<EngineContext>, name: &str, raw_args: &str) -> String {
    let args = DirectiveArgs::parse(raw_args);
    match ctx.registry.get(name) {
        Some(bridge) => match bridge.execute(ctx, &args) {
            Ok(out) => out,
            Err(e) => format!("<!-- lmd:@{name} error: {e:?} -->"),
        },
        None => format!("<!-- lmd: unknown directive @{name} -->"),
    }
}

// --- block node renderer ---------------------------------------------------

pub struct LmdDirectiveRenderer<W: TextWrite> {
    _phantom: core::marker::PhantomData<W>,
    writer: html::Writer,
    ctx: Rc<EngineContext>,
}

impl<W: TextWrite> LmdDirectiveRenderer<W> {
    fn with_options(html_opts: Options, options: LmdRendererOptions) -> Self {
        Self {
            _phantom: core::marker::PhantomData,
            writer: html::Writer::with_options(html_opts),
            ctx: options.ctx,
        }
    }
}

impl<W: TextWrite> RenderNode<W> for LmdDirectiveRenderer<W> {
    fn render_node<'a>(
        &mut self,
        w: &mut W,
        arena: &Arena,
        _source: &'a str,
        node_ref: NodeRef,
        entering: bool,
        _context: &mut renderer::ctx,
    ) -> Result<WalkStatus> {
        if entering {
            let d = as_extension_data!(arena, node_ref, LmdDirective);
            let out = dispatch(&self.ctx, &d.name, &d.args);
            self.writer.write_safe_str(w, &out)?;
        }
        Ok(WalkStatus::Continue)
    }
}

impl<'cb, W> NodeRenderer<'cb, W> for LmdDirectiveRenderer<W>
where
    W: TextWrite + 'cb,
{
    fn register_node_renderer_fn(self, nrr: &mut impl NodeRendererRegistry<'cb, W>) {
        nrr.register_node_renderer_fn(TypeId::of::<LmdDirective>(), BoxRenderNode::new(self));
    }
}

// --- inline node renderer --------------------------------------------------

pub struct LmdInlineRenderer<W: TextWrite> {
    _phantom: core::marker::PhantomData<W>,
    writer: html::Writer,
    ctx: Rc<EngineContext>,
}

impl<W: TextWrite> LmdInlineRenderer<W> {
    fn with_options(html_opts: Options, options: LmdRendererOptions) -> Self {
        Self {
            _phantom: core::marker::PhantomData,
            writer: html::Writer::with_options(html_opts),
            ctx: options.ctx,
        }
    }
}

impl<W: TextWrite> RenderNode<W> for LmdInlineRenderer<W> {
    fn render_node<'a>(
        &mut self,
        w: &mut W,
        arena: &Arena,
        _source: &'a str,
        node_ref: NodeRef,
        entering: bool,
        _context: &mut renderer::ctx,
    ) -> Result<WalkStatus> {
        if entering {
            let d = as_extension_data!(arena, node_ref, LmdInline);
            let out = dispatch(&self.ctx, &d.name, &d.args);
            self.writer.write_safe_str(w, &out)?;
        }
        Ok(WalkStatus::Continue)
    }
}

impl<'cb, W> NodeRenderer<'cb, W> for LmdInlineRenderer<W>
where
    W: TextWrite + 'cb,
{
    fn register_node_renderer_fn(self, nrr: &mut impl NodeRendererRegistry<'cb, W>) {
        nrr.register_node_renderer_fn(TypeId::of::<LmdInline>(), BoxRenderNode::new(self));
    }
}

/// Registers both lmd node renderers, each carrying a clone of the engine ctx.
pub fn lmd_renderer_extension<'cb, W>(ctx: Rc<EngineContext>) -> impl RendererExtension<'cb, W>
where
    W: TextWrite + 'cb,
{
    renderer_extension(move |r| {
        r.add_node_renderer(
            LmdDirectiveRenderer::with_options,
            LmdRendererOptions { ctx: ctx.clone() },
        );
        r.add_node_renderer(
            LmdInlineRenderer::with_options,
            LmdRendererOptions { ctx: ctx.clone() },
        );
    })
}
```

> Die exakte `render_node`-Signatur (Argument-Reihenfolge `&mut self, w, arena, source, node_ref, entering, context`) ist im Spike `UpperBlockHtmlRenderer::render_node` verifiziert — **1:1 übernehmen**, nicht raten. Falls `write_safe_str` HTML-escaped statt roh schreibt: gegen den Spike prüfen (dort schreibt `write_safe_str(w, "<p>")` literal `<p>`, also roh — korrekt für unseren Bridge-Output).

- [ ] **Step 2: Kompiliert (zusammen mit Task 11).** Eigener Behavior-Test folgt im Gate (Task 11/12).

- [ ] **Step 3: Reformat + Commit** (gemeinsam mit Task 11)

---

### Task 11: `EngineContext` + `render`/`render_body` Wiring + e2e-Unit-Tests (opus)

**Files:**
- Create: `rust/src/lmd/engine.rs`
- Modify: `rust/src/lmd/mod.rs` (`pub mod engine;`)

Dieser Task schließt den Kreis: er macht den in Tasks 7–10 begonnenen Block kompilierbar und liefert die ersten echten e2e-Tests (`@read`, `@include`, Header-Strip).

- [ ] **Step 1: Implementierung** — `rust/src/lmd/engine.rs` (oberer Teil):

```rust
//! Engine wiring: header pre-scan → build the rushdown render closure with the
//! lmd parser + renderer extensions → render the body. `render_body` is the
//! re-entry point used by `@include` for recursive fragment rendering.

use std::cell::Cell;
use std::path::PathBuf;
use std::rc::Rc;

use rushdown::new_markdown_to_html;

use super::bridges::{default_registry, BridgeError, BridgeRegistry};
use super::fragments::FragmentRegistry;
use super::header::{parse_header, LeanMdHeader};
use super::parser::lmd_parser_extension;
use super::render::lmd_renderer_extension;

/// Per-render engine state shared (via `Rc`) with the renderer hook and bridges.
/// Single-threaded within one render call, so `Cell`/`Rc` are sufficient.
pub struct EngineContext {
    pub header: LeanMdHeader,
    pub jail_root: PathBuf,
    pub fragments: FragmentRegistry,
    pub registry: BridgeRegistry,
    pub max_chain_depth: usize,
    depth: Cell<usize>,
}

impl EngineContext {
    pub fn new(header: LeanMdHeader, jail_root: PathBuf) -> Self {
        Self {
            header,
            jail_root,
            fragments: FragmentRegistry::with_builtins(),
            registry: default_registry(),
            max_chain_depth: 16,
            depth: Cell::new(0),
        }
    }

    /// Increment the include-chain depth; error past `max_chain_depth` (§7).
    pub fn enter(&self) -> Result<(), BridgeError> {
        let d = self.depth.get();
        if d >= self.max_chain_depth {
            return Err(BridgeError::DepthExceeded);
        }
        self.depth.set(d + 1);
        Ok(())
    }

    pub fn leave(&self) {
        self.depth.set(self.depth.get().saturating_sub(1));
    }
}

/// Top-level entry: pre-scan the `@lean-md` header, then render the body.
pub fn render(input: &str) -> String {
    let (header, body) = parse_header(input);
    let ctx = Rc::new(EngineContext::new(header, PathBuf::from(".")));
    render_body(&ctx, body)
}

/// Build a fresh rushdown render closure wired with the lmd extensions and
/// render `body`. Re-entrant: `@include` calls this for fragment content.
pub fn render_body(ctx: &Rc<EngineContext>, body: &str) -> String {
    let render = new_markdown_to_html(
        rushdown::parser::Options::default(),
        rushdown::renderer::html::Options::default(),
        lmd_parser_extension(),
        lmd_renderer_extension(ctx.clone()),
    );
    let mut out = String::new();
    let _ = render(&mut out, body);
    out
}
```

- [ ] **Step 2: e2e-Unit-Tests** — am Ende von `rust/src/lmd/engine.rs` anfügen:

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn renders_include_builtin() {
        let out = render("@include hard-rules\n");
        assert!(out.contains("lean-ctx"), "got: {out}");
    }

    #[test]
    fn renders_read_directive() {
        let f = std::env::temp_dir().join("lmd_engine_read.txt");
        std::fs::write(&f, "ENGINE_SENTINEL_7\n").unwrap();
        let out = render(&format!("@read {}\n", f.to_str().unwrap()));
        assert!(out.contains("ENGINE_SENTINEL_7"), "got: {out}");
    }

    #[test]
    fn header_is_stripped_from_output() {
        let out = render("@lean-md 0.1\nconsumer: ai\n\n@include hard-rules\n");
        assert!(!out.contains("@lean-md"), "header must not appear; got: {out}");
        assert!(out.contains("lean-ctx"));
    }

    #[test]
    fn unknown_directive_renders_comment() {
        let out = render("@frobnicate x\n");
        assert!(out.contains("unknown directive @frobnicate"), "got: {out}");
    }

    #[test]
    fn inline_include_dispatches() {
        let out = render("rules: {{ include hard-rules }}\n");
        assert!(out.contains("lean-ctx"), "inline dispatch must fire; got: {out}");
    }
}
```

- [ ] **Step 3: Alle lmd-Tests grün** (der ganze Block 7–11 kompiliert jetzt)

Run: `cargo nextest run -p lean-ctx lmd`
Expected: PASS — inkl. der zuvor verschobenen Tests aus Tasks 7, 8, 9.

> Falls Compile-Fehler aus der rushdown-Extension-Verdrahtung kommen: Datei für Datei gegen `rust/tests/lmd_rushdown_spike.rs` abgleichen (Trait-Methodennamen, `From`-Impls, `add_block_parser`/`add_inline_parser`/`add_node_renderer`-Signaturen). Der Spike ist die Ground Truth. Bei `RendererOptions`/Options-Extraktion: leerer Marker-Trait, `LmdRendererOptions` wird per Wert in `with_options` durchgereicht — verifiziert in `rushdown-0.18.0/src/renderer/mod.rs:520` (`ropt: Cell::new(Some(ropt))`).

- [ ] **Step 4: Reformat + Commit** (Block 7–11 gemeinsam)

Reformat: `mcp__jetbrains__reformat_file` auf `engine.rs`, `render.rs`, `bridges/mod.rs`, `bridges/read.rs`, `bridges/include.rs`, `mod.rs`.

```bash
git add rust/src/lmd/engine.rs rust/src/lmd/render.rs rust/src/lmd/bridges rust/src/lmd/mod.rs
git commit -m "feat(lmd): engine wiring + bridge registry + @read/@include bridges + render hook (phase 1)"
```

---

### Task 12: Phase-1-Gate Integrationstest (sonnet)

**Files:**
- Create: `rust/tests/lmd_phase1_gate.rs`

Spiegelt `rust/tests/lmd_rushdown_spike.rs` als Integrationstest (Crate von außen als `lean_ctx`). Belegt das §6-Gate: `@lean-md`, `@include`, `@read` rendern e2e.

- [ ] **Step 1: Test schreiben** — `rust/tests/lmd_phase1_gate.rs`:

```rust
//! Phase-1 gate (spec §6): `@lean-md` header + `@include` + an `@read` R-router
//! render end-to-end through the native lmd rushdown extension.

use lean_ctx::lmd::engine::render;

#[test]
fn gate_header_include_read_render_e2e() {
    let f = std::env::temp_dir().join("lmd_gate_fixture.txt");
    std::fs::write(&f, "GATE_FIXTURE_CONTENT\n").unwrap();

    let input = format!(
        "@lean-md 0.1\nconsumer: ai\n\n@include hard-rules\n\n@read {}\n",
        f.to_str().unwrap()
    );
    let out = render(&input);

    // header consumed
    assert!(!out.contains("@lean-md"), "header leaked: {out}");
    // @include rendered the built-in hard-rules fragment
    assert!(out.contains("lean-ctx"), "include did not render: {out}");
    // @read routed into ctx_read and surfaced the file content
    assert!(out.contains("GATE_FIXTURE_CONTENT"), "read did not render: {out}");
}

#[test]
fn gate_plain_markdown_passes_through() {
    let out = render("# Title\n\nsome **bold** text\n");
    assert!(out.contains("Title"));
    assert!(out.contains("<strong>bold</strong>"), "commonmark still works: {out}");
}
```

- [ ] **Step 2: Test scheitert nicht durch Compile, sondern läuft**

Run: `cargo nextest run -p lean-ctx --test lmd_phase1_gate`
Expected: PASS (2 tests).

> Falls `lean_ctx::lmd::engine::render` nicht sichtbar ist: sicherstellen, dass `lmd`, `engine` und `render` `pub` sind (lib.rs hat bereits `pub mod lmd;`).

- [ ] **Step 3: Volle Suite grün**

Run: `cargo nextest run -p lean-ctx lmd`
Expected: PASS (alle lmd-Unit- + Integrationstests).

- [ ] **Step 4: Reformat + Commit**

```bash
git add rust/tests/lmd_phase1_gate.rs
git commit -m "test(lmd): phase-1 gate e2e (header + @include + @read) "
```

---

## Self-Review

**Spec-Coverage (§6 Phase-1-Gate):**
- Header-Parser → Task 2 (`parse_header`). ✓
- Block-Parser → Task 5. Inline-Parser → Task 6. ✓
- Bridge-Registry → Task 7. ✓
- Fragment-Resolver (built-in-first) → Task 3. ✓
- Gate „`@lean-md`, `@include`, ein R-Router (`@read`) rendern e2e" → Tasks 11+12. ✓

**Typ-Konsistenz:** `DirectiveArgs` (Task 1) `positional`/`get` einheitlich in Tasks 8/9/10. `DirectiveBridge::execute(&Rc<EngineContext>, &DirectiveArgs) -> Result<String, BridgeError>` identisch in Tasks 7/8/9/10. `render_body(&Rc<EngineContext>, &str) -> String` identisch in Tasks 9/11. `FragmentRegistry::resolve(&str, &Path) -> Result<String, ResolveError>` (Task 3) → `BridgeError` via `From` (Task 9). `LmdRendererOptions { ctx: Rc<EngineContext> }` (Task 10) konsumiert in beiden Renderern.

**Bewusste Task-Kopplung:** Tasks 7–11 bilden einen Compile-Block (Trait + Bridges + Renderer + Engine referenzieren sich zyklisch). Tests für 7/8/9 sind in Task 11 grün — explizit notiert, kein verstecktes Placeholder. Reihenfolge 1→12 hält jeden vorgelagerten reinen Helper (args/header/fragments/parser-helper/node) einzeln testbar.

**Offen/deferred (nicht Phase 1, dokumentiert):** HTML-Escaping/Output-Format-Target, `@read`-Jail, Macro/`@if`/`{{ expr }}`-Eval (Phase 3), alle weiteren R-Bridges (Phase 2).
