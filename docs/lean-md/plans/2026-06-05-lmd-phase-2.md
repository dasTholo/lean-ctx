# lmd Phase 2 — R-Bridges (Daten-Direktiven) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:
> executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Die restlichen Router-Direktiven `@search`/`@list`/`@env`/`@date`/`@count`/`@query` als dünne
`DirectiveBridge`-Implementierungen ergänzen, die in bestehende lean-ctx-Core-APIs routen — `@graph` ist bewusst
ausgeklammert (jetzt Phase 3).

**Architecture:** Jede Bridge ist ein Zero-Field-Struct, das `DirectiveBridge` implementiert und in eine vorhandene
`crate::tools::*`-Core-Funktion routet (keine Neualgorithmik, Spec §3.1/§4.2). Der Render-Dispatch ist bereits
registry-getrieben (`render.rs::dispatch` → `ctx.registry.get(name)`), daher ist die **einzige** Verdrahtung pro Bridge:
Modul deklarieren + in `default_registry()` registrieren. Kein Parser-/Renderer-Change. `@query` erbt die
lean-ctx-Shell-Defense (validate_command + shell_allowlist + Secret-Redaction) und ist consumer-gegatet auf
`@lean-md shell=allow` (Spec §7).

**Tech Stack:** Rust, rushdown 0.18 (bestehende lmd-Extension), `cargo nextest`. Bereits vorhandene Deps: `chrono 0.4`,
`glob 0.3.3`, `walkdir 2`.

---

## Tool-Disziplin für die Ausführung (verbindlich)

- **Neue `.rs`-Dateien anlegen:** `Write` (Neuanlage ist kein „Editieren").
- **Bestehende `.rs` ändern** (`bridges/mod.rs`, `args.rs`): **Serena** — `mcp__serena__replace_content` für
  Modul-Deklarationen/Textblöcke, `mcp__serena__replace_symbol_body` für Funktions-/Enum-Körper. **Nie** native `Edit`/
  `ctx_edit` auf Rust.
- **Tests:** immer `cargo nextest run` (nie `cargo test`), via `ctx_shell`.
- **Vor jedem `git add`:** `mcp__jetbrains__reformat_file` auf jede geänderte Datei.
- Reads/Search via `ctx_read`/`ctx_search`, nie `fresh`/`raw`.

---

## File Structure

| Datei                            | Verantwortung                                                        | Aktion                      |
|----------------------------------|----------------------------------------------------------------------|-----------------------------|
| `rust/src/lmd/bridges/search.rs` | `@search` → `ctx_search::handle`                                     | Create                      |
| `rust/src/lmd/bridges/list.rs`   | `@list` → `ctx_tree::handle`                                         | Create                      |
| `rust/src/lmd/bridges/env.rs`    | `@env` → `std::env::var`                                             | Create                      |
| `rust/src/lmd/bridges/date.rs`   | `@date` → `chrono::Local::now`                                       | Create                      |
| `rust/src/lmd/bridges/count.rs`  | `@count` → `glob::glob`                                              | Create                      |
| `rust/src/lmd/bridges/query.rs`  | `@query` → Shell-Exec (gated, §7)                                    | Create                      |
| `rust/src/lmd/bridges/mod.rs`    | Modul-Deklarationen + `default_registry()` + `BridgeError`-Varianten | Modify                      |
| `rust/src/lmd/args.rs`           | `DirectiveArgs::raw()`-Accessor für `@query`                         | Modify                      |
| `rust/src/lmd/engine.rs`         | e2e-Render-Tests (`@query`-Gate)                                     | Modify (nur `#[cfg(test)]`) |

**Referenz-Muster:** `bridges/read.rs` (Router, nutzt `ctx.cache`), `bridges/include.rs` (Extension, nutzt
`ctx.enter/leave`). Neue Router-Bridges, die keinen Cache brauchen, nehmen `_ctx: &Rc<EngineContext>`.

**Verifizierte Core-Signaturen (Stand 2026-06-05):**

- `crate::tools::ctx_read::handle(&mut SessionCache, path: &str, mode: &str, CrpMode) -> String`
-
`crate::tools::ctx_search::handle(pattern: &str, dir: &str, ext_filter: Option<&str>, max_results: usize, CrpMode, respect_gitignore: bool, allow_secret_paths: bool) -> (String, usize)`
-
`crate::tools::ctx_tree::handle(path: &str, depth: usize, show_hidden: bool, respect_gitignore: bool) -> (String, usize)`
- `crate::tools::ctx_shell::validate_command(cmd: &str) -> Option<String>` (strict-mode `$()`/Backtick-Block)
- `crate::core::shell_allowlist::check_shell_allowlist(cmd: &str) -> Result<(), String>`
- `crate::server::execute::execute_command_with_env(cmd: &str, cwd: &str, &HashMap<String,String>) -> (String, i32)` (
  `pub(crate)`, aus `src/lmd` erreichbar)
- `crate::tools::ctx_shell::handle(cmd: &str, output: &str, CrpMode) -> String` (komprimiert bereits ausgeführten
  Output)
- `crate::core::secret_detection::scan_and_redact(&str, &cfg.secret_detection) -> (String, Vec<_>)`
- `crate::core::redaction::redact_text_if_enabled(&str) -> String`
- `CrpMode` liegt unter `crate::tools::CrpMode` (Variante `Off`).

**Bestehende `DirectiveBridge`-/`EngineContext`-Fakten:**

-
`trait DirectiveBridge { fn name(&self) -> &'static str; fn execute(&self, ctx: &Rc<EngineContext>, args: &DirectiveArgs) -> Result<String, BridgeError>; }`
- `enum BridgeError { MissingArg(&'static str), Resolve(String), Io(String), DepthExceeded }`
-
`EngineContext { header: LeanMdHeader, jail_root: PathBuf, fragments, registry, cache: RefCell<SessionCache>, max_chain_depth, depth }`
- `LeanMdHeader { version: Option<String>, consumer: Consumer, shell: ShellMode }`,
  `enum ShellMode { Deny (default), Allow }`.
- `DirectiveArgs::parse(raw) ` → `positional(i) -> Option<&str>`, `get(key) -> Option<&str>`.
- `default_registry()` registriert aktuell `read::ReadBridge` + `include::IncludeBridge`.
- `render.rs::dispatch` rendert `Err(e)` als `<!-- lmd:@<name> err: <Debug> -->` (sanitisiert), Miss als
  `<!-- lmd: unknown directive @<name> -->`.

---

## Task 1: `@search` Bridge → `ctx_search`

**Files:**

- Create: `rust/src/lmd/bridges/search.rs`
- Modify: `rust/src/lmd/bridges/mod.rs` (Modul + Registry)

- [ ] **Step 1: Failing test — neue Datei `search.rs` anlegen (Write)**

```rust
//! `@search` Router bridge → the same core as the `ctx_search` MCP tool.
use std::rc::Rc;

use super::{BridgeError, DirectiveBridge};
use crate::lmd::args::DirectiveArgs;
use crate::lmd::engine::EngineContext;

/// `@search <pattern> [path=<dir>] [ext=<ext>] [max=<n>]` — defaults: path=".",
/// max=20, gitignore respected, secret paths skipped. Routes `ctx_search::handle`.
/// Phase 2 passes `path` through unchanged (PathJail inheritance is §7/Phase-7).
pub struct SearchBridge;

impl DirectiveBridge for SearchBridge {
    fn name(&self) -> &'static str {
        "search"
    }

    fn execute(
        &self,
        _ctx: &Rc<EngineContext>,
        args: &DirectiveArgs,
    ) -> Result<String, BridgeError> {
        let pattern = args
            .positional(0)
            .or_else(|| args.get("pattern"))
            .ok_or(BridgeError::MissingArg("pattern"))?;
        let dir = args.get("path").unwrap_or(".");
        let ext = args.get("ext");
        let max = args
            .get("max")
            .and_then(|s| s.parse::<usize>().ok())
            .unwrap_or(20);
        let (out, _hits) = crate::tools::ctx_search::handle(
            pattern,
            dir,
            ext,
            max,
            crate::tools::CrpMode::Off,
            true,  // respect_gitignore
            false, // allow_secret_paths
        );
        Ok(out)
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::lmd::args::DirectiveArgs;
    use crate::lmd::engine::EngineContext;
    use crate::lmd::header::LeanMdHeader;
    use std::path::PathBuf;

    fn ctx() -> Rc<EngineContext> {
        Rc::new(EngineContext::new(LeanMdHeader::default(), PathBuf::from(".")))
    }

    #[test]
    fn searches_via_ctx_search() {
        let dir = std::env::temp_dir().join("lmd_search_bridge");
        std::fs::create_dir_all(&dir).unwrap();
        std::fs::write(dir.join("hit.txt"), "fn target_marker_42() {}\n").unwrap();
        let args = DirectiveArgs::parse(&format!(
            "target_marker_42 path={}",
            dir.to_str().unwrap()
        ));
        let out = SearchBridge.execute(&ctx(), &args).unwrap();
        assert!(out.contains("target_marker_42"), "got: {out}");
    }

    #[test]
    fn missing_pattern_errors() {
        let err = SearchBridge
            .execute(&ctx(), &DirectiveArgs::parse(""))
            .unwrap_err();
        assert!(matches!(err, BridgeError::MissingArg(_)));
    }

    #[test]
    fn search_is_registered() {
        assert!(super::super::default_registry().get("search").is_some());
    }
}
```

- [ ] **Step 2: Modul deklarieren + registrieren in `mod.rs` (Serena)**

`mcp__serena__replace_content` — Modul-Deklaration nach `pub mod read;` einfügen:

old:

```rust
pub mod include;
pub mod read;
```

new:

```rust
pub mod include;
pub mod read;
pub mod search;
```

`mcp__serena__replace_symbol_body` auf `default_registry` — neuen Body:

```rust
    let mut reg = BridgeRegistry::new();
reg.register(Box::new(read::ReadBridge));
reg.register(Box::new(include::IncludeBridge));
reg.register(Box::new(search::SearchBridge));
reg
```

- [ ] **Step 3: Test laufen lassen — muss vor Step 2 fehlschlagen, jetzt grün**

Run (via `ctx_shell`, cwd `rust/`): `cargo nextest run lmd::bridges::search`
Expected: 3 Tests PASS (`searches_via_ctx_search`, `missing_pattern_errors`, `search_is_registered`).

- [ ] **Step 4: Reformat + Commit**

```bash
# reformat: mcp__jetbrains__reformat_file auf search.rs + bridges/mod.rs
git add rust/src/lmd/bridges/search.rs rust/src/lmd/bridges/mod.rs
git commit -m "feat(lmd): @search router bridge → ctx_search (Phase 2)"
```

---

## Task 2: `@list` Bridge → `ctx_tree`

**Files:**

- Create: `rust/src/lmd/bridges/list.rs`
- Modify: `rust/src/lmd/bridges/mod.rs`

- [ ] **Step 1: `list.rs` anlegen (Write)**

```rust
//! `@list` Router bridge → the same core as the `ctx_tree` MCP tool.
use std::rc::Rc;

use super::{BridgeError, DirectiveBridge};
use crate::lmd::args::DirectiveArgs;
use crate::lmd::engine::EngineContext;

/// `@list [path] [depth=<n>]` — defaults path=".", depth=2, hidden files off,
/// gitignore respected. Routes `ctx_tree::handle`. No required arg.
pub struct ListBridge;

impl DirectiveBridge for ListBridge {
    fn name(&self) -> &'static str {
        "list"
    }

    fn execute(
        &self,
        _ctx: &Rc<EngineContext>,
        args: &DirectiveArgs,
    ) -> Result<String, BridgeError> {
        let path = args.positional(0).or_else(|| args.get("path")).unwrap_or(".");
        let depth = args
            .get("depth")
            .and_then(|s| s.parse::<usize>().ok())
            .unwrap_or(2);
        let (out, _n) = crate::tools::ctx_tree::handle(path, depth, false, true);
        Ok(out)
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::lmd::args::DirectiveArgs;
    use crate::lmd::engine::EngineContext;
    use crate::lmd::header::LeanMdHeader;
    use std::path::PathBuf;

    fn ctx() -> Rc<EngineContext> {
        Rc::new(EngineContext::new(LeanMdHeader::default(), PathBuf::from(".")))
    }

    #[test]
    fn lists_a_directory_via_ctx_tree() {
        let dir = std::env::temp_dir().join("lmd_list_bridge");
        std::fs::create_dir_all(&dir).unwrap();
        std::fs::write(dir.join("listed_marker.txt"), "x\n").unwrap();
        let args = DirectiveArgs::parse(dir.to_str().unwrap());
        let out = ListBridge.execute(&ctx(), &args).unwrap();
        assert!(out.contains("listed_marker"), "got: {out}");
    }

    #[test]
    fn list_is_registered() {
        assert!(super::super::default_registry().get("list").is_some());
    }
}
```

- [ ] **Step 2: `mod.rs` (Serena)**

`replace_content` — Modul nach `pub mod include;` (alphabetisch) einsortieren:
old:

```rust
pub mod include;
pub mod read;
pub mod search;
```

new:

```rust
pub mod include;
pub mod list;
pub mod read;
pub mod search;
```

`replace_symbol_body` auf `default_registry` — Zeile ergänzen:

```rust
    let mut reg = BridgeRegistry::new();
reg.register(Box::new(read::ReadBridge));
reg.register(Box::new(include::IncludeBridge));
reg.register(Box::new(search::SearchBridge));
reg.register(Box::new(list::ListBridge));
reg
```

- [ ] **Step 3: Run** `cargo nextest run lmd::bridges::list`
  Expected: 2 Tests PASS.

- [ ] **Step 4: Reformat + Commit**

```bash
# reformat list.rs + bridges/mod.rs
git add rust/src/lmd/bridges/list.rs rust/src/lmd/bridges/mod.rs
git commit -m "feat(lmd): @list router bridge → ctx_tree (Phase 2)"
```

---

## Task 3: `@env` Bridge → `std::env::var`

**Files:**

- Create: `rust/src/lmd/bridges/env.rs`
- Modify: `rust/src/lmd/bridges/mod.rs`

- [ ] **Step 1: `env.rs` anlegen (Write)**

```rust
//! `@env` Router bridge → process environment lookup (Spec §3.1 trivial-R).
use std::rc::Rc;

use super::{BridgeError, DirectiveBridge};
use crate::lmd::args::DirectiveArgs;
use crate::lmd::engine::EngineContext;

/// `@env <KEY>` — expands to the value of env var `KEY`, or empty if unset.
pub struct EnvBridge;

impl DirectiveBridge for EnvBridge {
    fn name(&self) -> &'static str {
        "env"
    }

    fn execute(
        &self,
        _ctx: &Rc<EngineContext>,
        args: &DirectiveArgs,
    ) -> Result<String, BridgeError> {
        let key = args.positional(0).ok_or(BridgeError::MissingArg("key"))?;
        Ok(std::env::var(key).unwrap_or_default())
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::lmd::args::DirectiveArgs;
    use crate::lmd::engine::EngineContext;
    use crate::lmd::header::LeanMdHeader;
    use std::path::PathBuf;

    fn ctx() -> Rc<EngineContext> {
        Rc::new(EngineContext::new(LeanMdHeader::default(), PathBuf::from(".")))
    }

    #[test]
    fn expands_set_var() {
        // SAFETY: single-threaded test, no concurrent env access.
        unsafe { std::env::set_var("LMD_ENV_TEST_KEY", "env_marker_7") };
        let out = EnvBridge
            .execute(&ctx(), &DirectiveArgs::parse("LMD_ENV_TEST_KEY"))
            .unwrap();
        assert_eq!(out, "env_marker_7");
    }

    #[test]
    fn unset_var_is_empty() {
        let out = EnvBridge
            .execute(&ctx(), &DirectiveArgs::parse("LMD_DEFINITELY_UNSET_XYZ"))
            .unwrap();
        assert_eq!(out, "");
    }

    #[test]
    fn missing_key_errors() {
        let err = EnvBridge.execute(&ctx(), &DirectiveArgs::parse("")).unwrap_err();
        assert!(matches!(err, BridgeError::MissingArg(_)));
    }

    #[test]
    fn env_is_registered() {
        assert!(super::super::default_registry().get("env").is_some());
    }
}
```

> Hinweis: `std::env::set_var` ist in Rust 2024 `unsafe` — der `unsafe`-Block oben ist korrekt. Falls das Crate auf
> einer älteren Edition steht und ein `unused_unsafe`-Lint feuert, den `unsafe`-Wrapper entfernen.

- [ ] **Step 2: `mod.rs` (Serena)**

`replace_content`:
old:

```rust
pub mod include;
pub mod list;
pub mod read;
pub mod search;
```

new:

```rust
pub mod env;
pub mod include;
pub mod list;
pub mod read;
pub mod search;
```

`replace_symbol_body` auf `default_registry` — ergänzen:

```rust
    reg.register(Box::new(env::EnvBridge));
```

(als letzte `register`-Zeile vor `reg`).

- [ ] **Step 3: Run** `cargo nextest run lmd::bridges::env`
  Expected: 4 Tests PASS.

- [ ] **Step 4: Reformat + Commit**

```bash
# reformat env.rs + bridges/mod.rs
git add rust/src/lmd/bridges/env.rs rust/src/lmd/bridges/mod.rs
git commit -m "feat(lmd): @env router bridge (Phase 2)"
```

---

## Task 4: `@date` Bridge → `chrono`

**Files:**

- Create: `rust/src/lmd/bridges/date.rs`
- Modify: `rust/src/lmd/bridges/mod.rs`

- [ ] **Step 1: `date.rs` anlegen (Write)**

```rust
//! `@date` Router bridge → current local date via chrono (Spec §3.1 trivial-R).
use std::rc::Rc;

use super::{BridgeError, DirectiveBridge};
use crate::lmd::args::DirectiveArgs;
use crate::lmd::engine::EngineContext;

/// `@date [fmt=<strftime>]` — current local date, default `%Y-%m-%d`.
pub struct DateBridge;

impl DirectiveBridge for DateBridge {
    fn name(&self) -> &'static str {
        "date"
    }

    fn execute(
        &self,
        _ctx: &Rc<EngineContext>,
        args: &DirectiveArgs,
    ) -> Result<String, BridgeError> {
        let fmt = args.get("fmt").unwrap_or("%Y-%m-%d");
        Ok(chrono::Local::now().format(fmt).to_string())
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::lmd::args::DirectiveArgs;
    use crate::lmd::engine::EngineContext;
    use crate::lmd::header::LeanMdHeader;
    use std::path::PathBuf;

    fn ctx() -> Rc<EngineContext> {
        Rc::new(EngineContext::new(LeanMdHeader::default(), PathBuf::from(".")))
    }

    #[test]
    fn default_format_is_iso_date() {
        let out = DateBridge.execute(&ctx(), &DirectiveArgs::parse("")).unwrap();
        // YYYY-MM-DD => length 10, dashes at index 4 and 7.
        assert_eq!(out.len(), 10, "got: {out}");
        assert_eq!(out.as_bytes()[4], b'-', "got: {out}");
        assert_eq!(out.as_bytes()[7], b'-', "got: {out}");
    }

    #[test]
    fn custom_format_year_only() {
        let out = DateBridge
            .execute(&ctx(), &DirectiveArgs::parse("fmt=%Y"))
            .unwrap();
        assert_eq!(out.len(), 4, "got: {out}");
        assert!(out.chars().all(|c| c.is_ascii_digit()), "got: {out}");
    }

    #[test]
    fn date_is_registered() {
        assert!(super::super::default_registry().get("date").is_some());
    }
}
```

- [ ] **Step 2: `mod.rs` (Serena)**

`replace_content`:
old:

```rust
pub mod env;
pub mod include;
```

new:

```rust
pub mod date;
pub mod env;
pub mod include;
```

`replace_symbol_body` auf `default_registry` — ergänzen:

```rust
    reg.register(Box::new(date::DateBridge));
```

- [ ] **Step 3: Run** `cargo nextest run lmd::bridges::date`
  Expected: 3 Tests PASS.

- [ ] **Step 4: Reformat + Commit**

```bash
# reformat date.rs + bridges/mod.rs
git add rust/src/lmd/bridges/date.rs rust/src/lmd/bridges/mod.rs
git commit -m "feat(lmd): @date router bridge → chrono (Phase 2)"
```

---

## Task 5: `@count` Bridge → `glob`

**Files:**

- Create: `rust/src/lmd/bridges/count.rs`
- Modify: `rust/src/lmd/bridges/mod.rs`

- [ ] **Step 1: `count.rs` anlegen (Write)**

```rust
//! `@count` Router bridge → glob match count (Spec §3.1 trivial-R).
use std::rc::Rc;

use super::{BridgeError, DirectiveBridge};
use crate::lmd::args::DirectiveArgs;
use crate::lmd::engine::EngineContext;

/// `@count <glob>` — number of filesystem paths matching the glob pattern.
pub struct CountBridge;

impl DirectiveBridge for CountBridge {
    fn name(&self) -> &'static str {
        "count"
    }

    fn execute(
        &self,
        _ctx: &Rc<EngineContext>,
        args: &DirectiveArgs,
    ) -> Result<String, BridgeError> {
        let pattern = args
            .positional(0)
            .ok_or(BridgeError::MissingArg("pattern"))?;
        match glob::glob(pattern) {
            Ok(paths) => Ok(paths.filter(|r| r.is_ok()).count().to_string()),
            Err(e) => Err(BridgeError::Resolve(format!("bad glob: {e}"))),
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::lmd::args::DirectiveArgs;
    use crate::lmd::engine::EngineContext;
    use crate::lmd::header::LeanMdHeader;
    use std::path::PathBuf;

    fn ctx() -> Rc<EngineContext> {
        Rc::new(EngineContext::new(LeanMdHeader::default(), PathBuf::from(".")))
    }

    #[test]
    fn counts_matching_files() {
        let dir = std::env::temp_dir().join("lmd_count_bridge");
        std::fs::create_dir_all(&dir).unwrap();
        for i in 0..3 {
            std::fs::write(dir.join(format!("c{i}.cnt")), "x").unwrap();
        }
        let args = DirectiveArgs::parse(&format!("{}/*.cnt", dir.to_str().unwrap()));
        let out = CountBridge.execute(&ctx(), &args).unwrap();
        assert_eq!(out, "3", "got: {out}");
    }

    #[test]
    fn missing_pattern_errors() {
        let err = CountBridge
            .execute(&ctx(), &DirectiveArgs::parse(""))
            .unwrap_err();
        assert!(matches!(err, BridgeError::MissingArg(_)));
    }

    #[test]
    fn count_is_registered() {
        assert!(super::super::default_registry().get("count").is_some());
    }
}
```

- [ ] **Step 2: `mod.rs` (Serena)**

`replace_content`:
old:

```rust
pub mod date;
pub mod env;
```

new:

```rust
pub mod count;
pub mod date;
pub mod env;
```

`replace_symbol_body` auf `default_registry` — ergänzen:

```rust
    reg.register(Box::new(count::CountBridge));
```

- [ ] **Step 3: Run** `cargo nextest run lmd::bridges::count`
  Expected: 3 Tests PASS.

- [ ] **Step 4: Reformat + Commit**

```bash
# reformat count.rs + bridges/mod.rs
git add rust/src/lmd/bridges/count.rs rust/src/lmd/bridges/mod.rs
git commit -m "feat(lmd): @count router bridge → glob (Phase 2)"
```

---

## Task 6: `DirectiveArgs::raw()` — Vorbereitung für `@query`

`@query` braucht die **vollständige** Kommandozeile (mit Leerzeichen); `parse()` tokenisiert sie. Wir bewahren
zusätzlich den rohen Reststring auf.

**Files:**

- Modify: `rust/src/lmd/args.rs`

- [ ] **Step 1: Failing test — in `args.rs` `#[cfg(test)] mod tests` (Serena `insert_after_symbol` nach `empty_is_empty`
  oder `replace_symbol_body` des tests-Moduls)**

Neuer Test:

```rust
    #[test]
fn raw_preserves_full_command_line() {
    let a = DirectiveArgs::parse("echo hello world mode=x");
    assert_eq!(a.raw(), "echo hello world mode=x");
}
```

- [ ] **Step 2: Run → FAIL**

Run: `cargo nextest run lmd::args::tests::raw_preserves_full_command_line`
Expected: FAIL (compile error: no method `raw`).

- [ ] **Step 3: Feld + Accessor implementieren (Serena)**

`replace_symbol_body` auf das Struct ist nicht nötig — Felder ergänzen via `replace_content`.

`replace_content` — Struct-Felder:
old:

```rust
pub struct DirectiveArgs {
    positional: Vec<String>,
    named: Vec<(String, String)>,
}
```

new:

```rust
pub struct DirectiveArgs {
    positional: Vec<String>,
    named: Vec<(String, String)>,
    raw: String,
}
```

`replace_symbol_body` auf `parse` — `raw` setzen und in den Konstruktor aufnehmen:

```rust
        let mut positional = Vec::new();
let mut named = Vec::new();
for tok in raw.split_whitespace() {
match tok.split_once('=') {
Some((k, v)) if !k.is_empty() => named.push((k.to_string(), v.to_string())),
_ => positional.push(tok.to_string()),
}
}
Self {
positional,
named,
raw: raw.trim().to_string(),
}
```

`replace_content` — neuen Accessor nach `get()` einfügen (innerhalb `impl DirectiveArgs`, vor der schließenden `}` des
Impls):
old:

```rust
    pub fn get(&self, key: &str) -> Option<&str> {
    self.named
        .iter()
        .find(|(k, _)| k == key)
        .map(|(_, v)| v.as_str())
}
}
```

new:

```rust
    pub fn get(&self, key: &str) -> Option<&str> {
    self.named
        .iter()
        .find(|(k, _)| k == key)
        .map(|(_, v)| v.as_str())
}

/// The trimmed raw argument string (everything after the directive name),
/// space-preserving — needed by `@query` whose command contains spaces.
pub fn raw(&self) -> &str {
    &self.raw
}
}
```

- [ ] **Step 4: Run → PASS**

Run: `cargo nextest run lmd::args`
Expected: alle args-Tests PASS (inkl. `raw_preserves_full_command_line`, `parses_positional_and_named`,
`empty_is_empty`).

- [ ] **Step 5: Reformat + Commit**

```bash
# reformat args.rs
git add rust/src/lmd/args.rs
git commit -m "feat(lmd): DirectiveArgs::raw() preserves full command line"
```

---

## Task 7: `@query` Bridge → Shell-Exec (consumer-gated, §7)

Die einzige Bridge mit Seiteneffekt. **Erbt** die lean-ctx-Shell-Defense, erfindet keine eigenen Deny-Patterns (Spec
§7): consumer-Gate (`shell=allow`) → `validate_command` (strict-mode `$()`/Backtick) → `check_shell_allowlist` → Exec →
Secret-Redaction → Kompression.

**Files:**

- Modify: `rust/src/lmd/bridges/mod.rs` (`BridgeError`-Varianten)
- Create: `rust/src/lmd/bridges/query.rs`
- Modify: `rust/src/lmd/engine.rs` (e2e-Tests)

- [ ] **Step 1: `BridgeError` um Shell-Varianten erweitern (Serena, `mod.rs`)**

`replace_symbol_body` auf `enum BridgeError` (bzw. `replace_content` des Enum-Blocks):
old:

```rust
pub enum BridgeError {
    MissingArg(&'static str),
    Resolve(String),
    Io(String),
    DepthExceeded,
}
```

new:

```rust
pub enum BridgeError {
    MissingArg(&'static str),
    Resolve(String),
    Io(String),
    DepthExceeded,
    /// `@query` invoked without `@lean-md shell=allow` (Spec §7 consumer gate).
    ShellDenied,
    /// `@query` command rejected by an inherited lean-ctx shell defense
    /// (strict-mode `$()`/backtick block or shell allowlist).
    ShellRejected(String),
}
```

- [ ] **Step 2: `query.rs` anlegen (Write) — mit Tests**

```rust
//! `@query` Router bridge → executes a shell command via the same path as the
//! `ctx_shell` MCP tool, then compresses output. Consumer-gated: only runs with
//! `@lean-md shell=allow` (Spec §7). Inherits validate_command (strict-mode
//! $()/backtick block) + shell_allowlist + secret redaction — invents no new
//! deny-patterns.
use std::collections::HashMap;
use std::rc::Rc;

use super::{BridgeError, DirectiveBridge};
use crate::lmd::args::DirectiveArgs;
use crate::lmd::engine::EngineContext;
use crate::lmd::header::ShellMode;

pub struct QueryBridge;

impl DirectiveBridge for QueryBridge {
    fn name(&self) -> &'static str {
        "query"
    }

    fn execute(
        &self,
        ctx: &Rc<EngineContext>,
        args: &DirectiveArgs,
    ) -> Result<String, BridgeError> {
        // §7 consumer gate: shell only with `@lean-md shell=allow`.
        if ctx.header.shell != ShellMode::Allow {
            return Err(BridgeError::ShellDenied);
        }
        let cmd = args.raw();
        if cmd.is_empty() {
            return Err(BridgeError::MissingArg("command"));
        }
        // Inherited lean-ctx shell defenses — no new deny-patterns (§7).
        if let Some(rejection) = crate::tools::ctx_shell::validate_command(cmd) {
            return Err(BridgeError::ShellRejected(rejection));
        }
        if let Err(msg) = crate::core::shell_allowlist::check_shell_allowlist(cmd) {
            return Err(BridgeError::ShellRejected(msg));
        }
        let cwd = ctx.jail_root.to_string_lossy().to_string();
        let (raw_output, _exit) =
            crate::server::execute::execute_command_with_env(cmd, &cwd, &HashMap::new());
        // Secret-Redaction (nur bei enabled) — inherited, not reinvented (§7).
        let cfg = crate::core::config::Config::load();
        let safe_output = if cfg.secret_detection.enabled {
            crate::core::secret_detection::scan_and_redact(&raw_output, &cfg.secret_detection).0
        } else {
            raw_output
        };
        let compressed =
            crate::tools::ctx_shell::handle(cmd, &safe_output, crate::tools::CrpMode::Off);
        Ok(crate::core::redaction::redact_text_if_enabled(&compressed))
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use crate::lmd::args::DirectiveArgs;
    use crate::lmd::engine::EngineContext;
    use crate::lmd::header::{LeanMdHeader, ShellMode};
    use std::path::PathBuf;

    fn ctx_with_shell(mode: ShellMode) -> Rc<EngineContext> {
        let header = LeanMdHeader {
            shell: mode,
            ..Default::default()
        };
        Rc::new(EngineContext::new(header, PathBuf::from(".")))
    }

    #[test]
    fn denied_without_shell_allow() {
        let err = QueryBridge
            .execute(
                &ctx_with_shell(ShellMode::Deny),
                &DirectiveArgs::parse("git --version"),
            )
            .unwrap_err();
        assert!(matches!(err, BridgeError::ShellDenied));
    }

    #[test]
    fn runs_allowlisted_command_with_shell_allow() {
        // Hermetic: pin the allowlist via override so the test does NOT depend on
        // the user's config (default ships a NON-empty deny-by-default allowlist).
        // `git` is representative of @query's real purpose — a shell-only tool with
        // no native lmd directive (unlike echo/cat/ls, which @read/@search/@list
        // already cover). nextest = process-per-test, so the env override is isolated.
        // SAFETY: single-threaded test process.
        unsafe { std::env::set_var("LEAN_CTX_SHELL_ALLOWLIST_OVERRIDE", "git") };
        let out = QueryBridge
            .execute(
                &ctx_with_shell(ShellMode::Allow),
                &DirectiveArgs::parse("git --version"),
            )
            .unwrap();
        unsafe { std::env::remove_var("LEAN_CTX_SHELL_ALLOWLIST_OVERRIDE") };
        assert!(out.contains("git version"), "got: {out}");
    }

    #[test]
    fn inherits_allowlist_deny_by_default() {
        // §7: a non-allowlisted base command is hard-blocked — @query inherits the
        // deny-by-default gate via check_shell_allowlist, it does not reinvent it.
        unsafe { std::env::set_var("LEAN_CTX_SHELL_ALLOWLIST_OVERRIDE", "git") };
        let err = QueryBridge
            .execute(
                &ctx_with_shell(ShellMode::Allow),
                &DirectiveArgs::parse("ls -la"),
            )
            .unwrap_err();
        unsafe { std::env::remove_var("LEAN_CTX_SHELL_ALLOWLIST_OVERRIDE") };
        assert!(matches!(err, BridgeError::ShellRejected(_)));
    }

    #[test]
    fn empty_command_errors() {
        let err = QueryBridge
            .execute(&ctx_with_shell(ShellMode::Allow), &DirectiveArgs::parse(""))
            .unwrap_err();
        assert!(matches!(err, BridgeError::MissingArg(_)));
    }

    #[test]
    fn query_is_registered() {
        assert!(super::super::default_registry().get("query").is_some());
    }
}
```

- [ ] **Step 3: `mod.rs` Modul + Registry (Serena)**

`replace_content`:
old:

```rust
pub mod read;
pub mod search;
```

new:

```rust
pub mod query;
pub mod read;
pub mod search;
```

`replace_symbol_body` auf `default_registry` — ergänzen:

```rust
    reg.register(Box::new(query::QueryBridge));
```

- [ ] **Step 4: e2e-Gate-Tests in `engine.rs` (Serena `insert_after_symbol` nach `inline_include_dispatches`)**

```rust
    #[test]
fn query_denied_without_shell_allow() {
    // Default header => shell=deny => @query must not execute.
    let out = render("@query git --version\n");
    assert!(
        !out.contains("git version"),
        "shell must be denied without shell=allow; got: {out}"
    );
}

#[test]
fn query_runs_with_shell_allow_header() {
    // Hermetic allowlist pin (see bridge unit test). nextest = process-per-test.
    // SAFETY: single-threaded test process.
    unsafe { std::env::set_var("LEAN_CTX_SHELL_ALLOWLIST_OVERRIDE", "git") };
    let out = render("@lean-md\nshell: allow\n\n@query git --version\n");
    unsafe { std::env::remove_var("LEAN_CTX_SHELL_ALLOWLIST_OVERRIDE") };
    assert!(out.contains("git version"), "got: {out}");
}
```

- [ ] **Step 5: Run → PASS**

Run: `cargo nextest run lmd::bridges::query` und `cargo nextest run lmd::engine::tests::query`
Expected: 5 Bridge-Tests + 2 Engine-Tests PASS. (`runs_allowlisted_command_with_shell_allow`/
`query_runs_with_shell_allow_header` verifizieren Exec; `denied_without_shell_allow` das consumer-Gate;
`inherits_allowlist_deny_by_default` beweist das **geerbte Deny-by-Default-Allowlist-Gate** — `ls` ist zwar in der
Default-Allowlist, aber unter dem Override `git` nicht → `ShellRejected`, §7.)

- [ ] **Step 6: Reformat + Commit**

```bash
# reformat query.rs + bridges/mod.rs + engine.rs
git add rust/src/lmd/bridges/query.rs rust/src/lmd/bridges/mod.rs rust/src/lmd/engine.rs
git commit -m "feat(lmd): @query shell bridge — consumer-gated, inherits §7 defenses (Phase 2)"
```

---

## Task 8: Phase-2-Abschluss — Voll-Suite + Spec-Statusupdate

**Files:**

- Modify: `docs/lean-md/specs/2026-05-31-lmd-lean-ctx-native-design.mdai.md` (§6 Phase-2-Gate-Zeile)

- [ ] **Step 1: Komplette lmd-Suite grün**

Run: `cargo nextest run lmd`
Expected: alle bisherigen 35 Phase-1-Tests + die neuen Phase-2-Tests PASS, 0 Failures. Notiere die Gesamtzahl.

- [ ] **Step 2: Clippy sauber**

Run: `cargo clippy -p <lmd-crate> -- -D warnings` (Paketname aus `rust/Cargo.toml`)
Expected: keine Warnings auf den neuen Dateien.

- [ ] **Step 3: §6 Phase-2-Zeile auf „bestanden" setzen (`ctx_edit`, da Markdown)**

In der §6-Tabelle die Phase-2-Zeile ergänzen:
`✅ **bestanden** — R-Bridges @search/@list/@env/@date/@count/@query live; @query consumer-gegatet (§7); <N> lmd-Tests grün`. (
Kein `@graph` — Phase 3.)

- [ ] **Step 4: Commit**

```bash
git add docs/lean-md/specs/2026-05-31-lmd-lean-ctx-native-design.mdai.md
git commit -m "docs(lmd): mark Phase-2 gate passed (R-bridges live)"
```

---

## Self-Review

**1. Spec-Coverage (§3.1 R-Direktiven, ohne @graph):**

- `@search` → Task 1 ✅ · `@list` → Task 2 ✅ · `@env` → Task 3 ✅ · `@date` → Task 4 ✅ · `@count` → Task 5 ✅ · `@query` →
  Task 7 ✅
- `@read`/`@include` bereits Phase 1 (nicht erneut). `@graph` bewusst ausgeklammert → Phase 3.
- §7 Shell-Gate: consumer-Gate (`shell=allow`) + **geerbtes Deny-by-Default-Allowlist-Gate** (`check_shell_allowlist`,
  nicht-leere Liste = Default) + validate_command + Secret-Redaction → Task 7 ✅; `inherits_allowlist_deny_by_default`
  -Test beweist die Vererbung, erfindet nichts neu. `@query` ist nur für shell-only-Tools (git/cargo/make), nicht für
  I/O mit nativer Direktive.
- §4.2/§4.2a: Router-Bridges ohne Cache nehmen `_ctx`; nur `@read` nutzt `ctx.cache` (unverändert) ✅.

**2. Placeholder-Scan:** Keine TBD/TODO; jeder Code-Step zeigt vollständigen Code + exakte Pfade + erwartete
Test-Ergebnisse.

**3. Typ-Konsistenz:** `execute(&self, ctx: &Rc<EngineContext>, args: &DirectiveArgs) -> Result<String, BridgeError>`
überall identisch. `CrpMode::Off` als `crate::tools::CrpMode::Off` durchgängig. Tupel-Rückgaben (`ctx_search`/
`ctx_tree`) korrekt destrukturiert (`(out, _n)`). `BridgeError`-Varianten in Task 7/Step 1 definiert, bevor Task 7/Step
2 sie nutzt. `DirectiveArgs::raw()` in Task 6 definiert, bevor Task 7 es nutzt.

**Risiko-Ordnung (wie gefordert):** read-Klone (search/list) zuerst → triviale (env/date/count) → `@query` (
Security-Checkpoint) zuletzt. Jede Task ist eigenständig committbar und lässt die Suite grün.
